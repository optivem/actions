# Auditor / Reviewer consistency audit — 2026-04-25

## Header

- `depth`: standard
- Subject files (line counts):
  - `.claude/agents/actions-auditor.md` — 482 lines
  - `.claude/agents/docs/devops-rubric.md` — 659 lines
  - `.claude/agents/actions-auditor-reviewer.md` — 191 lines
  - `.claude/agents/docs/review-dimensions.md` — 98 lines
- Date: 2026-04-25
- Pre-existing in-progress consistency report in `plans/`: none (only an unrelated `20260424-101715-component-docker-tagging.md` is present; not touched).

## Summary

The auditor / reviewer pair is **broadly mutually consistent**. All rubric sections (§1, §1.1, §1.2, §2, §3, §3.1, §4, §5, §6, §7, §8) referenced by the auditor exist; all dimensions (1, 2, 2b, 3, 4, 5) referenced by the reviewer exist; the file-path patterns (`*-audit-actions*.md` vs `*-review-actions-auditor.md`) are mutually exclusive; the auditor's read-only posture and tool surface are honoured by both files; the reviewer's `Edit` capability is justified by its self-improvement policy. The drift is minor — a small number of citation-line ranges, terminology variants, and asymmetries in the plan/report split (auditor produces report+plan in two directories; reviewer writes a single combined file to `plans/`).

**Top three conflicts:**

1. **Plan/report split asymmetry** — the auditor splits each run into `.reports/<ts>-…md` (frozen findings) plus `plans/<ts>-…md` (decision queue) and explicitly defends the split (auditor lines 148–151). The reviewer writes a *single* file to `plans/` (reviewer lines 154–166) that mixes findings AND a Recommended-edits queue, with the same `plans/` lifecycle rule applied to it. This is a real drift in the project's plan/report convention.
2. **Citation line range slightly off** — the reviewer points at `actions-auditor.md:26–32` for "publication intent" (reviewer line 190) but the publication-intent block is lines 32–38; lines 26–28 are the `mode` option.
3. **Reviewer's Process step 2 imposes a contract the auditor does NOT fully meet** — step 2 says every rubric rule must produce output in the auditor's Output section, but several rubric rules (§1.5 SemVer composition, §1.6 BuildKit fused step, §3.6 tag-vs-commit-status mechanism mismatch, §4 "deploy-* must include environment+service" Farley contract) have no dedicated finding slot in the auditor's Output schema — they fall into the catch-all "DevOps alignment → Other" subsection but the reviewer's literal cross-reference test will read this as a gap.

**Counts** — reference-integrity 2 | output-schema-contract 3 | scope-boundary 1 | terminology 4 | exemption-propagation 2 | tool-capability 2 | shared-principle 2 | plan-file 2 | additional 4

## Reference integrity findings

| Citing file + line | Cited target | Resolution state | Proposed fix |
|---|---|---|---|
| `actions-auditor-reviewer.md:190` | `actions-auditor.md:26–32` for "the auditor's declared publication intent" | ambiguous (range partly off) | Lines 26–28 are the `mode` option; the publication-intent block runs from line 32 to ~line 38. Update the reviewer citation to `actions-auditor.md:32–38`. |
| `actions-auditor-reviewer.md:42` | "When the auditor is invoked with `scope = naming`, Duplicates / Consolidation / DevOps alignment / Usage sections are rendered with body 'Skipped (scope = naming)'" | ambiguous | The auditor only documents the body-string `Skipped (scope = naming).` for the **naming-skipped** sections at line 209 (which says "The corresponding sections still appear with the body `Skipped (scope = naming).`"). The auditor's per-section bodies (lines 246–289) do NOT spell out the literal `Skipped` string in each section — they assume the header convention. The cross-reference holds, but is implicit; the auditor's output schema would be clearer if each affected section explicitly named the placeholder body when `scope = naming`. Debatable; flag for the author. |

Other cross-references all resolve cleanly:

- Auditor → rubric §1 (line 26), §1.1 (line 36), §1.2 (line 46), §2 (line 284), §3 (line 296), §3.1 (line 300), §4 (line 423), §5 (line 465), §6 (line 495), §7 (line 546), §8 (line 625): all exist.
- Auditor's Parameter findings rule codes (`name-kebab-case`, `name-mainstream-convention`, `name-misleading`, `output-name-verb-led`, `type-shape-ambiguous`, `required-implicit`, `required-default-contradiction`, `description-missing`, `description-tautological`, `description-too-terse`, `default-placeholder`, `deprecation-no-replacement`): every one is introduced in Process step 3 (auditor lines 87–120) and reused under "Parameter findings — External (breaking)" (line 249) / "Parameter findings — External (non-breaking)" (line 276) / "Parameter findings — Internal" (line 365). No dangling citations.
- Reviewer → review-dimensions §1, §2, §2b, §3, §4, §5: all exist (review-dimensions.md lines 11, 29, 47, 60, 74, 90).
- Reviewer's references to the auditor's "Process section" (auditor line 77) and "Output section" (auditor line 146): both real headings.
- Reviewer's reference to "forward-looking exemptions" (reviewer line 190 → "documented in `devops-rubric.md` §2") resolves to rubric line 284 ("# 2. Forward-looking context (repo-specific exemptions)").

## Output-schema contract findings

| What the reviewer expects | What the auditor actually produces | Effect | Proposed fix |
|---|---|---|---|
| Reviewer process step 2 (line 42): "for each rule in the rubric, verify it … **produces output in the auditor's Output section**." | Several rubric sub-rules have no dedicated finding slot in the auditor's Output: §1.5 SemVer composition, §1.6 BuildKit fused build/tag/push, §3.6 tag-vs-commit-status mechanism mismatch, §4 "deploy-* must include environment+service" (rubric line 34). The auditor's Output organises DevOps alignment under named subsections "Tool-agnostic composition / Separation of concerns / Composite opacity / Prefer VCS over platform API / Composition ordering / Idempotence / Secrets / auth / Other" (auditor line 266). The first three rules listed above end up under **Other**, but they are conceptually first-class DevOps dimensions. The §4 deploy-contract rule has no obvious home — it is a **Naming** finding *and* a Farley CD-alignment finding at once. | Reviewer's literal cross-reference test will read these as silently absent slots. Not a real gap (Other is the catch-all and the rubric's Filing Guide §8 says so), but the asymmetry between "first-class subsection in §1" and "filed under Other" is uneven. | Add the missing categories to the auditor's DevOps alignment subsection list (auditor line 266 and line 374): include **Version handling (§1.5)**, **Container build/tag/push (§1.6)**, **Tag vs commit-status mechanism (§3.6)**, and **Deploy contract (§4 deploy-*)**. Or, less invasively, extend rubric §8's filing guide with explicit row pointers naming "Other" as the home for these four and reference §8 from the auditor's Output schema. Debatable. |
| Reviewer process step 2 (line 42): treats `[SKIPPED — breaking]` items as correctly populated. | Auditor's `[SKIPPED — breaking]` convention is defined in the Backwards-compatibility-handling section (auditor line 466) and referenced again in plan-item rules (line 409). | Both files agree the prefix exists; the reviewer correctly understands the convention. | None — convention is discoverable from both. |
| Reviewer process step 2 (line 42): "When the auditor is invoked with `scope = naming`, Duplicates / Consolidation / DevOps alignment / Usage sections are rendered with body 'Skipped (scope = naming)'" | Auditor's `scope = naming` body convention is documented at line 209 as a single statement; the per-section finding subsections (auditor lines 246–289 for external, 362–376 for internal) do NOT each repeat the literal `Skipped (scope = naming).` body. | The reviewer's expectation matches the spirit but expects a per-section literal that the auditor only states once in aggregate. Low-impact. | Optional clarification: at each section affected by `scope = naming` (Duplicates / Consolidation / DevOps alignment external & internal), append a one-line "When `scope = naming`, body is `Skipped (scope = naming).`" reminder so the reviewer's literal cross-reference test passes mechanically. |

## Scope-boundary findings

| Boundary violated (quote + line + file) | Why it's a violation | Proposed fix |
|---|---|---|
| Reviewer Process step 3 (line 44): "Glob `*/action.yml` and read 5–10 representative ones (prefer a mix: a Tier 1 primitive, a Tier 2 git-native action, a Tier 3 GitHub-specific action, **at least one composite if any exist**, **at least one that looks like it might mix concerns**). For each, ask: would the auditor's rules, applied literally, produce findings that match reality?" | This step asks the reviewer to read 5–10 `action.yml` files and judge whether the auditor's rules would produce sensible findings. That is a sample read of the actions — but the reviewer's own Rules at line 185 say "Do not audit the actions themselves. Finding an action that violates a rule is NOT a finding for this review." The line between "checking the rule against the action" and "auditing the action" is thin and may be interpreted differently by different runs. | Tighten the wording of Process step 3 to make explicit that the reviewer reads action.yml only to **stress-test the rule** (the rule fails if it produces a confidently-wrong finding when applied literally), and that any finding generated against the action belongs in the **rule's** finding sheet (e.g. "rule X is over-eager — when applied literally to action Y it produces a false positive"), never as an independent action-level finding. The current text is mostly there but not quite watertight. Debatable. |

Other scope boundaries hold:

- **Auditor read-only posture** — process steps 1–8 all read or compute; only the Output / file-write step writes. Auditor line 9 declares read-only on action.yml, line 75 on consumer repos, lines 475–479 list writable files. No process step requires a write outside the report/plan files.
- **Reviewer read-only-except-report** — reviewer line 184 declares the boundary; line 191 adds an `Edit` belt-and-braces rule before any edit. Self-improvement policy is the only source of non-report writes, and it is bounded.
- **Filename patterns** — `*-audit-actions*.md` vs `*-review-actions-auditor.md` are mutually exclusive (no overlap when matched against either pattern as a regex on `audit-actions` vs `review-actions-auditor`). No collision risk.
- **Responsibility split** — auditor audits actions against rules; reviewer audits the rules. This consistency-audit agent is a third tier (audits the rule-auditor pair). No overlap, no orphan.

## Terminology findings

Grouped by concept.

| Concept | Variant A | Variant B | Proposed canonical form |
|---|---|---|---|
| Single-concern action | "primitive" — used throughout rubric §5–§7 (e.g. line 467 "primitive", "single-concern action", "one concern per action — uses interchangeably"); auditor line 130 "primitives vs. composites". Rubric line 467 makes the equivalence explicit. | review-dimensions.md line 22 lists "'primitive' vs. 'single-concern action' vs. 'atomic action'" as a signal of inconsistency to flag — implying these three are NOT yet stated as equivalent in the auditor's Output schema. | The rubric DOES state the equivalence explicitly at line 467. The auditor's process and output sections inherit that equivalence transitively but do not repeat it. **Canonical:** the rubric's existing line-467 equivalence statement is sufficient — no additional rewrite needed. The reviewer's "atomic action" mention is forward-looking (a third synonym some authors use) and is fine to include in the open-ended pass without re-defining it. |
| Duplicate vs near-duplicate vs overlap | auditor line 124 "Duplicates. Find actions that do the same or nearly the same thing under different names." then "Compare behavior, not just names." then line 128 conservative two-condition test. | review-dimensions.md and the rest of the auditor use "duplicate" only — never "near-duplicate" or "overlap" as a separately-defined term. | Canonical: keep "duplicate" as the only term. The auditor's line-124 phrase "or nearly the same thing" is descriptive prose, not a separate category. No edit needed; flagged for awareness. |
| Consolidation vs merge | auditor line 126 "Consolidation. Find actions that could be merged into a single action with an input flag or mode." Lines 182, 287, 311 reuse "consolidation" as a category. Lines 21, 205, 287 use "merges" as a verb-form within the same concept. | auditor line 369 "two private scripts inside one action that could be merged" — uses "merged" inside the *internal* duplicates section, where a different concept (script-level merging, not action-level consolidation) is meant. | Canonical: keep both terms but **only at different abstraction levels** — "consolidation" for action-level merges (the auditor's category), "merge" for script-level merges inside an action (auditor line 369). The current text accidentally aligns with this pattern. Debatable; consider adding a one-line gloss at line 124 to make the distinction explicit. |
| `backwards_compatible` spelling | auditor uses `backwards_compatible` consistently (12 occurrences across input definition, output header, plan-item rules, and Backwards-compatibility-handling section). | review-dimensions.md line 21 uses `backwards_compatible`. Reviewer also uses `backwards_compatible`. Canonical form is uniform. | None. Spelling is consistent. (Listed here so the reader can see the check was performed.) |

## Exemption-propagation findings

| Exemption | Declared in | Honoured in / contradicted in | Proposed fix |
|---|---|---|---|
| `:latest` is load-bearing | Rubric §2 (devops-rubric.md line 291) | Reviewer Rules line 190 explicitly names "`:latest` as load-bearing" among the auditor's exemptions to respect. **Honoured.** | None. |
| Docker Compose stepping stone | Rubric §2 (devops-rubric.md line 288) | Reviewer Rules line 190 names "Docker Compose as stepping stone". **Honoured.** | None. |
| Author-determined environments | Rubric §2 (devops-rubric.md line 289) | Reviewer Rules line 190 names "author-determined environments". **Honoured.** | None. |
| Future-proofing for consolidations | Rubric §2 (devops-rubric.md line 290) | Reviewer line 190 lists "future-proofing for consolidations" — **not a separate exemption**, it is forward-looking guidance for *how* consolidations should be designed. Listing it alongside the bona-fide exemptions slightly conflates "exemption" with "design hint". | Reviewer line 190: split this into two sub-clauses — "(a) the four bona-fide forward-looking exemptions: Docker Compose stepping stone, author-determined environments, `:latest` as load-bearing, teaching-clarity override; and (b) the future-proofing guidance for consolidations, which is a design hint, not an exemption." Debatable. |
| Teaching-clarity override | Rubric §2 (devops-rubric.md line 292), with cross-reference at devops-rubric.md line 22 ("see §2, teaching-clarity override — which is narrow"); auditor line 126 (Consolidation step) and 144 (tie-breaker block). | Reviewer Rules line 190 lists "teaching-clarity override" among honoured exemptions. **Honoured.** Note: the rubric makes clear (line 292) that "Mainstream consolidation always wins; this 'override' does not reverse that" — the override is **flag, don't block**. The auditor's Consolidation step (line 126) reads as "do not propose merges that flatten pedagogically-important distinctions" — which is the older, blocking phrasing. The newer line-144 tie-breaker corrects this ("real-world best practice wins; pedagogy-preserving split is demoted to Option 2"). The auditor's two paragraphs on teaching-clarity (lines 126 and 144) say slightly different things. | Auditor line 126 should be updated to defer to the line-144 tie-breaker, e.g. "Respect the **teaching-clarity override** (rubric §2 — flag, don't block); see step 8 tie-breaker for the resolution rule when override and best-practice consolidation conflict." Removes the older blocking-tone phrasing without losing the cross-reference. |
| Publication intent = internal-only | Auditor lines 32–38 | Reviewer line 190 names "the auditor's declared publication intent at `actions-auditor.md:26–32`" — see Reference-integrity finding above; line range partly off. **Honoured in spirit; citation is off by ~6 lines.** | Update reviewer line 190 to `actions-auditor.md:32–38`. |
| Mainstream-first principle's specifics — `check-*` over `has-*` (mainstream default), `get-*` acceptable, `repository` over `repo`, `github` prefix only for ambiguous Tier 3 | Rubric "Mainstream-first principle" (devops-rubric.md lines 11–22), §3.1 (line 314) for `github`-prefix ambiguity test, §4 line 440 for `check-*` aliasing, §1 line 33 for `repository` vs `repo`. | Reviewer dimension 2b (review-dimensions.md lines 53–55) lists the same four specifics. **Consistent, no drift.** | None. |
| Reviewer exemption the auditor is unaware of | Reviewer line 190 lists *only* exemptions the auditor declared. No reviewer-side novel exemptions. | None. | None. |

## Tool / capability findings

| Agent | Tool missing / unused | Evidence | Proposed fix |
|---|---|---|---|
| auditor | `Edit` is not in `tools:` (line 4 lists `Read, Glob, Grep, Bash, Write`). | Auditor's process is read-only on subjects and writes via `Write` to new files; reconciliation of prior plans (lines 444–453) edits prior plan files in place — *which would require `Edit`, not just `Write`*. The auditor's Rules at line 477 say "Prior `plans/*-audit-actions*.md` files, edited ONLY per the Prior-plan handling section (mark items done, strike obsolete items, add status sub-bullets — never delete items outright)." A `Write` tool would overwrite the file wholesale; the reconciliation language ("update the item in-place from `- [ ]` to `- [x]`", "leave the `- [ ]` and prepend `~~` / append `~~`") is in-place editing. | Add `Edit` to the auditor's `tools:` frontmatter (line 4). The current setup either (a) implicitly relies on `Write` to overwrite the entire prior plan file (risky — could clobber unrelated content), or (b) silently lacks the capability. Alternatively, restate the reconciliation as "rewrite the prior plan file from scratch with the reconciled content" and keep `Write`-only — but `Edit` is the cheaper, safer move. **Recommended: add `Edit`.** |
| reviewer | `WebFetch` and `WebSearch` are listed in `tools:` (line 4) but are gated by `depth = deep` only (line 22). | Reviewer line 22: "`deep`: all of the above, plus cite primary sources … Use WebSearch / WebFetch where citations are thin." This is the only invocation. At `standard` (line 21): "Web lookups allowed but not required." Both tools are appropriately scoped. **No fix needed**, but worth noting for future audits — if `deep` mode is rarely used, the reviewer is carrying tools with low utilisation. | None today. Revisit if a future review changes `depth = deep` to no longer require web lookups. |
| reviewer | `Edit` justification | Reviewer line 191 ("Before using `Edit` on any file, verify the target is one of: …") provides the belt-and-braces guard, and the self-improvement policy at lines 168–180 is the active justification. **Edit access remains justified.** | None. |
| auditor and reviewer | Neither agent instructs the caller to run a tool the agent doesn't have. Auditor's `gh api` invocations (lines 132–138) are run via `Bash`; reviewer's web lookups are run via `WebFetch`/`WebSearch`. **All capabilities present.** | None. |

## Shared-principle findings

| Principle | Treatment A | Treatment B | Drift | Proposed fix |
|---|---|---|---|---|
| Mainstream-first | Rubric "Mainstream-first principle" at devops-rubric.md lines 11–22 (full statement, with four concrete implications); auditor opening at line 13 (full restatement, all four implications); reviewer dimension 2b at review-dimensions.md lines 47–58 (full restatement, four bullets). | All three places say the same thing. **No drift.** | None — except the principle is restated in three places. If the rubric is the source of truth, the auditor and reviewer could shorten their restatements to a one-line cite ("see rubric Mainstream-first principle"). Optional. | None required. Debatable cleanup. |
| Teaching-clarity override | Rubric §2 line 292 ("flag, don't block; mainstream consolidation always wins"); auditor line 126 (older blocking phrasing — "do not propose merges that flatten pedagogically-important distinctions"); auditor line 144 (newer "real-world best practice wins" tie-breaker). | review-dimensions.md line 56 "Pedagogical conventions leaking outside their scope" — narrow guard. | The auditor's two paragraphs (lines 126 and 144) are inconsistent in tone — line 126 reads as a hard block, line 144 explicitly demotes the pedagogy split to "Option 2". The reviewer's narrow-guard language (line 56) aligns with line 144, not 126. | Authoritative source: rubric §2 line 292 (latest, and the rubric is the standard). Update auditor line 126 to align with the line-144 tie-breaker (see Exemption-propagation finding above). |

## Plan-file convention findings

| Issue | Quote A / Quote B | Proposed fix |
|---|---|---|
| `.reports/` vs `plans/` split asymmetry | Auditor lines 148–151: "the agent emits **two files**: a **report** file that captures all findings as a frozen snapshot, and a **plan** file that is just the actionable checklist (decision queue). The plan links to the report so readers can read the context without the plan itself bloating as findings accumulate." Lines 152–158 specify `.reports/<ts>-audit-actions-…md` plus `plans/<ts>-audit-actions-…md`. **Reviewer (line 159):** "Save the full report to: `plans/<YYYYMMDD-HHMMSS>-review-actions-auditor.md`" — single file under `plans/`, mixing findings and Recommended-edits queue. | The auditor explicitly defends the report/plan split (lines 148–151) on grounds of "as the author processes plan items, the plan file shrinks; the findings are reference material from the moment the audit ran and do NOT change as items are resolved. Keeping them in the plan file makes the plan increasingly stale and verbose." This rationale applies equally to the reviewer's output — its Recommended-edits section is a queue that will shrink as edits land, but the rest of the report (findings + per-dimension sections) is a frozen snapshot. **Recommended:** split the reviewer's output into `.reports/<ts>-review-actions-auditor.md` (frozen) + `plans/<ts>-review-actions-auditor.md` (Recommended-edits queue only). Mirror the auditor's pattern. Or, at minimum, add a one-line note in the reviewer that the asymmetry is deliberate and explain why (e.g. reviewer findings are smaller in volume; merging is acceptable). |
| Timestamp command | Auditor line 152: "use the current UTC timestamp from `date -u +%Y%m%d-%H%M%S`". Reviewer line 162: "Get it with `date -u +%Y%m%d-%H%M%S`." | **Consistent.** Both use the same command. No fix needed. |
| `plans/` lifecycle | Auditor line 412: "When a plan file has no open items, delete the plan file (the corresponding report stays — it is a historical snapshot). When `plans/` contains no files, delete the directory." Reviewer line 166: "Per project convention, items are removed from this file as they are executed, the file is deleted when empty, and the `plans/` directory is deleted when it contains no files." | **Consistent.** Both honour the root `CLAUDE.md` `plans/` rule. No fix needed. |
| Filename pattern collision | Auditor: `*-audit-actions-external.md` / `*-audit-actions-internal.md` (and legacy `*-audit-actions.md`). Reviewer: `*-review-actions-auditor.md`. | **No collision.** Patterns are mutually exclusive. No fix needed. |

## Additional findings

Grouped by theme.

### A1 — Reviewer's Process step 3 reads action.yml; auditor's Inventory step also reads action.yml. No issue, but the duplicated-read is a minor efficiency note.

| Issue | Quote | Why | Fix |
|---|---|---|---|
| Both agents read `*/action.yml` independently | Auditor process step 1 (line 81) "Glob `*/action.yml`. For each, read and capture: …" + Reviewer process step 3 (line 44) "Glob `*/action.yml` and read 5–10 representative ones …". | These two reads happen in different agent runs (auditor produces a report, reviewer reviews the rule), so there is no in-process duplication. But the reviewer could in principle read the auditor's most recent inventory section in `.reports/<ts>-audit-actions-external.md` and stress-test the rule against THAT, rather than re-globbing. | Optional optimisation: reviewer could prefer a recent auditor report's Inventory if one exists. Not required, low-value, debatable. |

### A2 — `actions-auditor.md` line 30 says default `mode = both`; the consistency-audit agent (this agent) was instructed to assume `depth = standard` similarly. Convention is consistent across all three agents — every agent declares its defaults explicitly. Not a finding; positive observation.

### A3 — Reviewer's "Self-edits" section (reviewer lines 134–142) has a structural assumption that may surprise readers: the same run that produces the review can ALSO modify the reviewer's own definition file or the dimensions doc. This is documented and bounded by lines 168–180 (Self-improvement policy) and the line-191 belt-and-braces guard, but the audit-trail is split across two files (the report and the edited-self file). Not a defect — flagged so the author is aware that a future "what was changed in this run?" question requires reading two files, not one.

| Issue | Quote | Why | Fix |
|---|---|---|---|
| Self-edits split the run's audit trail across two files | Reviewer lines 134–142 (Self-edits section); reviewer lines 168–180 (Self-improvement policy). | When the reviewer self-edits, the report records "what was changed" textually (in the Self-edits section) but the actual file diffs are in the edited file's git history. A reader replaying a run needs both. | Optional: include a `git diff` snippet inline in the Self-edits section, so the report is self-contained. Adds tokens. Debatable. |

### A4 — Out-of-scope-but-noticed (per the consistency agent's "Hard rules" — flag in passing only).

| Issue | Quote | Note |
|---|---|---|
| Rubric §3.6 "Tag vs commit-status mechanism" has no companion "Filing guide" entry in §8 (devops-rubric.md line 625). | Rubric §3.6 lines 376–419 (defines mechanism-mismatch finding). Rubric §8 lines 625–644 (filing guide table). | A reader following the rubric's filing guide will not find a row that says "where to file a §3.6 mechanism-mismatch finding". Falls under "DevOps alignment → Other" by default, but that is implicit. **Out of scope** for this audit (this is rubric-internal, not an auditor↔reviewer drift), noted in passing per the spec's open-ended pass. |

### A5 — Out-of-scope-but-noticed: rubric §1.9 (line 247, version currency) is a relatively new addition (v9, 2026-04-23) and the auditor's process step 7 (line 132) explicitly invokes it as the "§1.9 marketplace-action version currency sub-pass". This integration is clean. The reviewer does not yet have a corresponding awareness — review-dimensions.md does not mention §1.9 or version currency. Whether the reviewer SHOULD mention it is debatable (the reviewer's job is to audit the rubric, not to enumerate every rule); however, if a future rubric rule is similarly new and the reviewer has nothing to say about it, that is a silent gap. **Flagged for awareness.** Not a finding of absence (per the spec's "Do NOT invent rules either agent doesn't have" rule).

## Recommended edits

Consolidated, ordered by impact.

| # | File + location | Change | Resolves |
|---|---|---|---|
| 1 | `actions-auditor-reviewer.md` line 159 (Report file path); add a `.reports/` mirror per the auditor pattern | Split the reviewer's output into `.reports/<ts>-review-actions-auditor.md` (frozen findings) + `plans/<ts>-review-actions-auditor.md` (Recommended-edits queue only). Update the "Report file" section to specify the two paths, mirroring auditor lines 148–158. Plan file shrinks as edits land; report stays frozen. | Plan-file finding #1 (split asymmetry). |
| 2 | `actions-auditor.md` line 4 (`tools:` frontmatter) | Add `Edit` to the auditor's `tools:` list. Current: `Read, Glob, Grep, Bash, Write`. Proposed: `Read, Edit, Glob, Grep, Bash, Write`. | Tool/capability finding #1 (the Prior-plan reconciliation at lines 444–453 requires in-place edits, which `Write` cannot safely do). |
| 3 | `actions-auditor-reviewer.md` line 190 | Update the citation `actions-auditor.md:26–32` to `actions-auditor.md:32–38`. | Reference-integrity finding #1; Exemption-propagation finding (publication intent). |
| 4 | `actions-auditor.md` line 126 (Consolidation step's teaching-clarity-override sentence) | Replace the older blocking phrasing ("Respect the teaching-clarity override (rubric §2) — do not propose merges that flatten pedagogically-important distinctions.") with: "Respect the teaching-clarity override (rubric §2 — flag, don't block); see step 8 tie-breaker (auditor line 144) for the resolution rule when override and best-practice consolidation conflict." | Shared-principle finding #2; Exemption-propagation finding (teaching-clarity override). |
| 5 | `actions-auditor.md` lines 246, 260, 263, 266 (and internal counterparts at 362, 369, 372, 374) | At each section affected by `scope = naming`, append a one-line literal: "When `scope = naming`, body is `Skipped (scope = naming).`" so the reviewer's per-section literal-string check passes mechanically. | Output-schema-contract finding #3 (scope-naming literal). |
| 6 | `actions-auditor.md` line 266 (DevOps alignment subsection list) AND line 374 (internal counterpart) | Extend the named subsection list to include first-class slots for **Version handling (§1.5)**, **Container build/tag/push (§1.6)**, **Tag vs commit-status mechanism (§3.6)**, and **Deploy contract (§4 deploy-*)**. Or, less invasively, extend rubric §8 with explicit filing-guide rows pointing each at "DevOps alignment → Other" and reference §8 from the auditor's Output schema. | Output-schema-contract finding #1 (rubric rules without dedicated finding slots). |
| 7 | `actions-auditor-reviewer.md` line 190 | Split the bullet listing exemptions into two sub-clauses: "(a) the four bona-fide forward-looking exemptions: Docker Compose stepping stone, author-determined environments, `:latest` as load-bearing, teaching-clarity override; and (b) the future-proofing guidance for consolidations, which is a design hint, not an exemption." | Exemption-propagation finding #4 (future-proofing-as-design-hint). |
| 8 | `actions-auditor-reviewer.md` line 44 (Process step 3) | Tighten the wording: action.yml reads are for **stress-testing the rule** only — any finding generated belongs in the rule's finding sheet ("rule X is over-eager") and never as an action-level finding. The current text is mostly there but the "would the auditor's rules, applied literally, produce findings that match reality?" phrasing leaves room for action-level findings to creep in. | Scope-boundary finding (single item). |
| 9 | `actions-auditor.md` line 124 | Add a one-line gloss distinguishing **action-level consolidation** (the audit category) from **script-level merging** (the smaller, internal concept used at line 369). Keeps both terms but makes the abstraction-level explicit. | Terminology finding #3 (consolidation vs merge). |
| 10 (debatable) | `devops-rubric.md` §8 (line 625, filing guide table) | Add a row for §3.6 mechanism-mismatch findings naming "DevOps alignment → Other" as the home. | Additional finding A4 (rubric §3.6 has no §8 row). Out of scope for this audit per "Hard rules" but noted in passing. Author's call. |

If the author accepts edits 1–9, the auditor/reviewer pair is mutually consistent. Edit 10 is rubric-internal and out of scope for this audit.
