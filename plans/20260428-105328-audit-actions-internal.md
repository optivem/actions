# Actions audit — Internal — 2026-04-28 10:53 UTC

**Report:** `.reports/20260428-105328-audit-actions-internal.md` — full internal-scope findings and per-item reasoning.

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

## Plan — Internal items

Items with no consumer-visible surface change. Safe to land independently of consumer updates. Ordered by impact within internal (call-site count descending is still the secondary signal — high-traffic actions still rank higher even when the change is internal).

- [ ] **1. Extract `shared/ghcr-probe.sh` into `check-ghcr-image-tag-exists` (conditional on External item 3 retention)** — replace the inline `fetch_registry_token` + `probe_manifest` pair in `check-ghcr-image-tag-exists/action.yml` with `source "$GITHUB_ACTION_PATH/../shared/ghcr-probe.sh"` and call `ghcr_bearer_for` + `ghcr_probe_manifest` instead. Removes ~120 lines of duplication; brings the third action in line with `check-ghcr-packages-exist` and `bump-patch-versions`. **Conditional:** if External item 3 (delete `check-ghcr-image-tag-exists`) is decided in favour of deletion, mark this item **obsolete** rather than executing.
  - Affects: `check-ghcr-image-tag-exists`
  - Consumers to update: none (internal-only — the action's `runs:` block is replaced, contract is unchanged)
  - Category: devops-alignment
