# Actions audit — External — 2026-04-28 10:53 UTC

🤖 **Picked up by agent** — `ValentinaLaptop` at `2026-04-28T11:39:16Z`

**Report:** `.reports/20260428-105328-audit-actions-external.md` — full findings, inventory, rejected alternatives, and per-item reasoning.

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

## Plan — Breaking items

Items that force consumer workflows to be updated. Complete these first and co-ordinate landing with consumer updates in sibling repos (`shop`, `gh-optivem`, `optivem-testing`). Ordered by **impact** — primarily call-site count descending, with DevOps-alignment/correctness findings promoted above pure naming findings at the same call-site count. Ties broken by affected-dir alphabetical order.

- [ ] **1. Rename `generate-release-notes` → `generate-prod-release-notes`** — directory rename + `name:` field update + every `uses:` ref update. The action hardcodes a `🚀 … PROD` title shape and prod-deployment notes body; the generic name is misleading. Same finding as the prior 2026-04-24 audit, re-emitted because still unfixed.
  - Affects: `generate-release-notes`
  - Consumers to update (12 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/_meta-prerelease-pipeline.yml`
    - `shop/.github/workflows/meta-release-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage-cloud.yml`
  - Category: naming

- [ ] **2. Fix `create-component-tags` portability + silent-swallow bugs** — replace the two `git ls-remote ... origin` invocations (lines 68, 81 of `action.yml`) with `git ls-remote --tags "$(remote_query_url ...)" ...`-shaped calls (matching the `bump-patch-versions` pattern), and drop the `2>/dev/null` redirect on `git push`. The action declares `repository:` and `git-host:` inputs but only honours them on the push path; the silent-swallow on push violates the no-swallow rule in `claude/CLAUDE.md`. Classified breaking because the `repository` / `git-host` contract surface changes for any future caller (zero active callers today, but the action is intentionally retained pending the courses migration documented in `actions/plans/20260424-101715-component-docker-tagging.md`).
  - Affects: `create-component-tags`
  - Consumers to update: none today (0 active call sites; out-of-tree references in `courses/01-pipeline/accelerator/course/05-production-stage/{03-release-version-tag.md, 04-multi-component.md, 05-multi-repository.md}`, `courses/01-pipeline/accelerator/course/03-acceptance-stage/08-multi-repository.md`, and `courses/01-pipeline/accelerator/course/08-architecture-reference/04-component-patterns.md` should be reviewed for accuracy alongside the fix but are owned by the courses agent's queue)
  - Category: devops-alignment

- [ ] **3. Delete `check-ghcr-image-tag-exists` (or refactor to source `shared/ghcr-probe.sh`)** — zero callers across active consumers. Description claims pairing with `bump-patch-versions`, but `bump-patch-versions/action.yml` resolves the GHCR-image signal directly via `shared/ghcr-probe.sh` rather than `uses:`-ing this action. Per rubric §1.1 the input shape is **speculative flexibility — DROP**. Recommended option: delete the action directory. Lower-effort alternative: retain and source `shared/ghcr-probe.sh` (becomes the partially-done internal helper-extraction item rather than a removal item).
  - Affects: `check-ghcr-image-tag-exists`
  - Consumers to update: none (0 active call sites)
  - Category: dead-code

- [ ] **4. Rename `cleanup-orphan-manifests` → `cleanup-ghcr-orphan-manifests`** — directory rename + `name:` field update + `uses:` ref update. Per rubric §3.1 ambiguity test, "manifests" is bare (Kubernetes / Docker / package / web app). The action operates exclusively on GHCR Docker manifests via the GitHub Packages API; sibling `check-ghcr-packages-exist` already uses the narrower `ghcr` segment.
  - Affects: `cleanup-orphan-manifests`
  - Consumers to update (1 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/cleanup.yml`
  - Category: naming

## Plan — Non-breaking items

None.
