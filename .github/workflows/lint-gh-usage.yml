name: lint-gh-usage

on:
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check gh-retry wrapper usage
        run: bash shared/_lint/check-no-raw-gh.sh
