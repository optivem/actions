# Actions audit report — Internal — 2026-04-22 13:25 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `.plans/20260422-132542-audit-actions-internal.md` — the actionable checklist derived from this report.

**External-scope companion:** `.reports/20260422-132542-audit-actions-external.md` (findings) + `.plans/20260422-132542-audit-actions-external.md` (queue). The external report holds the shared inventory and usage findings.

## Header

- `backwards_compatible`: **false** (breaking changes are in scope; this scope's items happen to all be behaviour-preserving for consumers).
- `scope`: **all**.
- `mode`: **both** — paired run; external-scope findings live in the companion file.
- **Publication intent:** internal-only.
- **Total actions audited:** 37 (see companion external report for inventory).
- **Date:** 2026-04-22
- **Rubric version:** v8
- **Prior-plan reconciliation:** prior internal plan completed and deleted in commit `8a55d26` (author/branding stripping + workflow-description tightening + `git-host` on `bump-patch-versions`). No carry-over items.

## Naming violations — Internal

- **`check-sha-on-branch` — `name:` field carries a word not in the directory name.** The `name:` field reads `'Check SHA Is On Branch'` but rubric §4 requires the `name:` field to be the Title Case of the directory name (`check-sha-on-branch` → `Check SHA On Branch`, with "SHA" retained all-caps as the conventional acronym style used by GitHub Actions Marketplace). The extra "Is" is a word mismatch. Prior audit (commit `1402912`) explicitly rejected renaming the directory because industry leans concise (`actions/checkout`, `docker/login-action`, `hashicorp/setup-terraform` all omit "Is"). The committed intent was to fix via the internal `name:` edit — that edit has NOT been applied. Tracked here to close the loop.
  - **Proposed fix (Recommended — best-practice):** edit `action.yml` line 1 from `name: 'Check SHA Is On Branch'` → `name: 'Check SHA On Branch'`. No consumer impact — the `name:` field is internal metadata, not consumed by callers (callers reference the action by directory path `optivem/actions/check-sha-on-branch`).
  - Alternative: retain the "Is" in `name:` as a readability-enhancing deliberate deviation and document it. Not recommended — the deviation is not meaningfully more readable than the strict Title Case form, and it creates a precedent for ad-hoc `name:` embellishment.

## Parameter findings — Internal

None.

## Duplicates — Internal

None.

## Consolidation opportunities — Internal

None.

## DevOps alignment findings — Internal

### Tool-agnostic composition

None.

### Other

None. All actions use `token:` as a declared input with `default: ${{ github.token }}` (rubric §1.4); all bridge the token via step-level `env:` rather than interpolating it into `run:` lines. External action pins are `@v4` / `@v5` on Marketplace-trusted publishers (`docker/login-action@v4` in `tag-docker-images`) per rubric §1.4 pin-strategy ladder tier 1. `author:` / `branding:` are absent consistently across all 37 actions (prior audit resolution). `shell: bash` throughout — no `pwsh` or `.ps1` outside the `shared/_test-*` allowlist. No `wait-for-endpoints` timeout gap here — that finding is classified External because the proposed fix adds a capped default that may clip a subset of previously-unbounded runs.

## Summary — Internal

- **Counts:** naming violations = 1; parameter findings = 0; duplicate clusters = 0; consolidation opportunities = 0; DevOps alignment findings = 0.
- **Top internal changes:**
  1. **`check-sha-on-branch.name:` field** — close the loop on the prior audit's explicitly-committed internal fix.
