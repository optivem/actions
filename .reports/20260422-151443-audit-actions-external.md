# Actions audit report — External — 2026-04-22 15:14 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `.plans/20260422-151443-audit-actions-external.md` — the actionable checklist derived from this report.

**Internal-scope companion:** `.reports/20260422-151443-audit-actions-internal.md` (findings) + `.plans/20260422-151443-audit-actions-internal.md` (queue).

## Header

- `backwards_compatible`: **false** — breaking changes are in scope; renames, removals, and merges are fair game. Re-run with `backwards_compatible=true` to restrict to non-breaking changes.
- `scope`: **all** — full audit — naming, parameter quality, duplicates, consolidation, DevOps alignment, and usage findings are all in scope.
- `mode`: **both** — paired run; internal-scope findings and their plan items are written to the companion internal file.
- **Publication intent:** internal-only. `branding:` absence and Marketplace-only input conventions are not flagged. Considered-but-rejected: (a) adding `branding:` fields — the rubric explicitly exempts them for internal-only; (b) adding Marketplace categorisation metadata — same reason.
- **Total actions audited:** 37
- **Date:** 2026-04-22
- **Rubric version:** v8
- **Prior-plan reconciliation:**
  - `.plans/20260422-132542-audit-actions-internal.md` — 1 open item reconciled: marked **done** (the `check-sha-on-branch` `name:` field edit has been applied in `action.yml` line 1 — verified at audit time). No still-valid items to carry over.
  - Prior external plan `.plans/20260422-132542-audit-actions-external.md` — already deleted (all items resolved); not re-examined in this run.
  - Carry-over: none. This audit produces no de-duplication skips.

## Inventory

| dir | name | #inputs | #outputs | one-line behavior | call sites (shop + gh-optivem + optivem-testing) |
|---|---|---:|---:|---|---:|
| bump-patch-versions | Bump Patch Versions | 4 | 3 | Computes patch-version bumps for VERSION files whose matching release tag exists on remote; writes nothing. Accepts `git-host`. | 2 + 0 + 0 |
| check-changes-since-tag | Check Changes Since Tag | 2 | 4 | Walks tag-patterns, diffs paths since newest matching tag; fail-open on no match. | 1 + 0 + 0 |
| check-commit-status-exists | Check Commit Status Exists | 5 | 2 | Fetches statuses on head-sha; success-match on context + description=commit-sha. Fail-open on transient API errors. `sha` input renamed to `commit-sha` in prior audit. | 0 + 1 + 0 |
| check-ghcr-packages-exist | Check GHCR Packages Exist | 2 | 1 | `gh api /packages?type=container`; returns `exist` (singular form — plural subject `packages`, grammatical exception recorded in prior audit). | 18 + 0 + 0 |
| check-sha-on-branch | Check SHA On Branch | 2 | 1 | `git fetch` + `git merge-base --is-ancestor`. `name:` field fixed to match dir in prior audit cycle. | 7 + 0 + 0 |
| check-tag-pattern-exists | Check Tag Pattern Exists | 4 | 1 | `git ls-remote --tags` filtered by pattern. | 0 + 1 + 0 |
| check-timestamp-newer | Check Timestamp Newer | 2 | 1 | ISO 8601 lexicographic compare with empty-baseline fail-open. | 18 + 0 + 0 |
| cleanup-github-deployments | Cleanup GitHub Deployments | 7 | 2 | `gh api` delete-with-retention loop; respects protected-envs + rate limit. | 1 + 0 + 0 |
| cleanup-github-prereleases | Cleanup GitHub Prereleases | 6 | 2 | Deletes superseded prerelease git tags, GH releases, and container tags. | 1 + 1 + 0 |
| commit-files | Commit Files | 4 | 2 | Contents-API PUT with SHA preconditions + retry on conflict. | 2 + 0 + 0 |
| compose-docker-image-urls | Compose Docker Image URLs | 2 | 1 | Pure `{base}:{tag}` join → JSON array. | 24 + 0 + 0 |
| compose-prerelease-status | Compose Prerelease Status | 3 | 1 | Appends `-<env>-<status>` to prerelease version → status-marker tag. | 18 + 0 + 0 |
| compose-prerelease-version | Compose Prerelease Version | 4 | 1 | Composes `v{base}-{suffix}.{build-number}` (+ optional prefix). SemVer §9 shape. | 19 + 1 + 0 |
| compose-release-version | Compose Release Version | 1 | 1 | Strips SemVer prerelease identifier `-<word>.<number>` → base version. | 24 + 1 + 0 |
| create-commit-status | Create Commit Status | 6 | 0 | `POST /repos/{repo}/statuses/{sha}` via `gh api`. | 0 + 1 + 0 |
| create-component-tags | Create Component Tags | 4 | 2 | For each `component:VERSION-path`, creates + pushes `{component}-v{version}`; idempotent. | 12 + 0 + 0 |
| deploy-docker-compose | Deploy Docker Compose | 5 | 1 | `docker compose up -d` with echo-only logging; `service-urls` hardcoded `'[]'` for contract-symmetry with future cloud-deploy backends. | 36 + 0 + 0 |
| format-artifact-list | Format Artifact List | 1 | 1 | Newline-separated input → `• <item>` bulleted markdown. | 7 + 0 + 0 |
| generate-release-notes | Generate Release Notes | 3 | 2 | Writes PROD release title + markdown notes to `mktemp`. | 12 + 0 + 0 |
| get-commit-status | Get Commit Status | 5 | 3 | `gh api /statuses` filtered by context (+ optional state). | 0 + 1 + 0 |
| get-last-successful-github-workflow-run-timestamp | Get Last Successful GitHub Workflow Run Timestamp | 3 | 1 | `gh run list` → newest `success` `createdAt`. | 18 + 0 + 0 |
| map-signoff-to-stage-result | Map Signoff To Stage Result | 1 | 1 | `approved` → `success`, else `failure`. | 6 + 0 + 0 |
| publish-tag | Publish Tag | 5 | 0 | `git tag` + `git push` idempotent-on-same-SHA; rejects mismatch. | 42 + 2 + 0 |
| read-base-version | Read Base Version | 1 | 1 | `head -n1` of VERSION file, trimmed. | 26 + 1 + 0 |
| render-stage-summary | Render Stage Summary | 5 | 0 | Writes stage-result-branching markdown to `$GITHUB_STEP_SUMMARY`. | 7 + 0 + 0 |
| render-system-stage-summary | Render System Stage Summary | 8 | 0 | Thin composite over `render-stage-summary` + `format-artifact-list`. | 48 + 0 + 0 |
| resolve-commit | Resolve Commit | 4 | 2 | Shallow-fetch; emits SHA + committer timestamp. | 0 + 1 + 0 |
| resolve-docker-image-digests | Resolve Docker Image Digests | 3 | 2 | Resolves tag URLs to digest URLs + `latest-updated-at` across the set. | 18 + 0 + 0 |
| resolve-latest-prerelease-tag | Resolve Latest Prerelease Tag | 5 | 2 | `git ls-remote` filtered by prefix (+ optional suffix), version-sort, tail. | 24 + 1 + 0 |
| resolve-latest-tag-from-sha | Resolve Latest Tag from SHA | 5 | 1 | `git ls-remote` → match by commit SHA → glob filter → version-sort → tail. | 1 + 2 + 0 |
| tag-docker-images | Tag Docker Images | 5 | 1 | `docker/login-action@v4` + loop of `docker buildx imagetools create` server-side retag. Output renamed to `tagged-image-urls` in prior audit. | 24 + 0 + 0 |
| trigger-and-wait-for-github-workflow | Trigger and Wait for GitHub Workflow | 7 | 1 | `gh workflow run` + discovery + `gh run watch --exit-status`; `timeout-seconds` capped at 1800. | 14 + 7 + 2 |
| validate-env-vars-defined | Validate Env Vars Defined | 1 | 0 | Checks each named env var is non-empty; errors with list of missing. | 55 + 2 + 1 |
| validate-tag-exists | Validate Tag Exists | 4 | 0 | `git ls-remote --tags` + grep; exit 1 if not found. | 42 + 2 + 0 |
| validate-version-unreleased | Validate Version Unreleased | 1 | 0 | `git tag -l` local check; exit 1 if found. | 19 + 1 + 0 |
| wait-for-endpoints | Wait for Endpoints | 6 | 0 | Polls HTTP endpoints with exp-backoff (cap 16×) until success, max attempts, or `timeout-seconds` deadline (default 900). | 87 + 0 + 0 |
| wait-for-github-workflow | Wait for GitHub Workflow | 8 | 1 | Discovery poll by head-sha → `gh run watch --exit-status`; `timeout-seconds` cap 1800. | 0 + 0 + 3 |

### Per-action detail

The summary table above has been cross-verified against each `action.yml` at audit time. Per-action input/output names are cited in the findings below when load-bearing; readers who need the full per-action dump can read `<dir>/action.yml` directly — the inventory reflects HEAD on 2026-04-22.

Deltas against the prior-audit inventory (same 37 actions):

- `check-commit-status-exists` — `sha` input → `commit-sha` (applied).
- `tag-docker-images` — output renamed `image-urls` → `tagged-image-urls` (applied).
- `wait-for-endpoints` — input count is now 6 (was 5); added `timeout-seconds` with default `'900'` (applied).
- `check-sha-on-branch` — `name:` field now `'Check SHA On Branch'` (matches dir); previously `'Check SHA Is On Branch'`.

## Usage findings

- Actions with **zero** call sites across `shop`, `gh-optivem`, and `optivem-testing`: **None.** Every action is referenced by at least one consumer workflow. Several actions have fewer than 3 call sites (`resolve-commit`, `get-commit-status`, `create-commit-status`, `check-commit-status-exists`, `check-tag-pattern-exists`), but all are under active use in the gh-optivem upstream-bookkeeping pattern; not candidates for removal.
- Inputs declared but never passed by any consumer: no obvious dead inputs. All `required: false` inputs correspond to standard config axes per rubric §1.1 KEEP classification (tokens, `git-host`, `retention-days`, `rate-limit-threshold`, `protected-environments`, `base-branch`, `ref`, etc.).
- Outputs declared but never read by any consumer: `deploy-docker-compose.service-urls` = hardcoded `'[]'` (documented zero-value placeholder kept for future-parity with a cloud-deploy action) — per rubric §1.2 this is observability/symmetry metadata; **KEEP** (unchanged from prior audit).

## Breaking changes

### Naming violations — External

**None.** All action *directory* names honour rubric §3 (naming tiers), §4 (verb honesty), and the `check-*` / `validate-*` / `get-*` / `compose-*` / `generate-*` / `resolve-*` / `tag-*` conventions established by prior audits. Each verb observed in the repo has been cross-checked against the rubric §4 canonical-semantics table.

**Examined and rejected** (naming that LOOKS debatable but does not produce a finding):

- `get-last-successful-github-workflow-run-timestamp` — 50-character directory name is long but every segment is load-bearing (per prior audit). Keep.
- `format-artifact-list` — prior audit rejected the `compose-*` rename; `format-*` is the honest verb for display formatting. Respected.
- `render-stage-summary` / `render-system-stage-summary` — `render-*` is acceptable non-canonical verb per §4 "the prefix set is not closed"; distinct from `compose-*` / `generate-*`.
- `map-signoff-to-stage-result` — `map-*` is the honest verb for lookup-table transform.
- `deploy-docker-compose` — §2 exemption (Docker Compose stepping stone).
- `check-ghcr-packages-exist` — `exist` output spelling preserved as an English grammatical exception with a plural subject (rejected by the prior audit's post-review status note). Unchanged.
- `resolve-*` / `compose-*` / `get-*` verbs all match §4 canonical semantics (read-only, pure, side-effect-free). No verb-vs-runs mismatches.

### Parameter findings — External (breaking)

**None.**

Every parameter name, `required:` declaration, default, and description was cross-checked against rubric §1 (mainstream convention, commit-SHA qualification, version-handling SemVer vocabulary), §1.1 / §1.2 (dead-input / dead-output classification), §3 (tier naming), and §4 (verb honesty). Specific checks:

- `commit-sha` vs `sha` — all flat-namespace SHA inputs use `commit-sha` (the prior audit's `check-commit-status-exists.sha` → `commit-sha` rename is in place; `check-sha-on-branch.commit-sha`, `get-commit-status.commit-sha`, `publish-tag.commit-sha`, `resolve-docker-image-digests.commit-sha`, `resolve-latest-tag-from-sha.commit-sha`, `wait-for-github-workflow.commit-sha` all conform). Outputs: `resolve-commit.sha` and `commit-files.commits[].commit-sha` — the former is a bare `sha` on a producer whose name (`resolve-commit`) already disambiguates, which is explicitly acceptable per rubric §1 "Commit SHA outputs".
- `repository` vs `repo` — all action inputs exposed to consumers use `repository`; internal env var aliases (`REPO`, `REPOSITORY`) are an implementation detail. No mixed-convention drift.
- SemVer vocabulary (§1.5) — `compose-prerelease-version` uses `base-version`, `suffix`, `build-number`, `prefix` as mandated; `read-base-version` emits `base-version`; `compose-release-version` consumes `prerelease-version`. All shapes conform.
- `required:` explicitness — every input declares `required: true|false` explicitly. `required-implicit` rule not violated by any input across all 37 actions.
- `default:` + `required:` combinations — every `required: false` has a declared `default:` (or a documented empty-string semantic, e.g. `commit-sha: ''` on `publish-tag`). No `required-default-contradiction` findings.
- `description:` — all inputs and outputs have non-empty, shape-conveying descriptions. No `description-missing`, `description-tautological`, or `description-too-terse` findings that survived the directed read.
- `default-placeholder` — no placeholder defaults (`TODO`, `example`, `change-me`); all defaults are either environment expressions (`${{ github.token }}`, `${{ github.repository }}`, `${{ github.sha }}`), literal values chosen for safety (`30` days retention, `50` rate-limit threshold, `900`/`1800` seconds timeouts, `3` keep-count, `'main'` base-branch), or documented empty strings.
- `output-name-verb-led` — all outputs are noun-based (`digest`, `tag`, `base-tag`, `bumps`, `exists`, `exist`, `commits`, `deleted-count`, `dry-run-count`, `changed`, `newer`, `timestamp`, `base-version`, `stage-result`, `title`, `notes-file`, `formatted`, `status-tag`, `version`, `image-urls`, `image-digest-urls`, `latest-updated-at`, `tagged-image-urls`, `run-id`, `on-branch`, `created-at`, `created-tags`, `skipped-tags`, `baseline-tag`, `baseline-sha`, `changed-files`, `target-url`, `state`, `description`, `summary`, `bumped`, `committed`, `service-urls`).
- `type-shape-ambiguous` — every input whose type is not obvious from the name (`version-files`, `tag-patterns`, `paths`, `tag-prefix`, `tag-suffix`, `pattern`, `names`, `endpoints`, `image-urls`, `base-image-urls`, `workflow-inputs`, `files`, `components`, `artifact-urls`, `success-artifact-ids`, `latest-artifact-ids`) describes its shape explicitly (JSON array / newline-separated / `path:tag-prefix` format, etc.).

### Duplicates — External

**None.**

**Examined and rejected** (carried forward from prior audit, re-checked against current state):

- `check-commit-status-exists` vs `get-commit-status` — different contracts (boolean-existence with fail-open vs read-value-or-fail). Reaffirmed.
- `check-tag-pattern-exists` vs `validate-tag-exists` — different contracts (boolean output vs assert-or-fail per rubric §4). Reaffirmed.
- `resolve-latest-prerelease-tag` vs `resolve-latest-tag-from-sha` — different selectors (prefix+suffix vs commit SHA). Reaffirmed.
- `wait-for-github-workflow` vs `trigger-and-wait-for-github-workflow` — different entry points (discovery-by-SHA vs dispatch-then-watch). Reaffirmed.
- `cleanup-github-deployments` vs `cleanup-github-prereleases` — different resource classes + rate-limit concerns. Reaffirmed.
- `check-ghcr-packages-exist` vs `check-tag-pattern-exists` — different target (Packages API vs git). Reaffirmed.

### Consolidation opportunities — External

**None.**

**Examined and rejected** (carried forward from prior audit, re-checked):

- Unified `check-*-exists` boolean-query action — three distinct backends (Packages / git / commit-status), no single atomic source per §6.3. Keep separate.
- `render-stage-summary` + `render-system-stage-summary` flatten — system-variant adds material logic (validation + artifact-list composition + skipped-reason annotation). Passes §1.8 "material logic" test. Keep as thin composite.
- Split `resolve-docker-image-digests` into two primitives — both outputs are projections of the same registry query; splitting would 2× API calls with no swappability gain (rubric §6.3). Keep merged.

### DevOps alignment findings — External (breaking)

**None.**

Every rubric §1 / §5 / §6 / §7 dimension was walked against every action. Summary of the directed checks:

- **Tool-agnostic composition** — all Tier 2 actions (`publish-tag`, `create-component-tags`, `bump-patch-versions`, `check-tag-pattern-exists`, `validate-tag-exists`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`) accept `git-host:` with a `github.com` default (prior audit resolution). No hardcoded `github.com` in Tier 2 action internals. `publish-tag` and `create-component-tags` use the caller's `origin` alias for pre-push read probes (acceptable — the caller's checkout defines `origin`; `git-host` configures push target).
- **Prefer VCS over platform API** — all Tier 2 concepts use `git ls-remote` / `git fetch` / `git tag`; Tier 3 actions correctly use `gh api` / `gh run` / `gh workflow`.
- **Separation of concerns (§6)** — no action spans >1 concern from the §6 table. `tag-docker-images` (registry-retag) is the artifact-release-tagging concern alone; `publish-tag` is git-tag-creation alone; `commit-files` is commit-of-generated-files alone; `compose-prerelease-version` is version-source alone; etc. No concern-mixing violations.
- **Composite opacity** — `render-system-stage-summary` is the only non-trivial composite and its steps are all externally callable (`render-stage-summary`, `format-artifact-list`).
- **Composition ordering (§7.1)** — no composites bundle ordered side effects; each action is a single side-effect class.
- **Idempotence (§7.2)** — `publish-tag` is idempotent-on-same-SHA; `create-component-tags` tolerates concurrent creation; `commit-files` retries on SHA-precondition conflicts; `cleanup-*` actions respect retention windows.
- **Fail-fast on cheap preconditions** — `compose-prerelease-version` validates base-version format before composing; `check-sha-on-branch` validates non-empty inputs; `compose-prerelease-status` validates status against allowed set.
- **Fast-feedback sizing (§1.3)** — all three blocker actions (`wait-for-endpoints`, `wait-for-github-workflow`, `trigger-and-wait-for-github-workflow`) expose `timeout-seconds` with a default aligned with the fast-feedback budget (900 / 1800 / 1800 seconds respectively). The prior audit's `wait-for-endpoints` finding is resolved.
- **Rate-limit awareness** — `cleanup-github-deployments`, `cleanup-github-prereleases`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow` all expose `rate-limit-threshold:`; the shared `gh_retry` script bounds retries at 4 attempts with 5 → 15 → 45s backoff (§1.3 bounded-retry rule).
- **Bounded retry with backoff** — `wait-for-endpoints` caps exponential backoff at `MAX_BACKOFF = WAIT_SECONDS * 16` AND now caps total wait-time at `timeout-seconds` (prior audit resolution); `gh_retry` is bounded; `commit-files` caps at `max-retries`; no unbounded `while true` loops observed.
- **Secrets / auth (§1.4)** — every action that needs a token declares it as a named `token:` input with `default: ${{ github.token }}`. Shell-bridge pattern (`env: GH_TOKEN: ${{ inputs.token }}` → `run:` consumes `$GH_TOKEN`) is used consistently; no `${{ inputs.token }}` interpolated into `run:` lines anywhere in the repo. `tag-docker-images` passes the token to `docker/login-action@v4` via `with: password:` — that is a `with:` block on a Marketplace action, not a shell-level interpolation, so the expression is rendered outside any `run:` command string and is not a §1.4 leak surface.
- **Supply chain (§1.4 pin-strategy ladder)** — the only external `uses:` in the repo is `docker/login-action@v4` inside `tag-docker-images`. `docker/*` is a Marketplace-trusted tier-1 publisher; `@v4` is the current major tag; acceptable per the ladder. No mutable refs (`@main`, `@master`), no untrusted-publisher SHAs.
- **Observability — dual surface** — `publish-tag`, `create-commit-status`, `get-commit-status`, `resolve-commit`, `render-stage-summary`, `render-system-stage-summary`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow` all emit useful log lines and/or `$GITHUB_STEP_SUMMARY` entries; `check-commit-status-exists` fails-open to `exists=false` with a warning annotation rather than exit-1 (correct per §1 observability).
- **Shell portability** — `shell: bash` throughout. No `shell: pwsh` or `.ps1` outside `shared/_test-*` (verified via sweep). The `shared/_lint/check-no-pwsh.sh` guardrail is in place.
- **Docker build/tag/push (§1.6)** — the repo does NOT ship a hand-rolled `build-docker-image` / `tag-docker-image` / `push-docker-image` trio; `tag-docker-images` specifically performs server-side retag of *already-pushed* images via `docker buildx imagetools create` (one manifest-level operation, no image-data transfer). This is not the build+tag+push concern §1.6 targets — it is a post-build retag of an existing image, which has no Marketplace-native equivalent in a single action. Passes §1.6.
- **Thin wrappers (§1.8)** — no wrappers around `actions/setup-*`, `softprops/action-gh-release`, or `google-github-actions/deploy-cloudrun` exist in this repo (`create-github-release` was removed in a prior cycle). All remaining wrappers pass the "material logic" test (retry / rate-limit / cross-host / idempotency-on-same-SHA / race-safe writes).
- **Version handling (§1.5)** — `compose-prerelease-version` emits the canonical `v{base}-{suffix}.{build-number}` shape with fail-fast base-version regex validation; `compose-release-version` strips the `-<word>.<number>` identifier per SemVer §9; `resolve-latest-prerelease-tag` / `resolve-latest-tag-from-sha` order with `sort -V` (SemVer-aware). No calendar-versioning / build-metadata mis-use.

## Non-breaking changes

### Parameter findings — External (non-breaking)

**None.**

### DevOps alignment findings — External (non-breaking)

**None.**

## Summary — External

- **Counts — breaking:** naming violations = 0; parameter findings = 0; duplicate clusters = 0; consolidation opportunities = 0; DevOps alignment findings = 0.
- **Counts — non-breaking:** parameter findings = 0; DevOps alignment findings = 0.
- **Top external changes:** none. The prior audit's three external items (`check-commit-status-exists.sha` rename, `tag-docker-images.image-urls` output rename, `wait-for-endpoints` `timeout-seconds` cap) have all been applied; the repo passes rubric v8 with no external-scope action items this cycle.

## Additional findings (rubric-facing)

None. The mainstream-first principle at the top of the rubric has been internalised; no finding this run proposes a rename that would push the repo toward a private dialect. `check-*` verbs are retained as the mainstream default for boolean-return queries; `get-*` remains the mainstream read verb; `repository:` is the mainstream input name throughout; `github`/`ghcr` prefixes are applied only where the bare noun would collide in generic English per §3.1's ambiguity test.
