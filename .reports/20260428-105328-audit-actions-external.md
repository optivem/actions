# Actions audit report — External — 2026-04-28 10:53 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `plans/20260428-105328-audit-actions-external.md` — the actionable checklist derived from this report.
Internal-scope companion: `.reports/20260428-105328-audit-actions-internal.md` (findings) + `plans/20260428-105328-audit-actions-internal.md` (queue).

## Header
- `backwards_compatible`: false — breaking changes are in scope — renames, removals, and merges are fair game. Re-run with `backwards_compatible=true` to restrict to non-breaking changes.
- `scope`: all — full audit — naming, parameter quality, duplicates, consolidation, DevOps alignment, and usage findings are all in scope.
- `mode`: both — paired run — internal-scope findings and their plan items are written to the companion internal file.
- **Publication intent:** internal-only. Therefore: `branding:` is not required; Marketplace-specific conventions beyond rubric-for-internal-clarity are not applied.
- **Total actions audited:** 40
- **Date:** 2026-04-28

**Prior-plan reconciliation.** Enumerated prior audit artifacts:

- `.reports/20260423-170825-audit-actions-external.md` — frozen, read-only.
- `.reports/20260423-170825-audit-actions-internal.md` — frozen, read-only.
- `.reports/20260424-124513-audit-actions-external.md` — frozen, read-only (the prior run's reconciliation already covered the 2026-04-23 reports).
- `.reports/20260424-124513-audit-actions-internal.md` — frozen, read-only.
- `plans/*-audit-actions*.md` — **none on disk.** The 2026-04-24 paired run noted that no plan files were ever written to `plans/` (the `plans/` directory itself did not exist when the 2026-04-24 reports were emitted, despite the report headers referencing plan files). No plan files to reconcile in-place this run.

For completeness, cross-checked the 2026-04-24 external report's three actionable recommendations against current repo state:

1. Prior #1 — Rename `generate-release-notes` → `generate-prod-release-notes`. Status: **still valid.** The action remains named `generate-release-notes`; the description still hardcodes "Production-deployment releases only" + the `🚀 … PROD` title shape. This run re-emits the finding and adds the corresponding plan item. (No prior plan file to mark done.)
2. Prior #2 — `get-commit-status` return-empty-on-miss behaviour change. Status: **done — verified 2026-04-28.** The current `runs:` block writes empty outputs and `exit 0` when no matching status is found; the description states "Returns empty outputs if no matching status is found." Not re-emitted.
3. Prior #3 — Relax `deploy-docker-compose.environment` + `.version` from `required: true` → `required: false` with `default: ''`. Status: **author re-doubled-down.** Both inputs are still `required: true`; the descriptions now explicitly read "Required — every run must announce its target/the version being deployed." This is an active authorial counter-signal to the prior recommendation rather than an oversight. **Challenges-to-prior-plan flag** (chat return only): the prior recommendation no longer reflects the author's intent for this action's contract — propose retiring that prior item in favour of capturing the "display-only-but-mandatory" decision in the action's own description (which is already done). Not re-emitted.

Prior 2026-04-24 internal report's one actionable recommendation:

1. Prior internal #1 — Extract `shared/ghcr-probe.sh` across `check-ghcr-image-tag-exists`, `check-ghcr-packages-exist`, `bump-patch-versions`. Status: **partially done — 2 of 3 callers migrated.** `shared/ghcr-probe.sh` exists; `check-ghcr-packages-exist/action.yml` and `bump-patch-versions/action.yml` both `source` it. `check-ghcr-image-tag-exists/action.yml` still inlines the `fetch_registry_token` + `probe_manifest` pair. The remaining migration is re-emitted as an internal plan item (see internal report) and is partially overlapping with the external dead-action finding for `check-ghcr-image-tag-exists` below — if the action is removed, the helper-extraction becomes obsolete.

**Reconciliation counts (this run):** done=1 (prior external #2), still-valid=1 (prior external #1, re-emitted as new plan item), challenged=1 (prior external #3, see chat return), partially-done=1 (prior internal #1, residual re-emitted). No prior plan files exist on disk to mark in-place.

## Inventory

Summary table (one row per action; call-site counts from `grep optivem/actions/<dir>@` across `shop/.github/workflows/`, `gh-optivem/.github/workflows/`, `optivem-testing/.github/workflows/`; archived repos excluded per agent spec):

| dir | name | #inputs | #outputs | one-line behavior | call sites (shop + gh-optivem + optivem-testing) |
|---|---|---|---|---|---|
| `bump-patch-versions` | Bump Patch Versions | 3 | 3 | Per VERSION file (signal=git-tag or ghcr-image), probe for existing artifact at current version; if present, emit a patch bump. | 2 + 1 + 0 = 3 |
| `check-changes-since-tag` | Check Changes Since Tag | 2 | 4 | Walk tag globs in priority; first match baselines a `git diff SHA..HEAD` over pathspec. | 19 + 0 + 0 = 19 |
| `check-commit-status-exists` | Check Commit Status Exists | 5 | 2 | `repos/{r}/commits/{head-sha}/statuses` — success+context+description==commit-sha match → exists=true; indeterminate API failure → exit 1. | 0 + 1 + 0 = 1 |
| `check-ghcr-image-tag-exists` | Check GHCR Image Tag Exists | 2 | 2 | Batched per-(image, tag) OCI HEAD `/manifests/{tag}` probe; returns `results` (per-image) + `any-exist`. **Inline GHCR probe — does not source `shared/ghcr-probe.sh`.** | 0 + 0 + 0 = 0 |
| `check-ghcr-packages-exist` | Check GHCR Packages Exist | 3 | 2 | OCI HEAD `/manifests/{tag}` probe across newline image list (per-line tag override allowed); returns `exist` + per-line `results`. Sources `shared/ghcr-probe.sh`. | 19 + 0 + 0 = 19 |
| `check-sha-on-branch` | Check SHA On Branch | 2 | 1 | `git merge-base --is-ancestor SHA origin/base-branch`; distinguishes 0/1/other exit codes (no swallow). | 7 + 0 + 0 = 7 |
| `check-tag-exists` | Check Tag Exists | 4 | 1 | `git ls-remote --tags` on caller-supplied exact tag or glob pattern; returns `exists` boolean (non-asserting). | 20 + 2 + 0 = 22 |
| `check-timestamp-newer` | Check Timestamp Newer | 2 | 1 | Lexicographic ISO-8601-UTC-Z compare; empty `since` → newer=true fail-open. | 18 + 0 + 0 = 18 |
| `cleanup-deployments` | Cleanup Deployments | 6 | 2 | External shell script; per environment keep N newest, delete those beyond cap past retention, honour protected-environments globs. | 1 + 0 + 0 = 1 |
| `cleanup-orphan-manifests` | Cleanup Orphan Manifests | 6 | 2 | External shell script; deletes untagged GHCR Docker manifests older than retention window not referenced by any active tag. | 1 + 0 + 0 = 1 |
| `cleanup-prereleases` | Cleanup Prereleases | 6 | 2 | External shell script; prune stale prerelease git tags, GitHub releases, GHCR container tags per retention. | 1 + 1 + 0 = 2 |
| `commit-files` | Commit Files | 4 | 2 | Contents API PUT with SHA preconditions + exponential-backoff retry for race-safe writes. | 2 + 1 + 0 = 3 |
| `compose-docker-image-urls` | Compose Docker Image URLs | 2 | 1 | Pure string: emit JSON array of `{base}:{tag}`. | 24 + 0 + 0 = 24 |
| `compose-prerelease-status` | Compose Prerelease Status | 3 | 1 | Validate status ∈ {deployed,passed,failed,approved,rejected} and compose `{pv}-{env}-{status}` (a git tag, not a SemVer version). | 18 + 0 + 0 = 18 |
| `compose-prerelease-version` | Compose Prerelease Version | 4 | 1 | Fail-fast regex on base-version; emit `{prefix-}v{base}-{suffix}.{build-number}` (SemVer §9). | 19 + 1 + 0 = 20 |
| `compose-release-version` | Compose Release Version | 1 | 1 | Strip SemVer §9 pre-release identifier `-<word>.<number>`. | 24 + 1 + 0 = 25 |
| `compose-tags` | Compose Tags | 2 | 1 | Pure string: apply `{version}` template to each keyed version. | 12 + 0 + 0 = 12 |
| `create-commit-status` | Create Commit Status | 6 | 0 | POST `/repos/.../statuses/{sha}` with context/state/description/target-url. | 0 + 1 + 0 = 1 |
| `create-component-tags` | Create Component Tags | 4 | 2 | Per `{component-name:version-file-path}` line, read VERSION file and create+push `{component-name}-v{version}` tag; idempotent. | 0 + 0 + 0 = 0 (out-of-tree references in courses/ — see "Examined and rejected — usage" below) |
| `deploy-docker-compose` | Deploy Docker Compose | 6 | 0 | `docker compose up -d` from working directory; env+version inputs are display-only labels (logs only). | 38 + 0 + 0 = 38 |
| `evaluate-run-gate` | Evaluate Run Gate | 1 | 2 | First-match-wins over JSON array of `{when, reason}` skip conditions. | 7 + 0 + 0 = 7 |
| `format-artifact-list` | Format Artifact List | 1 | 1 | Newline-separated → bulleted markdown `• <item>`; also called internally by `render-system-stage-summary`. | 7 + 0 + 0 = 7 (plus 2 internal uses) |
| `generate-release-notes` | Generate Release Notes | 3 | 2 | Compose hardcoded "🚀 … PROD" title + markdown notes file under `mktemp` for prod-deployment releases. | 12 + 0 + 0 = 12 |
| `get-commit-status` | Get Commit Status | 5 | 3 | Fetch `/commits/{sha}/statuses`, filter by context (and optionally state); return empty outputs on no match (no hard-fail). | 0 + 1 + 0 = 1 |
| `get-last-workflow-run` | Get Last Workflow Run | 7 | 1 | `gh run list` filtered by status + conclusion (default completed/success), excluding the current run; returns first match's `createdAt`. | 18 + 0 + 0 = 18 |
| `publish-tag` | Publish Tag | 5 | 0 | Idempotent `git tag` + `git push`; tolerates concurrent same-SHA, fails hard on same-name-different-SHA. | 42 + 2 + 0 = 44 |
| `read-base-version` | Read Base Version | 1 | 1 | `head -n 1` of the VERSION file, trimmed. | 26 + 1 + 0 = 27 |
| `read-base-versions` | Read Base Versions | 2 | 1 | Batched keyed form of `read-base-version`; supports cross-repo via optional `repo` per entry (uses Contents API). | 12 + 0 + 0 = 12 |
| `render-stage-summary` | Render Stage Summary | 5 | 0 | Markdown block to `$GITHUB_STEP_SUMMARY`; validates stage-result ∈ {success,failure,cancelled,skipped}. | 7 + 0 + 0 = 7 (plus 1 internal use) |
| `render-system-stage-summary` | Render System Stage Summary | 9 | 0 | Thin composite: validate → `format-artifact-list` ×2 → `render-stage-summary` → `::notice` on skipped. | 48 + 0 + 0 = 48 |
| `resolve-commit` | Resolve Commit | 4 | 2 | Shallow-fetch ref into throwaway repo; return SHA + committer ISO-8601 timestamp. | 0 + 1 + 0 = 1 |
| `resolve-docker-image-digests` | Resolve Docker Image Digests | 2 | 2 | External shell script; per `base-image-url` + `tag` (default `latest`), resolve registry digest and aggregate newest pushed-at. | 19 + 0 + 0 = 19 |
| `resolve-latest-prerelease-tag` | Resolve Latest Prerelease Tag | 5 | 2 | `git ls-remote --tags` on `prefix*[suffix]`, `sort -V`, take highest; `base-tag` strips suffix. | 30 + 1 + 0 = 31 |
| `resolve-latest-tag-from-sha` | Resolve Latest Tag from SHA | 5 | 1 | `git ls-remote --tags` filtered by SHA and glob, `sort -V`, take highest. | 1 + 2 + 0 = 3 |
| `tag-docker-images` | Tag Docker Images | 6 | 1 | `docker buildx imagetools create` server-side re-tag. Two modes: broadcast (`image-urls + tag`) or map (`image-tags` with keyed entries). | 36 + 0 + 0 = 36 |
| `trigger-and-wait-for-workflow` | Trigger and Wait for Workflow | 8 | 1 | Rate-limit pre-check → `gh workflow run` → `gh run list` discovery (head-sha-disambiguated) → timeout-bounded `gh run watch --exit-status`. | 14 + 7 + 2 = 23 |
| `validate-env-vars-defined` | Validate Env Vars Defined | 1 | 0 | Error if any caller-supplied env-var name maps to an empty value. | 56 + 2 + 1 = 59 |
| `validate-tag-exists` | Validate Tag Exists | 4 | 0 | `git ls-remote --tags`; hard-fail if tag missing. | 42 + 2 + 0 = 44 |
| `wait-for-endpoints` | Wait for Endpoints | 6 | 0 | Exponential-backoff curl on each endpoint, capped + jittered; bounded by `max-attempts` and hard `timeout-seconds` deadline; dump Compose logs on failure. | 89 + 0 + 0 = 89 |
| `wait-for-workflow` | Wait for Workflow | 9 | 1 | Rate-limit pre-check → `gh run list` discovery by commit SHA → timeout-bounded `gh run watch --exit-status`. | 0 + 0 + 3 = 3 |

### Per-action detail

#### `bump-patch-versions`
- Inputs: `version-files*`, `repository`, `token`
- Outputs: `bumps`, `bumped`, `summary`
- Behavior: JSON array of `{path, signal, value}`; per file, probe remote (git tag via `git ls-remote`, OR GHCR via `shared/ghcr-probe.sh`'s `ghcr_bearer_for` + `ghcr_probe_manifest`); if present, compute the next patch bump. No writes — pairs with `commit-files`.
- Call sites: shop=2, gh-optivem=1, optivem-testing=0

#### `check-changes-since-tag`
- Inputs: `tag-patterns*`, `paths*`
- Outputs: `changed`, `baseline-tag`, `baseline-sha`, `changed-files`
- Behavior: Walks newline-separated tag globs in priority order; first pattern with a match yields the baseline tag (newest by `--sort=-version:refname`); `git diff baseline-sha..HEAD` over pathspec.
- Call sites: shop=19, gh-optivem=0, optivem-testing=0

#### `check-commit-status-exists`
- Inputs: `commit-sha*`, `status-context*`, `head-sha`, `repository`, `token`
- Outputs: `exists`, `created-at`
- Behavior: `repos/{r}/commits/{head-sha}/statuses` (paginated via `gh_retry`); match on success+context+description==commit-sha; indeterminate API failure → exit 1 (no swallow).
- Call sites: shop=0, gh-optivem=1, optivem-testing=0

#### `check-ghcr-image-tag-exists`
- Inputs: `images*`, `token`
- Outputs: `results`, `any-exist`
- Behavior: Per-entry OCI HEAD `/manifests/{tag}` probe after `GET /token?scope=repository:...:pull` exchange; hard-fails on 401/403; emits keyed boolean list + aggregate `any-exist`. **Inlines `fetch_registry_token` + `probe_manifest` rather than sourcing `shared/ghcr-probe.sh`.**
- Call sites: shop=0, gh-optivem=0, optivem-testing=0 — **zero callers across active consumers.** Description claims pairing with `bump-patch-versions`, but `bump-patch-versions/action.yml` resolves the GHCR-image signal via the shared helper directly, not by `uses:`-ing this action.

#### `check-ghcr-packages-exist`
- Inputs: `image-urls*`, `tag`, `token`
- Outputs: `exist`, `results`
- Behavior: Two usage shapes — preflight gate (URL-only lines + `tag` default `latest`) or per-image probe (mixed `image:tag` lines). Definitive only: 200 → true, 404 → false; 401/403/5xx/network → exit 1 (no swallow). Sources `shared/ghcr-probe.sh`.
- Call sites: shop=19, gh-optivem=0, optivem-testing=0

#### `check-sha-on-branch`
- Inputs: `commit-sha`, `base-branch`
- Outputs: `on-branch`
- Behavior: `git fetch origin base-branch --quiet`, then `git merge-base --is-ancestor`. Distinguishes exit codes 0 / 1 / other (other → exit 1, no swallow).
- Call sites: shop=7, gh-optivem=0, optivem-testing=0

#### `check-tag-exists`
- Inputs: `tag*`, `repository`, `token`, `git-host`
- Outputs: `exists`
- Behavior: `git ls-remote --tags "$remote" "refs/tags/$TAG"`; boolean return, non-asserting (never fails on missing tag). Sibling of `validate-tag-exists` (which asserts).
- Call sites: shop=20, gh-optivem=2, optivem-testing=0

#### `check-timestamp-newer`
- Inputs: `latest*`, `since`
- Outputs: `newer`
- Behavior: Lexicographic `>` on ISO-8601-UTC-Z strings; empty `since` → newer=true fail-open. (Inputs were renamed from `subject`/`baseline` to `latest`/`since` since the prior audit — already in use across 18 shop call sites.)
- Call sites: shop=18, gh-optivem=0, optivem-testing=0

#### `cleanup-deployments`
- Inputs: `keep-count`, `protected-environments`, `delete-delay-seconds`, `rate-limit-threshold`, `dry-run`, `token`
- Outputs: `deleted-count`, `dry-run-count`
- Behavior: External shell `cleanup-deployments.sh`; per-env keeps N newest, deletes those beyond cap past retention-days, honouring protected-environments globs.
- Call sites: shop=1, gh-optivem=0, optivem-testing=0

#### `cleanup-orphan-manifests`
- Inputs: `retention-days`, `container-packages*`, `delete-delay-seconds`, `rate-limit-threshold`, `dry-run`, `token`
- Outputs: `deleted-count`, `dry-run-count`
- Behavior: External shell `cleanup-orphan-manifests.sh`; deletes untagged GHCR Docker manifests older than retention window not referenced by any active tag.
- Call sites: shop=1, gh-optivem=0, optivem-testing=0

#### `cleanup-prereleases`
- Inputs: `retention-days`, `container-packages`, `delete-delay-seconds`, `rate-limit-threshold`, `dry-run`, `token`
- Outputs: `deleted-count`, `dry-run-count`
- Behavior: External shell `cleanup-prereleases.sh`; prunes stale prerelease git tags, GitHub releases, GHCR container tags.
- Call sites: shop=1, gh-optivem=1, optivem-testing=0

#### `commit-files`
- Inputs: `files*`, `branch*`, `max-retries`, `token`
- Outputs: `commits`, `committed`
- Behavior: For each `{path, content, message}`, PUT `/contents/{path}` with optimistic-concurrency SHA precondition; exponential backoff (2^attempt) on 409/422 up to `max-retries`.
- Call sites: shop=2, gh-optivem=1, optivem-testing=0

#### `compose-docker-image-urls`
- Inputs: `tag*`, `base-image-urls*`
- Outputs: `image-urls`
- Behavior: External shell script; emits JSON array of `{base}:{tag}`.
- Call sites: shop=24, gh-optivem=0, optivem-testing=0

#### `compose-prerelease-status`
- Inputs: `prerelease-version*`, `environment*`, `status*`
- Outputs: `status-tag`
- Behavior: Trim + validate status ∈ {deployed,passed,failed,approved,rejected}; compose `{pv}-{env}-{status}` (a git tag — past-participle verbs per rubric §3.6 tag-naming convention).
- Call sites: shop=18, gh-optivem=0, optivem-testing=0

#### `compose-prerelease-version`
- Inputs: `base-version*`, `suffix*`, `build-number*`, `prefix`
- Outputs: `version`
- Behavior: Fail-fast regex on base-version; emit `{prefix-}v{base}-{suffix}.{build-number}` (SemVer §9).
- Call sites: shop=19, gh-optivem=1, optivem-testing=0

#### `compose-release-version`
- Inputs: `prerelease-version*`
- Outputs: `version`
- Behavior: Strip SemVer §9 prerelease identifier `-<word>.<number>`; preserve `v` prefix; fail-fast if unchanged (input didn't match expected shape).
- Call sites: shop=24, gh-optivem=1, optivem-testing=0

#### `compose-tags`
- Inputs: `versions*`, `template`
- Outputs: `tags`
- Behavior: Substitute `{version}` in template across keyed JSON list. Validates array shape + template placeholder presence. `template` default is `v{version}`.
- Call sites: shop=12, gh-optivem=0, optivem-testing=0

#### `create-commit-status`
- Inputs: `commit-sha`, `context*`, `state`, `description`, `target-url`, `token`
- Outputs: (none)
- Behavior: POST `/repos/{r}/statuses/{sha}`; defaults `target-url` to the current run URL.
- Call sites: shop=0, gh-optivem=1, optivem-testing=0

#### `create-component-tags`
- Inputs: `components*`, `repository`, `git-host`, `token`
- Outputs: `created-tags`, `skipped-tags`
- Behavior: Per `{component-name:version-file-path}` line, read VERSION file and create+push `{component-name}-v{version}` tag using `github-actions[bot]`; idempotent (skips existing tags). **Mixed-portability bug:** `git ls-remote ... origin` (lines 68 and 81) uses `origin` for the queries, while `git push "$push_target"` (line 75) uses the parameterised target — the `git-host` and `repository` inputs are accepted but only honoured on the push path, not the query path. **Silent error swallow:** `git push "$push_target" "$component_tag" 2>/dev/null` discards the push diagnostic; failures fall through to the concurrent-creation reconciliation block without a recorded reason.
- Call sites: shop=0, gh-optivem=0, optivem-testing=0 — **zero callers across active consumers.** Out-of-tree references exist in `courses/01-pipeline/accelerator/course/05-production-stage/{03-release-version-tag.md, 04-multi-component.md, 05-multi-repository.md}`, `courses/01-pipeline/accelerator/course/03-acceptance-stage/08-multi-repository.md`, and `courses/01-pipeline/accelerator/course/08-architecture-reference/04-component-patterns.md`. The deferred-deletion plan `actions/plans/20260424-101715-component-docker-tagging.md` documents an intent to delete the action *after* the course pages are migrated. Treat as "intentionally retained pending course docs migration", not as a removal candidate per rubric §1.1 — but the portability bug and silent-swallow remain bugs that should be fixed if the action is to keep ticking until those course migrations complete.

#### `deploy-docker-compose`
- Inputs: `environment*`, `version*`, `image-urls*`, `service-names*`, `compose-file`, `working-directory*`
- Outputs: (none)
- Behavior: `docker compose up -d` (optionally `-f compose-file`) from `working-directory`. `environment` and `version` inputs are display-only labels surfaced in step logs. The `image-urls` + `service-names` pair is exported as `SYSTEM_IMAGE_<UPPER(name)>=<url>` env vars for compose substitution; counts must match.
- Call sites: shop=38, gh-optivem=0, optivem-testing=0

#### `evaluate-run-gate`
- Inputs: `skip-conditions*`
- Outputs: `should-run`, `skip-reason`
- Behavior: JSON array of `{when, reason}`; first `when=="true"` wins (`should-run=false`, `skip-reason=that.reason`).
- Call sites: shop=7, gh-optivem=0, optivem-testing=0

#### `format-artifact-list`
- Inputs: `artifacts`
- Outputs: `formatted`
- Behavior: Newline → bulleted `• <item>` markdown; empty input → empty output.
- Call sites: shop=7, gh-optivem=0, optivem-testing=0 (+ 2 internal uses in `render-system-stage-summary`)

#### `generate-release-notes`
- Inputs: `prerelease-version*`, `release-version*`, `artifact-urls`
- Outputs: `title`, `notes-file`
- Behavior: Compose hardcoded `🚀 {release-version} PROD` title + markdown notes file under `mktemp`. Embeds repo, commit, actor, run URL. Description explicitly says PROD-deploy only; callers needing QA/signoff variants must write a sibling.
- Call sites: shop=12, gh-optivem=0, optivem-testing=0

#### `get-commit-status`
- Inputs: `commit-sha*`, `context*`, `state`, `repository`, `token`
- Outputs: `description`, `state`, `target-url`
- Behavior: `repos/{r}/commits/{sha}/statuses`, filter by context (and optionally state). **Returns empty outputs on no match (no hard-fail).** Aligned with rubric §4 `get-*` semantics — fixed since the prior audit.
- Call sites: shop=0, gh-optivem=1, optivem-testing=0

#### `get-last-workflow-run`
- Inputs: `workflow-name*`, `repository`, `exclude-run-id`, `status`, `conclusion`, `limit`, `token`
- Outputs: `timestamp`
- Behavior: `gh run list --json databaseId,createdAt,status,conclusion`; first row matching `status` (default `completed`) and `conclusion` (default `success`), excluding `exclude-run-id` (default `github.run_id`); empty when no match.
- Call sites: shop=18, gh-optivem=0, optivem-testing=0

#### `publish-tag`
- Inputs: `tag*`, `commit-sha`, `repository`, `git-host`, `token`
- Outputs: (none)
- Behavior: Idempotent `git tag` + `git push`; tolerates concurrent same-SHA creation, fails hard on same-name-different-SHA. Sources `shared/remote-url.sh` + `shared/clear-persisted-credentials.sh`.
- Call sites: shop=42, gh-optivem=2, optivem-testing=0

#### `read-base-version`
- Inputs: `file`
- Outputs: `base-version`
- Behavior: `head -n 1 "$FILE" | tr -d '[:space:]'`. Default `file: 'VERSION'`.
- Call sites: shop=26, gh-optivem=1, optivem-testing=0

#### `read-base-versions`
- Inputs: `entries*`, `token`
- Outputs: `versions`
- Behavior: JSON `[{key, file, repo?}]` → JSON `[{key, version}]`; per-entry `head -n 1` + trim, fail on missing or empty file. Optional `repo` triggers cross-repo Contents-API fetch instead of local read.
- Call sites: shop=12, gh-optivem=0, optivem-testing=0

#### `render-stage-summary`
- Inputs: `stage-name*`, `stage-result*`, `stage-content`, `stage-success-content`, `stage-skipped-content`
- Outputs: (none)
- Behavior: Validate `stage-result` ∈ {success,failure,cancelled,skipped}; emit markdown block to `$GITHUB_STEP_SUMMARY` tailored to outcome.
- Call sites: shop=7, gh-optivem=0, optivem-testing=0 (+ 1 internal use in `render-system-stage-summary`)

#### `render-system-stage-summary`
- Inputs: `stage-name*`, `stage-result*`, `environment*`, `success-version`, `success-artifact-ids`, `skipped-reason`, `latest-artifact-ids`, `latest-updated-at`, `last-run-at`
- Outputs: (none)
- Behavior: Validate inputs → `format-artifact-list` ×2 → `render-stage-summary` with assembled markdown body → `::notice` annotation on skipped.
- Call sites: shop=48, gh-optivem=0, optivem-testing=0

#### `resolve-commit`
- Inputs: `repository*`, `ref`, `token`, `git-host`
- Outputs: `sha`, `timestamp`
- Behavior: Shallow-fetch ref into throwaway repo via `git init` + `git fetch --depth=1`; return `rev-parse FETCH_HEAD` + `git log -1 --format=%cI`.
- Call sites: shop=0, gh-optivem=1, optivem-testing=0

#### `resolve-docker-image-digests`
- Inputs: `base-image-urls*`, `tag`
- Outputs: `image-digest-urls`, `latest-updated-at`
- Behavior: External shell script; for each base URL + tag (default `latest`), resolve registry digest and aggregate newest pushed-at. (Input renamed from `commit-sha` to caller-owned `tag` since the prior audit — callers now own the tag convention.)
- Call sites: shop=19, gh-optivem=0, optivem-testing=0

#### `resolve-latest-prerelease-tag`
- Inputs: `tag-prefix*`, `tag-suffix`, `repository`, `token`, `git-host`
- Outputs: `tag`, `base-tag`
- Behavior: `git ls-remote --tags "$remote" "refs/tags/${PREFIX}*${SUFFIX}"`, strip peeled refs (`^{}`), `sort -V | tail -1`. `base-tag` strips suffix.
- Call sites: shop=30, gh-optivem=1, optivem-testing=0

#### `resolve-latest-tag-from-sha`
- Inputs: `repository`, `commit-sha*`, `pattern`, `token`, `git-host`
- Outputs: `tag`
- Behavior: `git ls-remote --tags`; match rows whose SHA == input (strip `^{}`), filter by caller glob, `sort -V`, take highest.
- Call sites: shop=1, gh-optivem=2, optivem-testing=0

#### `tag-docker-images`
- Inputs: `image-urls`, `tag`, `image-tags`, `registry`, `registry-username`, `token`
- Outputs: `tagged-image-urls`
- Behavior: `docker/login-action@v4`, then external shell using `docker buildx imagetools create` (server-side re-tag, no image data crosses runner). Two modes: broadcast (`image-urls + tag`) or map (`image-tags`).
- Call sites: shop=36, gh-optivem=0, optivem-testing=0

#### `trigger-and-wait-for-workflow`
- Inputs: `workflow*`, `repository`, `ref`, `workflow-inputs`, `poll-interval`, `rate-limit-threshold`, `timeout-seconds`, `token`
- Outputs: `run-id`
- Behavior: Rate-limit pre-check → `timeout 60 gh workflow run` → `gh run list` discovery (head-sha-disambiguated) → `timeout "$TIMEOUT_SECONDS" gh run watch --exit-status`.
- Call sites: shop=14, gh-optivem=7, optivem-testing=2

#### `validate-env-vars-defined`
- Inputs: `names*`
- Outputs: (none)
- Behavior: Fail the step listing any names whose env value is empty.
- Call sites: shop=56, gh-optivem=2, optivem-testing=1

#### `validate-tag-exists`
- Inputs: `tag*`, `repository`, `token`, `git-host`
- Outputs: (none)
- Behavior: `git ls-remote --tags`; grep match; hard-fail if absent.
- Call sites: shop=42, gh-optivem=2, optivem-testing=0

#### `wait-for-endpoints`
- Inputs: `endpoints*`, `compose-file`, `working-directory`, `max-attempts`, `wait-seconds`, `timeout-seconds`
- Outputs: (none)
- Behavior: Per endpoint, curl -f on exponential backoff (capped, jittered); bounded by `max-attempts` and hard `timeout-seconds` deadline; dumps Compose logs on failure. Default `timeout-seconds: 900`.
- Call sites: shop=89, gh-optivem=0, optivem-testing=0

#### `wait-for-workflow`
- Inputs: `workflow*`, `commit-sha*`, `repository`, `poll-interval`, `watch-interval`, `max-discovery-attempts`, `rate-limit-threshold`, `timeout-seconds`, `token`
- Outputs: `run-id`
- Behavior: Rate-limit pre-check loop → `gh run list` discovery by `headSha==commit-sha` → `timeout` + `gh run watch --exit-status`. Default `timeout-seconds: 1800`.
- Call sites: shop=0, gh-optivem=0, optivem-testing=3

## Usage findings

- **Actions with zero call sites across active consumers:**
  - `check-ghcr-image-tag-exists` — **removal / consolidation candidate.** Description claims pairing with `bump-patch-versions`, but `bump-patch-versions/action.yml` resolves the GHCR-image signal directly via `shared/ghcr-probe.sh` and the inline `probe_ghcr_image` helper, NOT by `uses:`-ing this action. The action's batched-keyed `[{key, image-url, tag}]` shape has no caller in tree. Per rubric §1.1, classify as **speculative flexibility — DROP**: the input shape was added for a hypothetical caller that materialised differently. (See External Breaking findings below.)
  - `create-component-tags` — **intentionally retained pending course migration.** The deferred-deletion plan `actions/plans/20260424-101715-component-docker-tagging.md` documents that the action was removed from all 12 prod-stage workflows in shop but kept in tree because three accelerator course pages teach it as working example code (`courses/01-pipeline/accelerator/course/05-production-stage/{03,04,05}*.md`, `08-architecture-reference/04-component-patterns.md`, `03-acceptance-stage/08-multi-repository.md`). Out-of-scope for removal under §1.1 — but the portability bug and silent-swallow flagged in External Breaking findings are still real bugs in the code as it stands.
- **Inputs declared but never passed by any consumer (sampled):** No new dead-input candidates this run. Prior audit's KEEP-classified inputs (cross-repo `repository`, `git-host`, `token`, registry-auth knobs, retention/dry-run toggles, etc.) all retain their §1.1 standard-config-axis classification.
- **Outputs declared but never read by any consumer (sampled):** No new dead-output candidates this run. Prior audit's KEEP-classified observability outputs (`baseline-sha`, `created-at`, `results`, `commits`, `committed`, `deleted-count`, `dry-run-count`, `target-url`, `run-id` on idle paths, etc.) all retain their §1.2 observability-metadata classification.

## Breaking changes

Items here force consumers to update synchronously with the action change. Complete these first, co-ordinated with consumer updates across `shop`, `gh-optivem`, and `optivem-testing`.

### Naming violations — External

| dir | rule violated | proposed name |
|---|---|---|
| `generate-release-notes` | rubric §4 `name-misleading` — the action's description states it "hardcodes the `🚀`/`PROD` title shape because every caller to date emits prod-deployed release notes", and internally composes a literal `"🚀 $RELEASE_VERSION PROD"` title. The generic name promises any release-notes variant; the implementation is specifically PROD-deployment. Same finding as the prior 2026-04-24 audit — re-emitted because still unfixed. | `generate-prod-release-notes` (preferred — honest about specialisation), OR parameterise the title prefix + stage label and keep the generic name (alternate). **Recommended: rename to `generate-prod-release-notes`.** Reason: the PROD hardcoding is structural (title shape, notes body assumptions, `Release Version: / Promoted From:` framing all assume a prerelease → release promotion); making it parameter-driven would require either (a) a richer title template input plus body-section toggles — significant input surface growth — or (b) a separate sibling anyway for non-PROD variants, since the current name would then read awkwardly against a full template input. Consumer churn: 12 call sites in shop. |
| `cleanup-orphan-manifests` | rubric §3.1 `name-mainstream-convention` (Tier 3 ambiguity test) — "manifests" is bare; in software contexts the word maps to multiple distinct concepts (Kubernetes manifests, Docker manifests, package manifests, web app manifests). The action operates exclusively on **GHCR Docker manifests** via the GitHub Packages API. Sibling `check-ghcr-packages-exist` and `check-ghcr-image-tag-exists` already use the narrower `ghcr` segment. Consistency + ambiguity-test both push toward the prefix. | `cleanup-ghcr-orphan-manifests`. Recommended (best-practice): the rename. Consumer churn: 1 call site (`shop/.github/workflows/cleanup.yml`). Lower-cost alternatives exist (rename to `cleanup-ghcr-untagged-manifests` to also retire the slightly-non-standard "orphan" verb — but "orphan untagged manifest" is a real GHCR term in the cleanup ecosystem so keep "orphan"). |

**Examined and rejected — naming:**

- `cleanup-deployments`, `cleanup-prereleases` — the rubric §3.1 ambiguity-test table explicitly names these as the rubric-correct names (no `github` prefix), even though the bare nouns `deployment` and `prerelease` would on their own be ambiguous. Treating the table as authoritative; the actions match it. KEEP. (Note the inconsistency between the rubric's narrative argument — "add the prefix when bare is ambiguous" — and its concrete examples here; flagged in Internal report under Mainstream-first review for the rubric author's attention.)
- `get-last-workflow-run`, `wait-for-workflow`, `trigger-and-wait-for-workflow` — `workflow-run` and `workflow` are listed in the rubric §3.1 ambiguity table as "bare ambiguous", but the rubric examples for those rows show names *without* the `github` prefix (`wait-for-workflow`, `trigger-and-wait-for-workflow`). Treating the table as authoritative; the actions match it. KEEP.
- `compose-prerelease-status` — considered renaming because "status" can be confused with commit-statuses (a different concept the repo also models — `create-commit-status`/`get-commit-status`). Rejected: the action emits a *git tag* whose suffix is a status verb (`-deployed`, `-passed`, `-approved`); the action and output names (`compose-prerelease-status` / `status-tag`) plus the description's explicit "Output is a git tag, not a SemVer version" disambiguate cleanly. Past-participle verb suffixes match rubric §3.6 tag-naming convention. KEEP.
- `check-ghcr-image-tag-exists` vs `check-ghcr-packages-exist` — neither rename proposed; the `check-ghcr-image-tag-exists` action is instead flagged for **removal** (zero callers, see Parameter findings — External (breaking) below) rather than rename.
- `check-tag-exists` vs `validate-tag-exists` — the split looks asymmetric ("`check-*` non-assert + `validate-*` assert") but matches the Mainstream-first principle (`check-*` = boolean return / non-asserting; `validate-*` = assert-and-fail) and is honest about side-effect-on-failure semantics. KEEP as-is.

### Parameter findings — External (breaking)

#### `check-ghcr-image-tag-exists`
- **Removal candidate** — see Usage findings above. Zero callers in active consumers; the description's stated pairing with `bump-patch-versions` does not hold (the latter resolves the GHCR-image signal directly through `shared/ghcr-probe.sh` rather than by calling this action). Three resolutions:
  1. **Recommended (best-practice — DROP):** delete the action directory. The functionality lives in `shared/ghcr-probe.sh` (used by `bump-patch-versions` and `check-ghcr-packages-exist`); the batched-keyed `[{key, image-url, tag}]` shape has no caller and was speculative under rubric §1.1. Removes ~120 lines of inline GHCR probe code that duplicates the helper. Breaking only at the `optivem/actions/check-ghcr-image-tag-exists@v1` reference surface — and there are no such references in active consumers, so the breaking-change risk is zero in practice. Classify breaking per the §3 always-breaking rule for action-directory removal.
  2. **Lower-effort — RETAIN.** Keep the action as a public primitive in case a future caller wants the batched-keyed shape; refactor it to source `shared/ghcr-probe.sh` (so the inline duplication is removed) and treat the zero-call-site state as "primitive available, not yet adopted". Sacrifices: still preserves the batched-keyed shape that no caller has materialised; rule §1.1 prefers "speculative flexibility — DROP" when the YAGNI evidence is strong. Becomes the partially-done internal helper-extraction item rather than a removal item.
  3. **Status quo.** Leave the inline duplication in place. Sacrifices the helper-extraction benefit and leaves a zero-callsite action in tree that will quietly drift as the helper evolves. Not recommended.
- **Option 1 recommended.** Categorisation: `dead-code`. Consumer impact: 0 active call sites — the change becomes a clean repo-level deletion with no consumer-update step. Classify breaking by rubric convention (action-directory removal is always breaking when external) even though the *practical* impact is nil today.

#### `create-component-tags`
- **Portability + safety bug.** Two distinct issues, both fixable in one item; both are visible from the action's contract:
  - **`name-misleading` on the contract**: the action declares `repository:` and `git-host:` inputs (with non-trivial documentation) but the implementation queries for tags via the local `origin` remote (lines 68 and 81 of `action.yml`). Honouring the inputs only on the push path means the action is only "portable" for the push side; the existence-check side silently uses whatever `origin` happens to point at on the runner. A caller that legitimately uses `repository:` to point at a sibling repo would get a false skip when the local checkout is of a different repo than the target.
  - **`description-too-terse` adjacent — silent error swallow** (`git push "$push_target" "$component_tag" 2>/dev/null`, line 75): violates the "no-swallow" rule in `claude/CLAUDE.md` (the same rule that produced the recent `check-*` action sweep). The push diagnostic is discarded; the fall-through path then re-queries `origin` and reconstructs a tolerable error from `remote_sha` vs `local_sha`, but the original failure reason (auth, network, ref-update protection, etc.) is gone.
  - Two resolutions, with the action's deferred-deletion status (`actions/plans/20260424-101715-component-docker-tagging.md`) influencing the recommendation:
    1. **Recommended (best-practice if the action is staying around) — fix the bugs.** Replace the two `git ls-remote ... origin` invocations with `git ls-remote --tags "$(remote_query_url ...)" ...`-shaped calls (matching `bump-patch-versions`' pattern), and drop the `2>/dev/null` redirect on `git push`. Internal, behaviour-preserving for any caller passing `repository == github.repository` (the current state in courses) — but classify as breaking because the contract surface changes for any future caller that wires up a non-default `repository`. Categorisation: `devops-alignment` (portability) + `parameter-naming`. Consumer impact: 0 active call sites today; out-of-tree references in `courses/` are illustrative and would benefit from a working example anyway.
    2. **Alternative — accelerate the deletion** by collapsing it with the course-pages migration (out-of-scope for this audit — it requires updating five course `.md` files alongside removing the action). Demoted because the timing is gated by the courses agent's own queue.
- **Option 1 recommended.** Categorisation: `devops-alignment`. Consumer impact today: 0 active call sites. Classify breaking by rubric convention (the contract surface change for `repository` / `git-host` is consumer-visible).

### Duplicates — External

None. Examined and rejected:

- `check-ghcr-image-tag-exists` vs `check-ghcr-packages-exist` — same underlying operation (OCI HEAD `/manifests/{tag}` against `ghcr.io`) but different input/output shapes: per-(image, tag) batched vs newline-list with shared-or-per-line tag. Test (a): same side effect on same target — yes. Test (b): swappable inputs/outputs — no. Not a duplicate; covered instead under Parameter findings — External (breaking) as a removal candidate (zero callers makes the swappability question moot).
- `wait-for-workflow` vs `trigger-and-wait-for-workflow` — the second is a strict superset of the first's watch phase + a dispatch step. Same side effect on same target? No — one triggers, one only observes. Test (b): cannot swap. Not a duplicate. Shared watch logic is extractable but is intentionally inlined.
- `read-base-version` vs `read-base-versions` — singular vs plural (batched keyed form). Different surface entirely. Not a duplicate; both are called.
- `check-commit-status-exists` vs `get-commit-status` — same subject (commit statuses on a SHA) but different polarities and surfaces: one is a definitive boolean probe with `created-at` observability metadata, the other is a single-match read that returns empty outputs on no-match. Not a duplicate — both are first-class.
- `validate-tag-exists` vs `check-tag-exists` — paired read/assert. Same input shape, opposite failure semantics. Not a duplicate.
- `compose-docker-image-urls` vs `compose-tags` — examined again this run. `compose-docker-image-urls` accepts a flat string list (or JSON array) and outputs JSON `[string]`; `compose-tags` accepts keyed JSON `[{key, version}]` and outputs keyed JSON `[{key, tag}]`. Different shapes on both ends; merge would force 24 callers of `compose-docker-image-urls` into the keyed shape for no behavioural gain. KEEP both.

### Consolidation opportunities — External

None with external (consumer-visible) impact. Examined and rejected / considered:

- **`resolve-docker-image-digests` — §6.3 merged projections.** The action emits both `image-digest-urls` and `latest-updated-at`. Both projections derive from the same registry-query atom; splitting forces 19 shop callers to run the query twice. KEEP merged.
- **`check-ghcr-image-tag-exists` + `check-ghcr-packages-exist` merge with mode flag.** Considered again this run. Rejected for the same reason as the prior audit AND because the more pressing question is the dead-code status of `check-ghcr-image-tag-exists` itself — see External (breaking) Parameter findings for the recommended resolution (drop the unused action rather than merging it into the active one).
- **Generic `bump-version` over `bump-patch-versions`.** Considered: the action is named `bump-patch-versions` (plural) but only computes patch bumps; a future need for minor or major auto-bumps would force a parallel action. Rejected as a current-state finding under the §6.3 / §1.1 standard config axis lens — patch-only is the genuine use case today (`+1` on the rightmost SemVer segment), and the YAGNI/over-generalisation cost would be larger than the rename cost when a real second-axis caller appears.

### DevOps alignment findings — External (breaking)

Organised by subsection (only those with findings, plus **Other**):

- Tool-agnostic composition — **finding (covered above):** `create-component-tags` mixes parameterised `push_target` with hardcoded `origin` query — see Parameter findings. The fix is filed there; cross-referenced here for §8 filing-guide consistency (the portability claim of a Tier 2 action that hardcodes a remote name).
- Separation of concerns — None. `render-system-stage-summary` again passes the §1.8 thin-wrapper test (validate → format ×2 → render → conditional `::notice` is material orchestration).
- Composite opacity — None.
- Prefer VCS over platform API — None.
- Composition ordering — None.
- Idempotence — None.
- Secrets / auth — None. Every token-consuming action declares `token` as a named input with `default: ${{ github.token }}` and bridges via step-level `env:` rather than interpolating into `run:`.
- Other — **§1.9 marketplace-action version currency sub-pass.** The only external `uses:` ref inside this repo's `action.yml` files is `docker/login-action@v4` in `tag-docker-images/action.yml` (line 39). Queried `gh api repos/docker/login-action/releases/latest --jq .tag_name` → `v4.1.0`. The ref pins the current major — **no D/E/F finding.** The three internal `uses:` refs in `render-system-stage-summary/action.yml` (`optivem/actions/format-artifact-list@v1` ×2, `optivem/actions/render-stage-summary@v1`) are out of scope per rubric §1.9. **Cap note:** 1 distinct external repo this pass — well below the ~60-repo cap.

## Non-breaking changes

### Parameter findings — External (non-breaking)

None this run.

**Note on the prior `deploy-docker-compose` non-breaking item.** The 2026-04-24 audit recommended relaxing `environment` + `version` from `required: true` to `required: false` with `default: ''`. The current `action.yml` keeps both `required: true` and the descriptions have been updated to "Required — every run must announce its target/the version being deployed. The action does not route based on this value." This is an authorial counter-signal — the constraint is intentional and load-bearing as a "must-announce" UI contract even though the value is display-only. The prior recommendation does NOT carry forward; see chat-return Challenges-to-prior-plan section. (The display-only-but-mandatory shape is unusual but defensible — the rubric's `required-default-contradiction` rule offers three resolutions, and the author has chosen "drop the `default:` and keep `required: true`" by reading the input as a UI-mandatory display label rather than an operational parameter.)

### DevOps alignment findings — External (non-breaking)

None this run.

## Summary — External

- **Counts — breaking:** 2 naming violations (`generate-release-notes`, `cleanup-orphan-manifests`) · 2 parameter findings (`check-ghcr-image-tag-exists` removal, `create-component-tags` portability + swallow fix) · 0 duplicate clusters · 0 consolidation opportunities · 0 DevOps alignment findings filed separately (the `create-component-tags` portability concern is filed under Parameter findings rather than duplicated). Total breaking plan items: 4.
- **Counts — non-breaking:** 0 parameter findings · 0 DevOps alignment findings. Total non-breaking plan items: 0.
- **Top 3 highest-impact external changes** (by consumer churn; breaking ranks above non-breaking at the same call-site count):
  1. `[breaking]` Rename `generate-release-notes` → `generate-prod-release-notes` — 12 call sites across shop. Directory rename + `uses:` ref update at every call site + downstream documentation in courses. Category: `naming`.
  2. `[breaking]` Rename `cleanup-orphan-manifests` → `cleanup-ghcr-orphan-manifests` — 1 call site (`shop/.github/workflows/cleanup.yml`). Tier 3 ambiguity-test fix. Category: `naming`.
  3. `[breaking]` Delete `check-ghcr-image-tag-exists` (or, if retained, refactor to source `shared/ghcr-probe.sh`) — 0 active call sites. Category: `dead-code`.
