# Actions audit report — Internal — 2026-04-22 15:14 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `.plans/20260422-151443-audit-actions-internal.md` — the actionable checklist derived from this report.

**External-scope companion:** `.reports/20260422-151443-audit-actions-external.md` (findings) + `.plans/20260422-151443-audit-actions-external.md` (queue). The external report holds the shared inventory and usage findings.

## Header

- `backwards_compatible`: **false** (breaking changes are in scope; this scope contains no items).
- `scope`: **all**.
- `mode`: **both** — paired run; external-scope findings live in the companion file.
- **Publication intent:** internal-only.
- **Total actions audited:** 37 (see companion external report for inventory).
- **Date:** 2026-04-22
- **Rubric version:** v8
- **Prior-plan reconciliation:**
  - `.plans/20260422-132542-audit-actions-internal.md` — 1 open item reconciled: the `check-sha-on-branch` `name:` field fix has been applied (`action.yml` line 1 reads `'Check SHA On Branch'`); the prior item was marked **done — verified 2026-04-22** in place. No still-valid items to carry over; no obsolete items.
  - No legacy single-file plans or other `*-audit-actions*.md` files require reconciliation.

## Naming violations — Internal

None.

## Parameter findings — Internal

None.

## Duplicates — Internal

None.

## Consolidation opportunities — Internal

None.

## DevOps alignment findings — Internal

### Tool-agnostic composition

None.

### Other

None. All actions use `token:` as a declared input with `default: ${{ github.token }}`; all bridge the token via step-level `env:` rather than interpolating into `run:` lines; `docker/login-action@v4` receives the token through a `with: password:` block on the Marketplace action (not a shell interpolation). Supply-chain pins conform to the rubric §1.4 ladder tier 1 (Marketplace-trusted publisher on major tag). `author:` / `branding:` remain consistently absent across all 37 actions (prior audit resolution; internal-only publication intent per repo). `shell: bash` throughout — no `pwsh` or `.ps1` outside the `shared/_test-*` allowlist.

## Summary — Internal

- **Counts:** naming violations = 0; parameter findings = 0; duplicate clusters = 0; consolidation opportunities = 0; DevOps alignment findings = 0.
- **Top internal changes:** none. The prior internal plan's sole open item (`check-sha-on-branch` `name:` field edit) has been verified applied; the repo passes rubric v8 with no internal-scope action items this cycle.
