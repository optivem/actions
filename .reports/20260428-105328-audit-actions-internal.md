# Actions audit report — Internal — 2026-04-28 10:53 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `plans/20260428-105328-audit-actions-internal.md` — the actionable checklist derived from this report.
External-scope companion: `.reports/20260428-105328-audit-actions-external.md` (findings) + `plans/20260428-105328-audit-actions-external.md` (queue). The external report holds the shared inventory and usage findings.

## Header
- `backwards_compatible`: false
- `scope`: all
- `mode`: both — paired run.
- **Publication intent:** internal-only. Missing `branding:` is not a finding.
- **Total actions audited:** 40
- **Date:** 2026-04-28

**Prior-plan reconciliation:** see the paired external report's header for the full reconciliation log. Short summary: no prior plan files exist on disk to reconcile in-place. Cross-checked the prior 2026-04-24 internal report's one actionable recommendation (extract `shared/ghcr-probe.sh` across three actions) — **partially done** (2 of 3 callers migrated: `check-ghcr-packages-exist` and `bump-patch-versions` source the helper; `check-ghcr-image-tag-exists` still inlines the `fetch_registry_token` + `probe_manifest` pair). The residual is re-emitted as a plan item below — but is partially-overlapping with the External Breaking item that proposes deleting `check-ghcr-image-tag-exists` outright. If the action is removed (External item 3), this internal item becomes obsolete and should be marked so when External item 3 is decided.

## Naming violations — Internal

None. Every directory name matches its `name:` field in Title Case (with standard acronym capitalisation — `GHCR`, `SHA`, `URL`, `GitHub`, `OCI`) and passes the kebab-case / verb-first / Tier-3-ambiguity tests in rubric §3.1–§4.

**Examined and rejected:**

- `check-commit-status-exists` — considered simplifying to `check-commit-status` (the `-exists` suffix reads as verbose). Rejected per the Mainstream-first principle: the paired reader `get-commit-status` carries a conflicting verb, so the `-exists` suffix earns its keep by disambiguating the boolean return (`check-commit-status-exists`) from the single-match read (`get-commit-status`). KEEP.
- `check-ghcr-packages-exist` — considered renaming to `check-ghcr-images-exist` for surface consistency with the (soon-to-be-deleted) sibling `check-ghcr-image-tag-exists`. Rejected: the action operates at the GHCR "package" granularity (GitHub's vocabulary for a repo-scoped image path in the Packages model), and the name aligns with GitHub's documentation. Mainstream-first wins over internal consistency.
- `compose-prerelease-status` vs commit-status confusion — the action emits a *git tag* whose suffix is a status-verb (`-deployed`, `-passed`, etc.). Considered renaming to `compose-status-tag` or similar. Rejected: the description and the output name (`status-tag`) both make the tag-not-status distinction explicit; matches rubric §3.6 tag-naming convention (past-participle verb). KEEP.
- `read-base-version` vs `read-base-versions` — the plural is a batched keyed form; the singular is legitimately clearer for single-file callers (27 call sites) and is implementationally simpler. KEEP both.

## Parameter findings — Internal

#### `bump-patch-versions`
- All inputs/outputs OK. Now sources `shared/ghcr-probe.sh` (one of the two callers that have migrated since the prior audit's internal recommendation).

#### `check-changes-since-tag`
- Multi-line descriptions explain the priority-walk and pathspec semantics. Output `changed` documents the fail-open semantics ("also 'true' (fail-open) if no baseline tag matched any pattern"). §1.2 observability is explicit. OK.

#### `check-commit-status-exists`
- Description carries the no-swallow rule explicitly: "API failure (after gh_retry exhaustion) is indeterminate — the action exits 1 rather than coercing to a false answer." Aligns with the recent `check-*` no-swallow sweep. OK.
- `output.created-at` — unread by any consumer; §1.2 observability metadata. KEEP.

#### `check-ghcr-image-tag-exists`
- The `images` JSON-array shape is well-described. **Action-level finding** lives in the External report (zero call sites, recommended for deletion). No separate Internal-scope parameter finding.

#### `check-ghcr-packages-exist`
- Description carries the no-swallow rule explicitly. Two usage shapes (preflight gate / per-image probe) clearly explained. Sources `shared/ghcr-probe.sh`. OK.

#### `check-sha-on-branch`
- `runs:` block distinguishes `git merge-base --is-ancestor` exit codes 0 / 1 / other (other → exit 1). Aligns with the no-swallow rule. OK.

#### `check-tag-exists`
- All inputs OK. Standard Tier 2 portability + auth axes. OK.

#### `check-timestamp-newer`
- Inputs renamed since prior audit (`subject` → `latest`, `baseline` → `since`); 18 shop callers already on the new names. Empty `since` documents the fail-open semantic ("useful for first-run semantics"). `latest` is `required: true`, no default. OK.

#### `cleanup-deployments`, `cleanup-orphan-manifests`, `cleanup-prereleases`
- All three carry parallel input sets (`retention-days`, `delete-delay-seconds`, `rate-limit-threshold`, `dry-run`, `token`, plus their per-action specifics). Each declares `required:` explicitly and carries sensible defaults where applicable. OK.
- `cleanup-orphan-manifests` directory has a separate naming finding (External report), but the parameter shape itself is fine.

#### `commit-files`
- `input.branch` carries a long rationale block explaining why `github.ref_name` is unsafe as a fallback — excellent. `input.max-retries` default 3, sensible bounded retry per rubric §1.3. OK.

#### `compose-docker-image-urls`, `compose-prerelease-status`, `compose-prerelease-version`, `compose-release-version`, `compose-tags`
- All five compose actions are pure string transforms with explicit input shapes, fail-fast validation (regex / array-shape / placeholder presence), and clear output descriptions. OK.

#### `create-commit-status`
- `input.state` default `success`; description enumerates the four valid states. `input.target-url` default `''` with documented fallback to current run URL. OK.

#### `create-component-tags`
- All inputs/outputs are described. **Action-level findings** (portability bug + silent-swallow) live in the External report. No separate Internal-scope parameter finding.

#### `deploy-docker-compose`
- `input.environment`, `input.version` — `required: true` + the descriptions explicitly spell out "Required — every run must announce its target/the version being deployed. The action does not route based on this value." This is the author's deliberate "display-only-but-mandatory" contract — not a `required-default-contradiction` finding (the prior audit's relax-to-`required: false` recommendation has been counter-signalled by the author; see External report header reconciliation). OK as-is.
- `input.image-urls` + `input.service-names` — paired `required: true` inputs whose count must match. The pairing is described on both inputs. OK.

#### `evaluate-run-gate`
- `input.skip-conditions` description explains the JSON shape and caller-expression-resolution semantics in detail. OK.

#### `format-artifact-list`
- Empty input handled correctly. OK.

#### `generate-release-notes`
- `input.artifact-urls` default `'[]'`, description notes empty skips the section. OK.
- **Note:** flagged for rename in the External report (`generate-release-notes` → `generate-prod-release-notes`). No separate Internal-scope finding; the rename cascades into the internal `name:` field update from `'Generate Release Notes'` → `'Generate Prod Release Notes'`.

#### `get-commit-status`
- Now returns empty outputs on no-match (the prior 2026-04-24 External breaking item is **done**). Description says "Returns empty outputs if no matching status is found." `input.state` default `'success'` + "Empty = any state." remains; the default + escape-hatch combination is intentional and not a `required-default-contradiction`. OK.

#### `get-last-workflow-run`
- 7 inputs covering `workflow-name` + filtering knobs (`status`, `conclusion`, `exclude-run-id`, `limit`, `repository`, `token`). Each input has an explicit `required:` and a description spelling out the rationale (e.g. why `status='completed'` is the safe default to avoid picking concurrent in-progress siblings). OK.

#### `publish-tag`
- `input.token` description carries the careful note about when `GITHUB_TOKEN` is insufficient (workflow-file-changes case). §1.4 token-contract rule satisfied. OK.

#### `read-base-version`, `read-base-versions`
- Both well-shaped; `read-base-versions` now supports an optional `repo` per entry for cross-repo Contents-API fetch (added since prior audit). Description explains the local-vs-API mode-switching cleanly. OK.

#### `render-stage-summary`, `render-system-stage-summary`
- All inputs carry explicit `required:` + descriptions; validation fails fast on invalid `stage-result`. OK.

#### `resolve-commit`
- `input.ref` default `'main'` + runtime fallback to `'main'` on empty (defensive coding). OK.

#### `resolve-docker-image-digests`
- Input renamed from `commit-sha` to `tag` since the prior audit; callers now own the tag convention. Description: "Callers own the tag convention (e.g. 'latest', 'sha-<sha>', 'v1.2.3')." Default `tag: 'latest'` keeps prior callers working. OK.

#### `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`
- Both well-shaped. Long descriptions covering the `prefix*[suffix]` matching rules and the SHA-and-glob filter respectively. `resolve-latest-tag-from-sha` carries the rubric §3.4 scale TODO comment in the `runs:` block. OK.

#### `tag-docker-images`
- Two-mode mutual-exclusion (broadcast vs map) is validated in the shell script and described clearly. The `image-tags` `key` field is documented as feeding `compose-tags` output directly. OK.

#### `trigger-and-wait-for-workflow`
- `input.timeout-seconds` default `'1800'` with rubric §1.3 fast-feedback rationale embedded in the description. `input.workflow-inputs` (renamed from `inputs` to avoid shadowing) — description explains the shadow-avoidance choice. Head-SHA disambiguation logic in the dispatch path (resolves `ref` to a SHA before dispatch to avoid run-list ambiguity). OK.

#### `validate-env-vars-defined`
- Newline-separated, whitespace/blank handling documented. OK.

#### `validate-tag-exists`
- All inputs OK. Standard Tier 2 portability + auth axes. OK.

#### `wait-for-endpoints`
- `input.timeout-seconds` default `'900'`, with rubric §1.3 rationale embedded. Exponential backoff capped + jittered, deadline-clamped sleeps. OK.

#### `wait-for-workflow`
- All inputs have explicit `required:` + sensible defaults + rubric-aware descriptions. `input.timeout-seconds` default `'1800'` with rationale. OK.

**Summary of parameter findings — Internal plan items:** 0. Every parameter either already matches rubric expectations or has its open issue filed under External.

## Duplicates — Internal

- **GHCR OCI probe logic still inlined in `check-ghcr-image-tag-exists`.** Prior internal recommendation (extract `shared/ghcr-probe.sh` across three actions) is **partially done** — `check-ghcr-packages-exist` and `bump-patch-versions` now source the helper; `check-ghcr-image-tag-exists` still has the inline `fetch_registry_token` + `probe_manifest` pair. The remaining migration is straightforward (replace the inline functions with `source "$GITHUB_ACTION_PATH/../shared/ghcr-probe.sh"` and call `ghcr_bearer_for` + `ghcr_probe_manifest` instead) — but is partially overlapping with the External Breaking item proposing deletion of the action. If the action is deleted, this finding becomes obsolete; if it is retained, this finding stands.

  - **Recommended (best-practice):** decide the External Breaking item first. If deletion → mark this Internal item obsolete. If retention → apply the helper-extraction (filed as plan item 1 below).

**Examined and retained:**

- `gh-retry.sh` continues to be the repo's retry-with-backoff primitive for `gh api` calls, sourced from `check-commit-status-exists`, `commit-files`, `create-commit-status`, `get-commit-status`, `get-last-workflow-run`, `trigger-and-wait-for-workflow`, `wait-for-workflow`. Good use of the shared-helper pattern.
- `remote-url.sh` sourced from `bump-patch-versions`, `check-tag-exists`, `create-component-tags`, `publish-tag`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`, `validate-tag-exists`. Good (note `create-component-tags` sources the helper but still uses raw `origin` for two of three remote calls — see External Parameter findings).
- `clear-persisted-credentials.sh` sourced from `publish-tag` and `create-component-tags`. KEEP.

## Consolidation opportunities — Internal

- **Shared GHCR OCI probe helper migration** — see Duplicates above. One plan item; conditionally obsolete.

**Examined and rejected:**

- **`wait-for-workflow` watch-phase extraction.** Both `wait-for-workflow` and `trigger-and-wait-for-workflow` inline the watch block (rate-limit check → `gh_retry run list` → `gh_retry run watch`). Shared helper candidate (`shared/gh-watch.sh`?). Rejected: the watch phases differ in a load-bearing way (one matches by commit SHA after dispatch, the other discovers by run-id from the dispatch call). Factoring is plausible but would need careful review of the hard-timeout + exit-code propagation. Deferred — not in this plan.
- **`render-system-stage-summary` → §1.8 thin-wrapper test.** The action chains validate → `format-artifact-list` ×2 → `render-stage-summary` → conditional `::notice`. Re-evaluated against §1.8 material-logic test: the success-vs-skipped content assembly + the conditional `::notice` annotation are material orchestration. Passes the test. KEEP.
- **`check-ghcr-image-tag-exists` + `check-ghcr-packages-exist` merge with mode flag.** Considered again. With `check-ghcr-image-tag-exists` now flagged for deletion (External report), the merge question is moot — there is no "second action" to merge into the active one. KEEP `check-ghcr-packages-exist` as-is.

## DevOps alignment findings — Internal

Organised by subsection (only those with findings, plus **Other**):

- Tool-agnostic composition — None at internal scope. (The `create-component-tags` portability finding is filed externally because the contract surface change is consumer-visible.)
- Separation of concerns — None.
- Composite opacity — None.
- Prefer VCS over platform API — None. `check-tag-exists`, `check-changes-since-tag`, `resolve-latest-*`, `validate-tag-exists`, `publish-tag`, `bump-patch-versions` (git-tag path) all use `git ls-remote` / `git tag` / `git diff` / `git push`. `gh api` / `gh run list` / `gh workflow run` appear only in Tier 3 actions (commit statuses, workflow runs, cleanup jobs).
- Composition ordering — None.
- Idempotence — None. `publish-tag`, `commit-files`, `create-commit-status`, `cleanup-*` all pass §7.2.
- Secrets / auth — None. All token-consuming actions declare `token` as a named input with `default: ${{ github.token }}`; tokens are bridged to `env:` and referenced as shell variables (`$GH_TOKEN`, `$TOKEN`) rather than interpolated into `run:` lines directly.
- Shell portability — All actions are `shell: bash`. Matches repo policy. OK.
- Observability / dual surface — All long-running / rate-limited actions use `::warning::` / `::error::` / `::notice::` appropriately. OK.
- Bounded retry / fast-feedback sizing — All retry loops are bounded (4-attempt staircase for GHCR probes; exponential backoff for `commit-files`; deadline-clamped `wait-for-endpoints`; rate-limit-threshold-aware `wait-for-workflow` / `trigger-and-wait-for-workflow`). OK.
- `branding:` — publication intent is internal-only; not a finding.
- Other —
  - **§1.9 marketplace-action version currency sub-pass (internal scope).** Same scan as External: only one external `uses:` ref (`docker/login-action@v4` in `tag-docker-images/action.yml`); upstream latest is `v4.1.0`; pinned at current major. No finding.
  - **Mainstream-first principle flag — rubric inconsistency between §3.1 narrative and table.** The §3.1 narrative argues "add the `github` (or narrower `ghcr`) prefix when the bare core noun would collide with generic English or other domains." Its concrete examples table then lists `cleanup-deployments` (no prefix) and `cleanup-prereleases` (no prefix) for rows where "Ambiguous bare?" is "yes." That's an internal contradiction — the audited names match the table, not the narrative. Flagged here for the rubric author's attention rather than as an action-level finding (per the agent definition's instruction: "If a rubric rule would push away from mainstream, flag the rule (not the action) in 'Additional findings' so the author can update it"). Suggested rubric clarification: pick one — either expand the prefix to those rows (`cleanup-github-deployments`, `cleanup-github-prereleases`) and flag them in this run; or update the narrative to say the prefix is **optional** when the verb (`cleanup-`, `wait-for-`, `trigger-and-wait-for-`) provides additional context that lifts ambiguity. The audited repo's current naming sits comfortably under the second reading.

## Summary — Internal

- **Counts — Internal:**
  - Naming violations — 0 (four considered-and-rejected cases captured above).
  - Parameter findings — 0 plan items (every open parameter issue is filed under External).
  - Duplicates / consolidation — 1 plan item (extract `shared/ghcr-probe.sh` into `check-ghcr-image-tag-exists`), conditionally obsolete depending on the External deletion decision.
  - DevOps alignment — 0 direct DevOps plan items (the rubric-narrative-vs-table inconsistency is a meta-flag, not an action plan item).

- **Top 3 highest-impact internal changes:**
  1. Migrate `check-ghcr-image-tag-exists/action.yml` to source `shared/ghcr-probe.sh` (deletes ~120 lines of inline duplication) — **only if the action is retained per External item 3**; otherwise mark obsolete.
  2. (No further internal items this run.)
  3. (No further internal items this run.)
