# Actions audit report — Internal — 2026-04-22 09:21 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. Publication intent: internal-only.

**Plan (action queue):** `.plans/20260422-092143-audit-actions-internal.md` — the actionable checklist derived from this report.

**External-scope companion:** `.reports/20260422-092143-audit-actions-external.md` (findings) + `.plans/20260422-092143-audit-actions-external.md` (queue). The external report holds the shared inventory and usage findings.

> **Note:** This report is a frozen snapshot from the original audit run. Action directory names in the findings below reflect pre-rename audit-time state. Several renames proposed in the external report have since been applied (see git log in the `actions` repo after 2026-04-22 for current state).

## Header

- `backwards_compatible`: **false** (breaking changes are in scope).
- `scope`: **all**.
- **Publication intent:** internal-only.
- **Total actions audited:** 37 (see companion external report for inventory).
- **Date:** 2026-04-22

## Naming violations — Internal

None.

## Parameter findings — Internal

#### trigger-and-wait-for-github-workflow
- `input.workflow` — `type-shape-ambiguous`: description says "Workflow filename" but `gh workflow run --workflow` accepts both filename and display name. Proposed fix: tighten description to `"Workflow filename (e.g. monolith-java-commit-stage.yml) — matches \`gh workflow run --workflow\`"`. Description-only; no rename.

#### wait-for-github-workflow
- `input.workflow` — `type-shape-ambiguous`: same shape ambiguity as the sibling. Proposed fix: align description.

#### bump-patch-versions
- `input.remote` — Tier 2 sub-rule parity: other Tier 2 actions accept `git-host`. Proposed fix: add `git-host` input with default `github.com` and compose the remote URL the same way siblings do. Default preserves today's effective behaviour on `github.com` — no consumer workflow needs updating. Tracked as a plan item.

## Duplicates — Internal

None.

## Consolidation opportunities — Internal

None.

## DevOps alignment findings — Internal

### Tool-agnostic composition

- **`bump-patch-versions` hardcodes `origin` as the remote.** Every other Tier 2 action in this repo accepts a `git-host` input (`ensure-tag-exists`, `publish-tag`, `check-tag-pattern-exists`, `create-component-tags`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`). The inconsistency means a student porting to GitLab/self-hosted would have to edit *this* action while leaving its siblings alone. Rubric §3.1 Tier 2 sub-rule. Recommended: add `git-host` input with default `github.com` and compose the remote URL the same way siblings do — additive with behaviour-preserving default, no consumer impact.

### Other

- **`author:` + `branding:` inconsistency across the set.** Publication intent is internal-only. Most actions carry `author: 'Optivem'` + `branding:` blocks; a subset (`compose-prerelease-version`, `compose-release-version`, `read-base-version`, `deploy-docker-compose`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow`, `ensure-version-unreleased`, `render-system-stage-summary`) does not. The rubric exempts missing `branding:` as a finding under internal-only publication, and the spec explicitly says do NOT flag missing `branding:`. But the *inconsistency* is still a code-quality smell — pick one shape and apply it uniformly.
  - Options:
    1. **Recommended (best-practice):** strip `author:` + `branding:` from all 37 actions. Internal-only has no consumer that reads them.
    2. Lower-churn: add `author:` + a minimal `branding:` to the eight that lack them for uniformity.

## Summary — Internal

- **Counts:** naming violations = 0; parameter findings = 3 (description-only, across 2 actions); duplicate clusters = 0; consolidation opportunities = 0; DevOps alignment findings = 2.
- **Top 3 highest-impact internal changes:**
  1. **Unify `author:`/`branding:` posture across all 37 actions** — touches every file but is metadata-only. Biggest code-quality win per unit of consumer risk.
  2. **Add `git-host` to `bump-patch-versions`** — closes the Tier 2 parity gap flagged by rubric §3.1.
  3. **Clarify `workflow` input description** on both workflow-wait actions — removes the filename-vs-display-name ambiguity that surfaced during this audit.
