# Actions audit report — Internal — 2026-04-24 12:45 UTC

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

**Plan (action queue):** `.plans/20260424-124513-audit-actions-internal.md` — the actionable checklist derived from this report.
External-scope companion: `.reports/20260424-124513-audit-actions-external.md` (findings) + `.plans/20260424-124513-audit-actions-external.md` (queue). The external report holds the shared inventory and usage findings.

## Header
- `backwards_compatible`: false
- `scope`: all
- `mode`: both — paired run.
- **Publication intent:** internal-only. Missing `branding:` is not a finding.
- **Total actions audited:** 38
- **Date:** 2026-04-24

**Prior-plan reconciliation:** see the paired external report's header for the full reconciliation log. Short summary: no prior plan files exist on disk (the `.plans/` directory itself did not exist prior to this run). Cross-checked prior report recommendations against current repo state; 6 were done, 2 were obsolete, 0 were still-valid. No prior plan items carry forward.

## Naming violations — Internal

None. Every directory name matches its `name:` field in Title Case (with standard acronym capitalisation — `GHCR`, `SHA`, `URL`, `GitHub`) and passes the kebab-case / verb-first / Tier-3-ambiguity tests in rubric §3.1–§4.

**Examined and rejected:**

- `check-commit-status-exists` — considered simplifying to `check-commit-status` (the `-exists` suffix is arguably verbose). Rejected per the Mainstream-first principle: the paired reader `get-commit-status` carries a conflicting verb, so the `-exists` suffix earns its keep by disambiguating the boolean return (`check-commit-status-exists`) from the single-match read (`get-commit-status`). KEEP.
- `check-ghcr-packages-exist` — considered renaming to `check-ghcr-images-exist` for consistency with the sibling `check-ghcr-image-tag-exists`. Rejected: the action operates at the GHCR "package" granularity (GitHub's vocabulary for a repo-scoped image path in the Packages model), and the name aligns with GitHub's own documentation. Mainstream-first wins over internal consistency here.
- `get-last-successful-github-workflow-run-timestamp` — considered shortening (verbose). Rejected: the name is a precise compound noun covering a real Tier 3 concept (GitHub workflow runs), and the length is the honest reflection of the three specialising qualifiers (`last-successful`, `github-workflow-run`, `timestamp`). No shorter form without loss of clarity. KEEP.
- `read-base-version` vs `read-base-versions` — the plural is a batched keyed form; considered merging as `read-base-versions` with a list-of-one. Rejected: the singular form's simpler input (`file: string`) is legitimately clearer for single-file callers (26 shop + 1 gh-optivem = 27 call sites); collapsing would force those callers into the JSON-array shape. KEEP both.

## Parameter findings — Internal

#### `bump-patch-versions`
- `input.version-files` — JSON array input with a per-entry `signal` enum (`git-tag` | `ghcr-image`). The long description explicitly walks through both signal shapes with examples. OK — description shape is explicit, fail-fast on unknown signal.
- `input.repository` / `input.token` — both `required: false` with the standard `github.*` defaults. OK.

#### `check-changes-since-tag`
- `input.tag-patterns` / `input.paths` — multi-line descriptions explain the priority-walk and pathspec semantics. OK.
- `output.changed` — documents the fail-open semantics ("also 'true' (fail-open) if no baseline tag matched any pattern — i.e. first-ever run"). §1.2 observability is explicit. OK.

#### `check-commit-status-exists`
- `input.head-sha` — `default: ${{ github.sha }}`; bridges correctly. OK.
- `output.created-at` — unread by any consumer; §1.2 observability metadata. KEEP.

#### `check-ghcr-image-tag-exists`
- `input.images` — JSON array of `{key, image-url, tag}`. Description explicit about shape, including the `ghcr.io/` prefix and `:tag`-suffix-absence requirements that the validation enforces. OK.
- `output.results` — unread today but §1.2 observability metadata (per-image debug surface; the `any-exist` aggregate is consumed). KEEP.

#### `check-ghcr-packages-exist`
- `input.image-urls` — newline-separated, `required: true`. Description gives an example. OK.
- `input.tag` — `default: 'latest'` is sensible (per repo policy §2, `:latest` is the commit-stage tag). OK.

#### `check-sha-on-branch`
- `input.commit-sha` — `default: ${{ github.sha }}`; `required: false`. Description OK.
- `input.base-branch` — `default: 'main'`; `required: false`. OK.

#### `check-tag-exists`
- `input.tag` — description says "Exact tag or glob pattern to match". OK — shape is explicit.
- `input.repository` / `input.token` / `input.git-host` — standard Tier 2 portability + auth axes. OK.

#### `check-timestamp-newer`
- `input.baseline` — `default: ''` + explicit `fail-open` documentation on the empty-string semantic. OK.
- `input.subject` — `required: true`, no default. OK.

#### `cleanup-github-deployments`
- All inputs declare `required:` explicitly and carry sensible defaults where applicable. OK.

#### `cleanup-github-prereleases`
- Same as above. OK.

#### `commit-files`
- `input.branch` — the description carries a long rationale block explaining why `github.ref_name` is unsafe as a fallback — excellent; `required-default-contradiction` resolved by the author's explicit decision ("always require the caller to pass"). OK.
- `input.max-retries` — `default: '3'`, sensible bounded retry ceiling per rubric §1.3. OK.

#### `compose-docker-image-urls`
- Both inputs `required: true`; output description explicit. OK.

#### `compose-prerelease-status`
- `input.status` — description enumerates the enum. Validation fails fast on unknown. OK.

#### `compose-prerelease-version`
- `input.base-version` / `input.suffix` / `input.build-number` — all `required: true` with SemVer §1.5 alignment. OK.
- `input.prefix` — `required: false` + `default: ''`. Description shows the branching form. OK.

#### `compose-release-version`
- `input.prerelease-version` — description enumerates the accepted prerelease shapes (`v1.0.0-rc.1`, `v1.0.0-alpha.1`, `v1.0.0-dev.3`, etc.). OK.

#### `compose-tags`
- `input.versions` — JSON array of `{key, version}`, `required: true`. OK.
- `input.template` — `default: 'v{version}'`, explicit `{version}` placeholder enforcement. OK.
- `output.tags` — shape + key preservation documented. OK.

#### `create-commit-status`
- `input.state` — `default: 'success'`; description enumerates the four valid states. OK.
- `input.target-url` — `default: ''` + documented fallback to current run URL. OK.

#### `deploy-docker-compose`
- `input.environment` and `input.version` — covered in the External non-breaking parameter findings as a `required-default-contradiction`: both are `required: true` but documented as display-only. The **relaxation** (→ `required: false` + `default: ''`) is External non-breaking; there is no Internal-scope fix.

#### `evaluate-run-gate`
- `input.skip-conditions` — long description with the exact JSON shape and caller-resolution semantics. OK.
- `output.skip-reason` — documented as empty string when `should-run=true`. OK.

#### `format-artifact-list`
- `input.artifacts` — `default: ''`, handles empty correctly. OK.

#### `generate-release-notes`
- `input.artifact-urls` — `default: '[]'`, description notes empty skips the section. OK.
- **Note:** this action is flagged for a rename in the External report (`generate-release-notes` → `generate-prod-release-notes`). No separate Internal finding; the rename cascades into the internal naming surface (`name:` field update from `'Generate Release Notes'` → `'Generate Prod Release Notes'`).

#### `get-commit-status`
- `input.state` — `default: 'success'` + "Empty = any state." description. The combination is intentional: `default: 'success'` is the narrow common case, explicit empty string is the escape hatch for "match any state". **Not** a `required-default-contradiction` — the contradiction rule fires when `default:` is meaningless (e.g. `required: true` + `default:`); here the default is operational. OK.
- Behaviour finding (hard-fail on no match) is filed under the External Parameter findings (breaking) rather than Internal — the fix changes the caller-visible failure contract.

#### `get-last-successful-github-workflow-run-timestamp`
- `input.workflow-name` — `required: true`; description notes `github.workflow` is typically passed. OK.
- `output.timestamp` — documents empty-string-on-no-prior-run. OK.

#### `publish-tag`
- `input.token` — description carries a careful note about when `GITHUB_TOKEN` is insufficient (tagging a commit with workflow-file changes). §1.4 token-contract rule satisfied; the caution is a proactive observability warning. OK.

#### `read-base-version`
- `input.file` — `default: 'VERSION'` (Twelve-Factor standard filename). OK.

#### `read-base-versions`
- `input.entries` — JSON array of `{key, file}`. OK.

#### `render-stage-summary`
- All inputs have explicit `required:` + clear descriptions. Validation fails fast on invalid `stage-result`. OK.

#### `render-system-stage-summary`
- 9 inputs across success/skipped surfaces; each has explicit `required:` + a `default: ''` where optional. OK.

#### `resolve-commit`
- `input.ref` — `default: 'main'` + fallback to `'main'` when empty. Slightly redundant (the schema-default already supplies `'main'`; the runtime fallback handles a caller that explicitly passes `''`). Acceptable defensive coding, not a finding.

#### `resolve-docker-image-digests`
- `input.commit-sha` — `default: ''`; description explains both modes (empty → `:latest`, set → `:sha-<commit-sha>`). §1.1 standard config axis; OK.

#### `resolve-latest-prerelease-tag`
- `input.tag-prefix` / `input.tag-suffix` — long description showing the compose shape `${prefix}*${suffix}` and the `base-tag` strip semantics. OK.

#### `resolve-latest-tag-from-sha`
- `input.pattern` — `default: '*'`. Description describes glob semantics. Scale TODO comment already noted in the `runs:` block (rubric §3.4 performance caveat). OK.

#### `tag-docker-images`
- `input.image-urls` + `input.tag` + `input.image-tags` — all `required: false` with `default: ''`; the two modes (broadcast vs map) are mutually exclusive and validated in the shell script. Description is explicit about mode semantics and the `key` field's role in feeding `compose-tags` output directly. Acceptable — the `required: false` + `default: ''` is the repo's standard shape for mutually-exclusive inputs. OK.

#### `trigger-and-wait-for-github-workflow`
- `input.timeout-seconds` — `default: '1800'` with a rubric §1.3 fast-feedback rationale embedded in the description. OK.
- `input.workflow-inputs` — `default: '{}'`; description explains the shadowing-avoidance name choice. OK.

#### `validate-env-vars-defined`
- `input.names` — newline-separated, whitespace/blank handling documented. OK.

#### `validate-tag-exists`
- `input.tag` — `required: true`, no default. OK.
- `input.repository` / `input.token` / `input.git-host` — standard Tier 2 portability + auth axes. OK.

#### `wait-for-endpoints`
- `input.endpoints` — JSON array shape explicit; `required: true`. OK.
- `input.timeout-seconds` — `default: '900'` with rubric §1.3 fast-feedback rationale embedded. OK.

#### `wait-for-github-workflow`
- All inputs have explicit `required:` + sensible defaults + rubric-aware descriptions. OK.

**Summary of parameter findings — Internal plan items:** 0. Every parameter either already matches rubric expectations or has its open issue filed under External (non-breaking `deploy-docker-compose` relaxation; External breaking `get-commit-status` behaviour change).

## Duplicates — Internal

- **GHCR OCI probe logic duplicated across three actions.** The `fetch_registry_token` + `probe_manifest` pair (or its close sibling `fetch_ghcr_token` + `probe_ghcr_image`) is inlined in:
  - `check-ghcr-image-tag-exists/action.yml` (inline in the `runs.steps` bash)
  - `check-ghcr-packages-exist/action.yml` (inline)
  - `bump-patch-versions/action.yml` (inline — renamed variants, same logic + same retry ladder `(5 15 45)`)

  All three perform the same two-step OCI token exchange + HEAD `/manifests/{tag}` probe with identical retry schedules, identical `Accept:` header sets, and identical 401/403/200/404 case branches. The repo already has precedent for shared helpers (`shared/remote-url.sh`, `shared/clear-persisted-credentials.sh`, `shared/gh-retry.sh`) — extracting the GHCR probe logic into `shared/ghcr-probe.sh` that exports `ghcr_bearer_for PATH TOKEN` + `ghcr_probe_manifest URL BEARER` would delete ~60 lines of near-duplicated bash × 3 actions = ~180 lines of duplication.

  - **Recommended (best-practice):** add `shared/ghcr-probe.sh` exporting the two functions; source it from each of the three actions. Internal-scope only (no consumer surface change). Proposed for the plan.

**Examined and retained:**

- `gh-retry.sh` continues to be the repo's retry-with-backoff primitive for `gh api` calls, sourced from `check-commit-status-exists`, `commit-files`, `create-commit-status`, `get-commit-status`, `get-last-successful-github-workflow-run-timestamp`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow`. Good use of the shared-helper pattern.
- The `remote-url.sh` helper (prior audit's recommendation) is now sourced from `bump-patch-versions`, `check-tag-exists`, `publish-tag`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`, `validate-tag-exists`. Good.
- The `clear-persisted-credentials.sh` helper is sourced from `publish-tag`. (Only one action performs credential-cleared pushes today — `create-component-tags` was removed since the prior audit.) KEEP.

## Consolidation opportunities — Internal

- **Shared GHCR OCI probe helper** — see Duplicates above. One plan item covers the extraction across `check-ghcr-image-tag-exists`, `check-ghcr-packages-exist`, and `bump-patch-versions`.

**Examined and rejected:**

- **`wait-for-github-workflow` watch-phase extraction.** Both `wait-for-github-workflow` and `trigger-and-wait-for-github-workflow` inline a 15-line watch block (rate-limit check → `gh_retry run list --json databaseId,headSha --jq …` → `timeout "$remaining_secs" gh_retry run watch`). Shared helper candidate (`shared/gh-watch.sh`?). Rejected for this pass — the watch phases differ in a load-bearing way (one matches by commit SHA after dispatch, the other discovers by run-id from the dispatch call). Factoring a shared `gh_watch RUN_ID REPOSITORY TIMEOUT WATCH_INTERVAL` helper is plausible but would need careful review of the hard-timeout + exit-code propagation paths in both callers. Deferred — not in this plan.
- **`render-system-stage-summary` → §1.8 thin-wrapper test.** The action chains validate → `format-artifact-list` ×2 → `render-stage-summary` → `::notice`. Re-evaluated against §1.8 material-logic test: the post-validate `::notice` annotation (only on skipped + with non-empty reason) and the success-vs-skipped content assembly are material. Passes the test. KEEP.

## DevOps alignment findings — Internal

Organised by subsection (only those with findings, plus **Other**):

- Tool-agnostic composition — None. All Tier 2 actions (`check-tag-exists`, `validate-tag-exists`, `resolve-commit`, `resolve-latest-*`, `bump-patch-versions` git-tag path, `publish-tag`, `check-changes-since-tag`) parameterise the git host via `git-host` input or derive it via the shared `remote_query_url` / `remote_push_target` helpers. No hardcoded `github.com` outside the `default:` values.
- Separation of concerns — None.
- Composite opacity — None.
- Prefer VCS over platform API — None. Every action that has a portable VCS expression uses it (`git ls-remote`, `git tag -l`, `git merge-base`, `git diff`, `git rev-list`). `gh api` / `gh run list` / `gh workflow run` appear only in Tier 3 actions (commit statuses, workflow runs, cleanup jobs).
- Composition ordering — None.
- Idempotence — None. `publish-tag`, `commit-files`, `create-commit-status`, `cleanup-github-*` all pass §7.2.
- Secrets / auth — None. All token-consuming actions declare `token` as a named input with `default: ${{ github.token }}` (matching rubric §1.4). Inside composites, tokens are bridged to `env:` and referenced as shell variables (`$GH_TOKEN`, `$TOKEN`) rather than interpolated into `run:` lines directly.
- Shell portability — All actions are `shell: bash`. Matches repo policy. OK.
- Observability / dual surface —
  - `evaluate-run-gate` — emits `Gate blocked at condition #$i: $reason` as a log line on skip, but not as a `::notice::` annotation. Minor — the structured `skip-reason` output covers the machine-readable side, and a `::notice::` would surface the reason in the PR Files-changed panel for readers who don't scroll the full log. **Not filed as a plan item** — the structured output is already sufficient; adding `::notice::` is an opportunistic polish, not a rubric gap.
  - All other rate-limited / long-running actions use `::warning::` / `::error::` / `::notice::` appropriately (`check-ghcr-*`, `commit-files`, `publish-tag`, `wait-*`, `trigger-*`). OK.

- Bounded retry / fast-feedback sizing —
  - `wait-for-endpoints` — exponential backoff with jitter, capped at `WAIT_SECONDS * 16`, plus hard `timeout-seconds: 900` deadline with clamped sleeps. Excellent.
  - `wait-for-github-workflow`, `trigger-and-wait-for-github-workflow` — `timeout-seconds: 1800`-bounded watch phase plus rate-limit pre-check, plus 60s dispatch/discovery sub-timeouts. OK.
  - `commit-files` — exponential backoff (`2^attempt`) with `MAX_RETRIES` default 3. OK.
  - `check-ghcr-image-tag-exists`, `check-ghcr-packages-exist`, `bump-patch-versions` (ghcr-image probe path) — bounded 4-attempt retry with staircase delays `(5 15 45)` between attempts. Good structure; one aspect: jitter is not applied to the retry delays. For low-volume probes (a handful of images per run) this is acceptable; a future high-volume user of the probe (e.g. a cleanup job hitting dozens of tags) would benefit from jitter. **Not filed as a plan item** — the current callers are all low-volume.
  - `cleanup-github-deployments`, `cleanup-github-prereleases` — `delete-delay-seconds` + `rate-limit-threshold` knobs. OK.
  - No unbounded retry loops found.

- `branding:` — publication intent is internal-only; not a finding.

- Other —
  - **§1.9 marketplace-action version currency sub-pass (internal scope).** The only external `uses:` ref inside any `action.yml` is `docker/login-action@v4` in `tag-docker-images/action.yml`. Upstream latest (as of 2026-04-24) is `v4.1.0` — pinned at current major. No finding. The three internal `uses:` refs (`optivem/actions/format-artifact-list@v1` ×2, `optivem/actions/render-stage-summary@v1` in `render-system-stage-summary/action.yml`) are out of scope per §1.9. No finding.
  - **Mainstream-first principle flag — none.** Every mainstream-first judgement in this run went *in favour of* the current repo state (`check-*` verb preferred; `get-*` accepted as a read verb; `repository` is the input name everywhere; `github` prefix on Tier 3 actions). No rubric-rule challenge this pass.

## Summary — Internal

- **Counts — Internal:**
  - Naming violations — 0 (four considered-and-rejected cases captured above).
  - Parameter findings — 0 plan items (every open parameter issue is filed under External).
  - Duplicates / consolidation — 1 plan item (extract shared `shared/ghcr-probe.sh` helper across `check-ghcr-image-tag-exists`, `check-ghcr-packages-exist`, `bump-patch-versions`).
  - DevOps alignment — 0 direct DevOps plan items.

- **Top 3 highest-impact internal changes:**
  1. Extract `shared/ghcr-probe.sh` (OCI token exchange + HEAD `/manifests/{tag}` probe) across 3 actions — deletes ~180 lines of duplicated bash; all three callers are hot-path (shop acceptance-stage + shop bump workflows). No consumer impact.
  2. (No further internal items this run.)
  3. (No further internal items this run.)
