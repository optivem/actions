# Actions audit plan — 2026-04-20 10:50 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false`.

See report section in workflow output for full context.

## Items

- [ ] **Consolidate QA-status actions** — Merge `approve-rc-in-qa`, `reject-rc-in-qa`, and `promote-rc-to-qa` into a single action (e.g. `record-rc-qa-status`) that takes a `status` input (`approved`, `rejected`, `deployed`) and an optional `artifact-urls`. All three currently differ only by hardcoded status string + whether artifact-urls is forwarded.
  - Affects: `approve-rc-in-qa`, `reject-rc-in-qa`, `promote-rc-to-qa`
  - Consumers to update: 24 call sites in shop (6 approve + 6 reject + 12 promote-rc-to-qa); 0 in gh-optivem
  - Category: consolidation

- [ ] **Delete deprecated `simulate-deployment`** — Action is an explicit pass-through wrapper around `deploy-to-docker`; its own description starts with `[Deprecated]`. Migrate the 36 shop call sites to `deploy-to-docker@v1` directly (input surface is identical), then remove the directory.
  - Affects: `simulate-deployment`
  - Consumers to update: 36 call sites in shop; 0 in gh-optivem
  - Category: duplicate

- [ ] **Rename `set-commit-status` to an allowed verb-prefix** — `set-` is not in the approved prefix set (`check-`, `resolve-`, `generate-`, `has-`, `summarize-`, `setup-`, `deploy-`, `promote-`, `build-`, `push-`, `tag-`, `cleanup-`, `create-`, `bump-`, `read-`, `find-`, `wait-for-`, `approve-`, `reject-`, `validate-`, `simulate-`, `compose-`, `trigger-`). The action posts a new commit status via `POST /repos/{repo}/statuses/{sha}`, so `create-commit-status` fits the existing `create-` family. Update the `name:` field to `Create Commit Status` accordingly.
  - Affects: `set-commit-status`
  - Consumers to update: 1 call site in gh-optivem; 0 in shop
  - Category: naming

- [ ] **Align `name:` fields with directory Title Case for the three QA-status actions** (only applies if the consolidation item above is rejected). The rule is "`name:` field is the Title Case of the directory name". Current mismatches:
  - `approve-rc-in-qa` → name is `Promote RC to QA Approved` (should be `Approve RC In QA`)
  - `reject-rc-in-qa` → name is `Promote RC to QA Rejected` (should be `Reject RC In QA`)
  - `promote-rc-to-qa` → name is `Promote RC to QA Deployed` (should be `Promote RC To QA`)
  - `promote-rc-to-prod` → name is `Promote RC to Production Deployed` (should be `Promote RC To Prod`)
  - Affects: `approve-rc-in-qa`, `reject-rc-in-qa`, `promote-rc-to-qa`, `promote-rc-to-prod`
  - Consumers to update: 0 (name field is display-only)
  - Category: naming

- [ ] **Delete unused `wait-for-commit-run`** — Zero references in shop, gh-optivem, or any other action.yml composition. A similar (but different) primitive `trigger-and-wait` covers the dispatch-and-watch case; commit-triggered watching is currently not needed.
  - Affects: `wait-for-commit-run`
  - Consumers to update: 0
  - Category: dead-code

- [ ] **Delete unused `wait-for-docker-compose`** — Zero references anywhere. The equivalent wait logic is already embedded inline in `deploy-to-docker`.
  - Affects: `wait-for-docker-compose`
  - Consumers to update: 0
  - Category: dead-code

- [ ] **Delete unused `has-update-since-last-run`** — Zero references in any consumer or composition. Keep only if a concrete near-term caller is planned; otherwise remove the directory.
  - Affects: `has-update-since-last-run`
  - Consumers to update: 0
  - Category: dead-code

- [ ] **Rename `wait-for-docker-compose` to `wait-for-url`** (if kept instead of deleted) — The `runs:` block is a plain `curl -f` polling loop against `inputs.url`; the `docker compose logs` fallback is only invoked on failure and does not make the action specific to Compose. Current directory name misleads callers about what the action requires.
  - Affects: `wait-for-docker-compose`
  - Consumers to update: 0
  - Category: naming

- [ ] **Remove dead inputs from `deploy-to-cloud-run`** — No consumer in shop or gh-optivem passes `memory`, `cpu`, `min-instances`, `max-instances`, or `allow-unauthenticated`. Drop them from the action surface; keep the hardcoded defaults inline in the `gcloud run deploy` command, or re-add later if a caller appears.
  - Affects: `deploy-to-cloud-run`
  - Consumers to update: 51 call sites in shop (no input rewrites needed — these inputs are never set)
  - Category: dead-input

- [ ] **Remove dead inputs from `build-docker-image`** — No consumer passes `image-latest-tag` or `registry-username`; both always use their defaults. Drop from the input surface.
  - Affects: `build-docker-image`
  - Consumers to update: 7 call sites in shop (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead inputs from `push-docker-image`** — No consumer passes `registry-username`; it always defaults to `github.actor`. Drop from input surface.
  - Affects: `push-docker-image`
  - Consumers to update: 7 call sites in shop (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead `suffix` input from `generate-prerelease-version`** — No consumer overrides the default `rc`. The one internal caller (`promote-to-rc`) does not pass it either. Drop and hardcode `rc` in the call to `compose-prerelease-version`.
  - Affects: `generate-prerelease-version`
  - Consumers to update: 1 internal (`promote-to-rc`) + 1 gh-optivem usage — neither sets the value
  - Category: dead-input

- [ ] **Remove dead inputs from `trigger-and-wait`** — No consumer passes `rate-limit-threshold`. Drop it and keep the `50` default inline. `poll-interval` is actively used; keep it.
  - Affects: `trigger-and-wait`
  - Consumers to update: 30 shop + 7 gh-optivem call sites (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead inputs from `find-release-by-run`** — No consumer passes `repository` or `search-limit`. Drop both and keep the current defaults inline.
  - Affects: `find-release-by-run`
  - Consumers to update: 1 call site in shop (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead inputs from `has-unverified-sha`** — No consumer passes `head-sha`, `repo-owner`, or `repo-name`. Drop them and rely on defaults.
  - Affects: `has-unverified-sha`
  - Consumers to update: 1 call site in gh-optivem (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead inputs from `set-commit-status`** (or the renamed `create-commit-status`) — No consumer passes `sha`, `state`, or `target-url`. Drop them and keep defaults inline.
  - Affects: `set-commit-status`
  - Consumers to update: 1 call site in gh-optivem (no rewrites needed)
  - Category: dead-input

- [ ] **Remove dead outputs** — The following outputs are declared but never read by any consumer or composition. Drop them.
  - `check-release-exists.exists` (action has 43 call sites; none read the output — all use it as a failing guard)
  - `check-target-version-exists.exists` (action has 25 call sites; none read the output)
  - `create-release.release-url`
  - `resolve-commit.timestamp`
  - `has-changes-since-tag.baseline-sha`
  - `has-unverified-sha.verified-at`
  - Affects: `check-release-exists`, `check-target-version-exists`, `create-release`, `resolve-commit`, `has-changes-since-tag`, `has-unverified-sha`
  - Consumers to update: 0 (nothing reads these)
  - Category: dead-input

- [ ] **Collapse `check-target-version-exists` into `read-target-version`** — Both inspect the same VERSION file. `read-target-version` already fails with the same error message when the file is missing (`c:\Users\valen\Documents\GitHub\optivem\academy\actions\read-target-version\action.yml:23-25`). The dedicated existence-check has 25 call sites but adds no signal that a later `read-target-version` would not already provide; callers can drop the standalone check step and rely on `read-target-version` for the guard.
  - Affects: `check-target-version-exists`, `read-target-version`
  - Consumers to update: 25 call sites in shop (remove the `check-target-version-exists` step; keep existing `read-target-version` step); 0 in gh-optivem
  - Category: consolidation
