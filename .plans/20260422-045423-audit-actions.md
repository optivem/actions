# Actions audit plan — 2026-04-22 04:54 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false` (breaking changes are in scope). `scope = all`. Publication intent: internal-only.

See report section in workflow output for full context.

## Items

- [ ] **Consolidate `resolve-tag-from-sha` into `resolve-latest-tag-from-sha`** — the former returns "the first tag pointing at a SHA"; the latter returns "the highest-version tag matching a glob pattern pointing at a SHA". The second is a superset: with `pattern: '*'` it approximates the first (sort-by-version vs. insertion-order). Keep only `resolve-latest-tag-from-sha`, default `pattern: '*'`, document that multiple matches resolve to highest version sort.
  - Affects: `resolve-tag-from-sha`, `resolve-latest-tag-from-sha`
  - Consumers to update (1 in shop, 2 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/_prerelease-pipeline.yml`
    - `gh-optivem/.github/workflows/gh-acceptance-stage.yml`
    - `gh-optivem/.github/workflows/gh-release-stage.yml`
  - Category: consolidation
  - **Status:** Author response "I don't understand". Pending clarification.

- [ ] **Rename `push-docker-image` input keys to reflect they are full tagged URLs, not raw tags** — `image-sha-tag`, `image-latest-tag`, `image-version-tag` read as "a tag value" but actually carry full `{registry}/{namespace}/{name}:{tag}` URLs. Per rubric §4 `name-misleading`: rename to `image-sha-url`, `image-latest-url`, `image-version-url` for honest shape. Update description accordingly.
  - Affects: `push-docker-image`
  - Consumers to update (7 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-dotnet-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-java-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: parameter-naming
  - **Status:** Author response "Are you sure? show me examples". Pending examples + decision.
