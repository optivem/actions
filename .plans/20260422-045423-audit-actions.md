# Actions audit plan — 2026-04-22 04:54 UTC

All items from this audit have been executed. This file can be deleted once the follow-ups below are triaged.

## Follow-ups discovered during execution (not in original audit)

- [ ] **eshop workflows reference stale action contracts** — `eshop/.github/workflows/commit-stage-backend.yml` and `commit-stage-frontend.yml` use `optivem/actions/push-docker-image@v1` (plus `build-docker-image@v1` and a non-existent `summarize-commit-stage@v1`) with input/output keys that do not match current `@v1` contracts. These are broken independent of item #8 — needs triage: stale/abandoned, partial-migration, or points at a different actions repo snapshot.

- [ ] **courses teaching material uses old `-tag` input names** — `courses/01-pipeline/accelerator/course/02-commit-stage/03-publish-artifact.md` shows `image-sha-tag` / `image-latest-tag` / `image-version-tag` as the example inputs to `push-docker-image@v1`. After item #8, the action expects `-url` names. Refresh the teaching material for consistency.
