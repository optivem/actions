# Actions audit plan — 2026-04-22 05:50 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false` (breaking changes are in scope). `scope = all`. Publication intent: internal-only.

Coexisting with earlier plan `.plans/20260422-045423-audit-actions.md` (2 open items). De-duplication applied — items already captured there (consolidate `resolve-tag-from-sha` into `resolve-latest-tag-from-sha`; rename `push-docker-image` `image-*-tag` inputs to `-url`) are omitted from this queue. Challenges to those items are in the report, not here.

See report section in workflow output for full context.

## Items

- [ ] **Rename `check-ghcr-packages-exist` output `exist` → `exists`** — single-subject boolean output should be plural-subject neutral (`exists`). Mainstream-checked: `actions/*` boolean outputs (`cache-hit`, `outcome`, `conclusion`) use grammatically-correct English; `exist` reads as a verb fragment, not an output noun. Sibling action `check-tag-pattern-exists` already uses `exists`. Align.
  - Affects: `check-ghcr-packages-exist`
  - Consumers to update (18 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-legacy.yml`
  - Category: parameter-naming
VJ: Ar eyou sure, where's your proof?


- [ ] **Rename `check-unverified-commit-status` → `has-unverified-commit-status` (or similar) and flip semantics to a positive-polarity verb** — current name is a double-negative: `changed=true` means "no matching success status found" i.e. "NOT verified". The `check-unverified-*` verb phrase asks "check that it is unverified", which is the opposite of what the action's `changed` output reports (it reports *whether* unverified, not that it is unverified). Rubric §4 `name-misleading`. Better: flip to a positive-polarity query such as `has-verified-commit-status` with output `verified=true|false`, or rename the output from `changed` → `unverified` so the polarity matches the verb.
  - Affects: `check-unverified-commit-status`
  - Consumers to update (0 in shop, 1 in gh-optivem, 0 in optivem-testing):
    - `gh-optivem/.github/workflows/gh-acceptance-stage.yml`
  - Category: naming
VJ: can you propose updating rules, because I thought we discovered check is more maisntream?

- [ ] **`check-tag-pattern-exists` uses platform API token + hardcoded-URL auth for a git-native operation** — action is Tier 2 (uses `git ls-remote`), but its `remote_url` is constructed with `https://x-access-token:${TOKEN}@${GIT_HOST}/${REPO}.git` — which works only because `GITHUB_TOKEN` accepts that URL format. Documented pattern in rubric §3.3: this specific auth shape is acceptable for Tier 2, but the action's `description:` says "no platform API dependency" which is true, and the `token` input description is accurate. No rename needed. HOWEVER the token input desc is generic ("Token for authenticating to the remote"); since it defaults to `${{ github.token }}` which is a GitHub-platform concept, clarify the description that the token shape is `x-access-token:<token>` and document the GitLab/Bitbucket substitute. Low priority.
  - Affects: `check-tag-pattern-exists`, `ensure-tag-exists`, `publish-tag`, `create-component-tags`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`, `resolve-tag-from-sha`
  - Consumers to update: none (description-only change, no behavior change)
  - Category: parameter-description

- [ ] **`create-github-release` is missing `description:` and `required:` declarations on several inputs** — no. Re-verified: all inputs have `description:` and explicit `required:`. SKIP; leave this item out on re-verification. (Keeping placeholder removed.)

- [ ] **`create-commit-status` input `state` has `required: false` with `default: 'success'` — document the set of valid values in the description** — description reads "Status state: success, failure, pending, or error" which is good. However `render-stage-summary` independently validates `stage-result` against the set `success|failure|cancelled|skipped` — note inconsistency. Not a bug per se (different subject — CI stage vs. commit status) but worth flagging that the verb `state` maps to GitHub Statuses API whereas `stage-result` maps to the GitHub Actions job result type. No action; listed here only to confirm — drop from plan.

- [ ] **`ensure-env-vars-defined` input `names` description is ambiguous about case and whitespace handling** — description says "Newline-separated list of environment variable names to validate". The script trims whitespace and treats empty lines as no-op, but the description doesn't say so. Rubric §3 rule `type-shape-ambiguous` — a caller reading the description can't tell whether leading/trailing whitespace or blank lines are allowed. Minor description fix.
  - Affects: `ensure-env-vars-defined`
  - Consumers to update: none (description-only change)
  - Category: parameter-description

- [ ] **`deploy-docker-compose` lacks outputs but `deploy-to-cloud-run` exports `service-url`** — naming pair is deliberately split by backend (Compose vs Cloud Run) which is acceptable per rubric §2 (Docker Compose stepping stone). BUT a caller written against the `deploy-*` verb contract cannot uniformly read a post-deploy URL. Add optional `service-urls` output to `deploy-docker-compose` (JSON array, possibly empty for Compose since URLs are caller-known). Low priority — only matters if the course ever starts reading deployed URLs uniformly.
  - Affects: `deploy-docker-compose`
  - Consumers to update: none (additive output)
  - Category: devops-alignment

- [ ] **`deploy-to-cloud-run` input `secrets` description is ambiguous — the shape is not standard "KEY=VALUE" but "KEY=SECRET_NAME:VERSION"** — description is adequate in isolation. But the `env-vars` description says "KEY=VALUE format, one per line" while `secrets` says "KEY=SECRET_NAME:VERSION format, one per line" — the two shapes are similar enough that a caller may conflate them. Add an example to each description clarifying they are distinct shapes. Minor description fix.
  - Affects: `deploy-to-cloud-run`
  - Consumers to update: none (description-only change)
  - Category: parameter-description

- [ ] **`compose-prerelease-version` input `suffix` allows any string — describe the standard (SemVer) set** — description says "Prerelease suffix (e.g., rc, dev, alpha, beta)". SemVer §9 defines `alpha`, `beta`, `rc`, `preview` as the standard set; `compose-release-version`'s regex already encodes exactly this set (`rc|alpha|beta|preview`). Align the `compose-prerelease-version` input description with the SemVer-§9 set that `compose-release-version` inverts — otherwise a caller can compose a prerelease with a suffix that `compose-release-version` cannot later strip. Description-only change.
  - Affects: `compose-prerelease-version`
  - Consumers to update: none
  - Category: parameter-description

- [ ] **`resolve-prerelease-tag` uses the GitHub Releases API (`gh api repos/.../releases`) where git tag enumeration would suffice** — per rubric §3.2 "Prefer VCS over platform API": the action enumerates published GitHub Releases to find the latest prerelease matching a prefix. Sibling action `resolve-latest-prerelease-tag` does the same computation via `git ls-remote --tags` + `sort -V`. Two viable options: (1) replace the Releases API call with `git ls-remote --tags` inside `resolve-prerelease-tag`; (2) deprecate `resolve-prerelease-tag` and have all callers use `resolve-latest-prerelease-tag` + `ensure-tag-exists` for the "explicit tag" path. **Recommended: option 2** — it moves toward Tier 2 composability and removes one platform-API dependency.
  - Affects: `resolve-prerelease-tag`, `resolve-latest-prerelease-tag`, `ensure-tag-exists`
  - Consumers to update (0 in shop, 1 in gh-optivem, 0 in optivem-testing):
    - `gh-optivem/.github/workflows/gh-local-stage.yml`
  - Category: duplicate

- [ ] **`promote-docker-images` interpolates token into `env:` but reads it inline via `$REGISTRY_TOKEN` in `echo "$REGISTRY_TOKEN" | docker login ...` — fine, but the surrounding step also exposes the token through `env:` without masking. Not a finding — env-bridge pattern is correct.** — KEEP. No change.

- [ ] **`promote-docker-images` input `image-urls` on input AND output use the same key** — input is "source URLs with old tags", output is "URLs with new tags applied". Both named `image-urls`. Not a bug per se (GitHub treats them separately), but the description needs to make it unambiguous. Description-only fix: add "(output)" / "(input)" to each description's opening clause to prevent a confused reader from thinking the action is a pure filter.
  - Affects: `promote-docker-images`
  - Consumers to update: none
  - Category: parameter-description

- [ ] **`render-system-stage-summary` passes values to `render-stage-summary` through markdown formatting literals — `fromJSON('"\n"')` is a workaround for lack of multi-line string support in `format()`** — this is a repo smell but a known GitHub Actions expression limitation. Not a rubric violation. Document in the action description that the composite relies on this pattern. No code change needed; filed for awareness only. Drop.

- [ ] **`setup-dotnet` has no `working-directory` input, unlike `setup-java-gradle` and `setup-node`** — inconsistency. For .NET, `actions/setup-dotnet@v5` also doesn't need `working-directory` because dependency restore is done separately (`dotnet restore`). So the absence is correct — but `setup-java-gradle` and `setup-node` both require `working-directory`; the three actions form an inconsistent set. Two options: (1) drop `working-directory` from Java and Node if the caller can `cd` before the action (per rubric §5, primitive-first); (2) add `working-directory` to .NET for symmetry even if unused. **Recommended: option 1** — Java and Node setup should be pure language-runtime setup; the caller should `cd` or pass cwd on later steps. This also aligns with `actions/setup-node@v5` which doesn't take `working-directory` (it takes `cache-dependency-path` for cache only).
  - Affects: `setup-java-gradle`, `setup-node`
  - Consumers to update (57 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-java-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
    - (and 24+ stage/qa workflows for java + typescript that use these setups — all monolith-*-java-*.yml, multitier-*-java-*.yml, monolith-*-typescript-*.yml, multitier-*-typescript-*.yml, multitier-backend-*.yml, multitier-frontend-*.yml that currently pass `working-directory`)
  - Category: consolidation

- [ ] **`wait-for-github-workflow` input `max-discovery-attempts` is a raw count — caller ergonomics would be improved by a `discovery-timeout-seconds` equivalent** — the action computes `MAX_DISCOVERY_ATTEMPTS * POLL_INTERVAL` in the error message. Accept both inputs (one derived from the other, mutually exclusive) or expose a single `discovery-timeout-seconds`. Minor ergonomics; defer unless a caller complains.
  - Affects: `wait-for-github-workflow`
  - Consumers to update: none (additive)
  - Category: parameter-naming

- [ ] **`trigger-and-wait-for-github-workflow` hardcodes a 10-second `sleep` between trigger and run-ID lookup** — rubric §1.3 "Bounded retry with backoff": this is a flat sleep, not retry — so not a retry-budget violation. But `gh run list --limit 1` may race and pick up a run from a *previous* triggering if the dispatch is slow. Action does not verify the matched run's `headSha`. Filing as a correctness concern, not a naming concern. Consider polling `gh run list` until a run with matching `headSha` (or created_at > dispatch time) appears instead of a fixed 10s sleep.
  - Affects: `trigger-and-wait-for-github-workflow`
  - Consumers to update: none (internal behavior change)
  - Category: devops-alignment

- [ ] **`render-stage-summary` input `stage-result` accepts `success|failure|cancelled|skipped` but the description only says "Result of the stage job (success, failure, cancelled, etc.)"** — `etc.` is imprecise and the validation in `runs:` rejects anything outside the exact set. Pin the description to the enumerated set.
  - Affects: `render-stage-summary`
  - Consumers to update: none (description-only)
  - Category: parameter-description

- [ ] **`build-docker-image` description says "Pair with `tag-docker-image-local` for additional tags"** — action `tag-docker-image-local` does not exist in this repo. The action is `tag-docker-image`. Stale cross-reference in description. Fix description.
  - Affects: `build-docker-image`
  - Consumers to update: none (description-only)
  - Category: parameter-description

- [ ] **`cleanup-github-deployments` always runs `git fetch --tags --force` as its first step, but the action's purpose is to delete Deployments (a platform API concept, not a tag concept)** — the fetch-tags step appears to be leftover from a shared template. It is a side effect that has no relationship to the action's stated purpose. Remove the `git fetch --tags --force` step from `cleanup-github-deployments/action.yml`.
  - Affects: `cleanup-github-deployments`
  - Consumers to update: none (internal step removal)
  - Category: devops-alignment
