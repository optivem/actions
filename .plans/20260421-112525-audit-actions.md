# Actions audit plan — 2026-04-21 11:25 UTC

🤖 **Picked up by agent** — `ValentinaLaptop` at `2026-04-21T11:33:37Z`

Generated by `actions-auditor` agent. `backwards_compatible = false` (breaking changes are in scope). Publication intent: internal-only.

See report section in workflow output for full context.

## Items

- [ ] **Unify repo-identifier input names to `repository` across all actions** — Four styles currently coexist: `repo` (7 actions), `repository` (2 actions), split `repo-owner` + `repo-name` (2 actions), and unique one-off shapes. Per mainstream-first (updated rubric §0 / §1): `repository` is the canonical input name used by `actions/checkout` and every official `actions/*` action. Rename all `repo` inputs → `repository`; collapse the split `repo-owner` + `repo-name` pairs → single `repository` (`owner/repo` form). Update consumers accordingly. Affected actions (both the `repo` majority AND the `repository` minority — unifying all to `repository` with a uniform default of `${{ github.repository }}`): `check-tag-pattern-exists`, `ensure-tag-exists`, `get-github-commit-status`, `resolve-commit`, `resolve-github-prerelease-tag`, `resolve-tag-from-sha`, `trigger-and-wait-for-github-workflow`, `wait-for-github-commit-run` (currently `repo`); `create-github-release`, `read-github-workflow-run-number` (already `repository` — only consumers that pass `repository:` need verification); `has-unverified-github-commit-status`, `has-update-since-last-github-workflow-run` (currently split `repo-owner` + `repo-name`).
  - Affects: `check-tag-pattern-exists`, `ensure-tag-exists`, `get-github-commit-status`, `resolve-commit`, `resolve-github-prerelease-tag`, `resolve-tag-from-sha`, `trigger-and-wait-for-github-workflow`, `wait-for-github-commit-run`, `has-unverified-github-commit-status`, `has-update-since-last-github-workflow-run`
  - Consumers to update (every caller that passes `with: { repo: ... }` or `with: { repo-owner: ..., repo-name: ... }`): all three consumer repos. Concrete file list to be verified by the executing agent via grep before editing — expected ~20 files in shop, ~4 files in gh-optivem, possibly 1 in optivem-testing.
  - Category: devops-alignment

- [ ] **Replace implicit-env token reads in `commit-files-via-github-contents-api` and `trigger-and-wait-for-github-workflow` with a named `token` input** — These actions read from `env.GITHUB_TOKEN` / `env.GH_TOKEN` (implicit, no interface contract in `action.yml`). Rubric §1.4 requires the token to be a named input, `default: ${{ github.token }}`. Remove the `env.` fallback and add the named input. For `trigger-and-wait-for-github-workflow`, preserve the workflow-dispatch-permissions intent by documenting that the caller may pass a higher-scoped token.
  - Affects: `commit-files-via-github-contents-api`, `trigger-and-wait-for-github-workflow`
  - Consumers to update (2 in shop, 7 in gh-optivem, 2 in optivem-testing):
    - `shop/.github/workflows/auto-bump-patch.yml` (commit-files-via-github-contents-api)
    - `shop/.github/workflows/bump-versions.yml` (commit-files-via-github-contents-api)
    - `shop/.github/workflows/_meta-prerelease-pipeline.yml` (trigger-and-wait-for-github-workflow)
    - `shop/.github/workflows/_prerelease-pipeline.yml` (trigger-and-wait-for-github-workflow)
    - `shop/.github/workflows/meta-release-stage.yml` (trigger-and-wait-for-github-workflow)
    - `gh-optivem/.github/workflows/gh-local-stage.yml` (trigger-and-wait-for-github-workflow — 7 call sites in one file)
    - `optivem-testing/.github/workflows/one-click-release.yml` (trigger-and-wait-for-github-workflow)
    - Call sites that currently pass the token via the workflow-level `env:` must switch to `with: { token: ... }` (or drop the env override if `github.token` is sufficient, which it usually is).
  - Category: devops-alignment

- [ ] **Split `tag-docker-images` into `pull-docker-image` + `push-docker-image` primitives** — ⏳ Deferred: author decision at end of batch. Action mixes three concerns (pull source, retag, push). Per rubric §6 (one concern per action) and §5 (primitives first), the flow should be expressible as `pull-docker-image` → `tag-docker-image-local` → `push-docker-image`. `tag-docker-images` stays as a thin composite that calls the primitives in sequence, so the common-case caller isn't churned.
  - Affects: `tag-docker-images` (to be decomposed), new `pull-docker-image` primitive
  - Consumers to update (24 in shop, 0 in gh-optivem, 0 in optivem-testing) — only for callers choosing to adopt the primitives; the thin composite keeps the existing call shape:
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
  - Category: devops-alignment

- [ ] **Decompose `create-component-tags` into `read-target-version` + `compose-component-tag` + `create-and-push-tag`** — Action mixes two concerns from rubric §6 (Version source + Git tag creation): iterates `component:VERSION-path` entries, reads each VERSION file, composes `{component}-v{version}`, and creates+pushes. Callers that want a different tag shape or a different version source (`package.json`, `pom.xml`) have to fork the action. Extract the per-component logic into primitives so `create-component-tags` becomes a thin iterator over them (or inline at the call site). A new `compose-component-tag` pure-string primitive is small enough to inline if not wanted.
  - Affects: `create-component-tags`, new `compose-component-tag` primitive
  - Consumers to update (12 in shop, 0 in gh-optivem, 0 in optivem-testing) — only if callers choose to call primitives inline rather than via the thin composite:
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
  - Category: devops-alignment
