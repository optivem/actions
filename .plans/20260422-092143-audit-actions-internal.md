# Actions audit — Internal — 2026-04-22 09:21 UTC

> 🤖 **Picked up by agent** — `ValentinaLaptop` at `2026-04-22T11:54:54Z`

**Report:** `.reports/20260422-092143-audit-actions-internal.md` — full internal-scope findings and per-item reasoning.

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. Publication intent: internal-only.

## Plan — Internal items

Items with no consumer-visible surface change. Safe to land independently of consumer updates.

- [ ] **Add `git-host` input to `bump-patch-versions` with default `github.com`** — Tier 2 action that calls `git ls-remote --tags origin` against a hardcoded `origin` remote. Per rubric §3.1 Tier 2 sub-rule, Tier 2 actions must not implicitly tie to a specific remote URL or git host. Other Tier 2 actions (`check-tag-pattern-exists`, `validate-tag-exists`, `create-component-tags`, `publish-tag`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`) accept `git-host`; `bump-patch-versions` does not. Add `git-host` input with default `github.com` and compose the remote URL the same way siblings do. Classified Internal because the default preserves today's effective behaviour on `github.com` — no consumer workflow needs updating.
  - Affects: `bump-patch-versions`
  - Consumers to update: none (additive input with default that preserves current behaviour for consumers on `github.com`)
  - Category: devops-alignment

- [ ] **Unify `author:` + `branding:` posture across all 37 `action.yml`s** — Publication intent is internal-only per rubric §1.4 and actions-auditor spec. `author:` and `branding:` are Marketplace metadata; they add noise in internal repos. Currently inconsistent — the majority carry `author: 'Optivem'` + a `branding:` block; a subset (`compose-prerelease-version`, `compose-release-version`, `read-base-version`, `deploy-docker-compose`, `render-system-stage-summary`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow`, `validate-version-unreleased`) does not. Pick one direction and apply uniformly. Recommended (best-practice for internal-only): strip `author:` + `branding:` from all 37 actions — no consumer reads them. Lower-churn alternative: add the blocks to the subset that lacks them. Either direction is acceptable; inconsistency is not.
  - Affects: all 37 actions
  - Consumers to update: none (metadata-only)
  - Category: devops-alignment
