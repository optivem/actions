# Actions audit — External — 2026-04-24 12:45 UTC

**Report:** `.reports/20260424-124513-audit-actions-external.md` — full findings, inventory, rejected alternatives, and per-item reasoning.

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

## Plan — Breaking items

Items that force consumer workflows to be updated. Complete these first and co-ordinate landing with consumer updates in sibling repos (`shop`, `gh-optivem`, `optivem-testing`). Ordered by **impact** — primarily call-site count descending, with DevOps-alignment/correctness findings promoted above pure naming findings at the same call-site count. Ties broken by affected-dir alphabetical order.

- [ ] **1. Rename `generate-release-notes` → `generate-prod-release-notes`** — the current name promises a generic release-notes generator but the implementation hardcodes a `"🚀 <version> PROD"` title and a promotion-framed body; rename so the specialisation is honest (rubric §4 `name-misleading`).
  - Affects: `generate-release-notes` (→ `generate-prod-release-notes`)
  - Consumers to update (12 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage-cloud.yml`
  - Category: naming

- [ ] **2. Relax `get-commit-status` to return empty outputs on no-match (instead of `exit 1`)** — `get-*` implies read-with-empty-on-miss per rubric §4 canonical-semantics table; current hard-fail is `validate-*`-shaped and contradicts the verb; also makes the action asymmetric with its `check-commit-status-exists` (fail-open) and `create-commit-status` siblings. Caller that relies on the failure must add an explicit `if description == ''` branch.
  - Affects: `get-commit-status`
  - Consumers to update (0 in shop, 1 in gh-optivem, 0 in optivem-testing):
    - `gh-optivem/.github/workflows/gh-release-stage.yml`
  - Category: devops-alignment

## Plan — Non-breaking items

Items that change the action's public surface but leave existing consumers working unchanged (safe cap defaults, additive outputs, relaxed `required`, deprecation messages that name a replacement, new optional inputs with behaviour-preserving defaults). Safe to land independently of consumer updates — consumers adopt at their own pace. Ordered by impact within this section using the same rules as the breaking list. Numbering continues from the Breaking section.

- [ ] **3. Relax `deploy-docker-compose.environment` and `.version` from `required: true` to `required: false` with `default: ''`** — both inputs are described as display-only ("surfaced in logs; the action does not route based on this value"); hard-requiring them raises a schema error for missing callers without any behavioural benefit. Relaxing `required` cannot break any of the 36 shop callers (all currently pass a value), but the signature change is surface-visible.
  - Affects: `deploy-docker-compose`
  - Consumers to update: none (surface change is preservative — all 36 callers already pass values; callers need not update to keep working)
  - Category: parameter-required
