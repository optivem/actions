# Actions audit plan — 2026-04-21 16:10 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false` (breaking changes are in scope). `scope = naming`. Publication intent: internal-only.

See report section in workflow output for full context.

## Items

- [ ] **Rename `compose-commit-stage-summary` → `render-commit-stage-summary`** — the composite calls `render-stage-summary`, which writes to `$GITHUB_STEP_SUMMARY` (a side effect). `compose-*` in this repo is the "pure string transform" verb; this action is not pure. Rename the directory and update the `name:` field to `Render Commit Stage Summary`.
  - Affects: `compose-commit-stage-summary`
  - Consumers to update (4 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: naming

- [ ] **Rename `compose-system-stage-summary` → `render-system-stage-summary`** — the composite calls `render-stage-summary` (writes to `$GITHUB_STEP_SUMMARY`) and emits a `::notice` annotation on skipped. Side-effecting. Apply the same `compose-*` → `render-*` principle. Update the `name:` field to `Render System Stage Summary`.
  - Affects: `compose-system-stage-summary`
  - Consumers to update (34 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-signoff.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-signoff.yml`
    - `shop/.github/workflows/monolith-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-signoff.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-signoff.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-qa-signoff.yml`
  - Category: naming

- [ ] **Rename `push-docker-images` → `push-docker-image`** — the action accepts a single image (plus its SHA/latest/version tag variants for the same base image) — every input is singular (`image-sha-tag`, `image-latest-url`, `image-version-tag`) and every output is singular (`image-digest-url`). The plural name misleads callers into expecting an array input, contradicting the actual contract. Sibling `tag-docker-image` (singular) already operates on a single image. Update the `name:` field to `Push Docker Image`.
  - Affects: `push-docker-images`
  - Consumers to update (4 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: naming

- [ ] **Remove `get-github-workflow-run-number`** — zero call sites across shop, gh-optivem, and optivem-testing, AND zero internal composite usage. No evidence of active consumer; safe to delete. If a future consumer materializes, re-add at that point.
  - Affects: `get-github-workflow-run-number`
  - Consumers to update (0 in shop, 0 in gh-optivem, 0 in optivem-testing): none
  - Category: dead-code

- [ ] **Rename input `push-docker-images.image-latest-url` → `image-latest-tag` (or make consistent with sibling inputs)** — the two other tag inputs on the same action are named with the `-tag` suffix (`image-sha-tag`, `image-version-tag`) and carry full image-URL values (registry/namespace/name:tag); `image-latest-url` carries the same shape but diverges in suffix. Pick one convention and apply it uniformly across the three inputs. Recommended: align all three on `-tag` (or all three on `-url`), updating the `description:` to make the full-URL shape explicit.
  - Affects: `push-docker-images`
  - Consumers to update (4 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: parameter-naming

- [ ] **Rename `resolve-latest-tag-from-sha.repo` → `repository`** — every other action in this repo that takes a repo identifier uses `repository` (`ensure-tag-exists`, `check-tag-pattern-exists`, `resolve-tag-from-sha`, `resolve-commit`, `resolve-latest-prerelease-tag`, `check-update-since-last-github-workflow-run`, `check-unverified-commit-status`, `commit-files`, `create-github-release`, `get-commit-status`, `get-github-workflow-run-number`, `trigger-and-wait-for-github-workflow`, `wait-for-github-workflow`, `resolve-prerelease-tag`). Only `resolve-latest-tag-from-sha` uses the short `repo` alias. Mainstream (`actions/checkout`) and sibling-consistency both point to `repository`.
  - Affects: `resolve-latest-tag-from-sha`
  - Consumers to update (1 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/_prerelease-pipeline.yml`
  - Category: parameter-naming

- [ ] **Rename `resolve-latest-tag-from-sha.github-token` → `token`** — every other action in this repo that takes a GitHub token uses `token` with `default: ${{ github.token }}`. Only `resolve-latest-tag-from-sha` uses the longer `github-token`. Sibling-consistency and mainstream (`actions/github-script` uses `github-token`, but `actions/checkout` and most others use `token`; this repo has standardised on `token`) both point to `token`.
  - Affects: `resolve-latest-tag-from-sha`
  - Consumers to update (1 in shop, 0 in gh-optivem, 0 in optivem-testing):
    - `shop/.github/workflows/_prerelease-pipeline.yml`
  - Category: parameter-naming

- [ ] **Rename `resolve-latest-tag-from-sha.sha` → `commit-sha`** — other actions in this repo use `commit-sha` for disambiguation when a SHA input sits next to other SHA-shaped strings; `resolve-tag-from-sha` uses bare `sha`, so this is not a clean repo-wide win, but the rubric §1 recommends `commit-sha` "when disambiguation is needed." Since this input IS a commit SHA (distinct from a tag-object SHA handled inside the action), prefer `commit-sha`. Apply the same rename to `resolve-tag-from-sha.sha` for consistency.
  - Affects: `resolve-latest-tag-from-sha`, `resolve-tag-from-sha`, `check-sha-on-branch`, `check-unverified-commit-status`, `create-commit-status`, `get-commit-status`, `publish-tag`
  - Consumers to update (~40 call sites total across shop + gh-optivem): touches every workflow calling `publish-tag`, `resolve-tag-from-sha`, or `check-sha-on-branch`. Specifically:
    - all `shop/.github/workflows/*-commit-stage.yml` files (for `check-sha-on-branch` and `publish-tag`)
    - all `shop/.github/workflows/*-qa-*.yml`, `*-acceptance-stage*.yml`, `*-prod-stage*.yml` files (for `publish-tag`)
    - `gh-optivem/.github/workflows/gh-acceptance-stage.yml` (`resolve-tag-from-sha`, `publish-tag`, `check-unverified-commit-status`, `create-commit-status`)
    - `gh-optivem/.github/workflows/gh-release-stage.yml` (`resolve-tag-from-sha`, `publish-tag`, `get-commit-status`, `ensure-tag-exists`)
  - Category: parameter-naming
  - **Note:** high-churn item. If taken, do it in one sweep so the final state is consistent.

- [ ] **Standardise `wait-for-github-workflow.commit-sha` vs. `publish-tag.sha` naming** — `wait-for-github-workflow` already uses `commit-sha`; several siblings use bare `sha`. Pick one repo-wide. Recommended: `commit-sha` (matches rubric §1 disambiguation guidance). This is the same scope as the preceding item; if that rename is done, include `wait-for-github-workflow` too (no change needed if `commit-sha` is the target).
  - Affects: `check-sha-on-branch`, `check-unverified-commit-status`, `create-commit-status`, `get-commit-status`, `publish-tag`, `resolve-latest-tag-from-sha`, `resolve-tag-from-sha`
  - Category: parameter-naming
  - **Note:** subsumed by the `sha` → `commit-sha` sweep above; list here for traceability only. Remove when that item is executed.

- [ ] **Fix `required`/`default` contradictions in description-declared mandatory inputs** — a few inputs declare `required: false` with no `default:`, which silently resolves to an empty string at runtime (see rubric §3 `required-default-contradiction`). Concrete cases:
  - `deploy-to-cloud-run.env-vars` (`required: false`, `default: ''`) — acceptable (empty means "no env vars")
  - `deploy-to-cloud-run.secrets` (`required: false`, `default: ''`) — acceptable
  - `compose-commit-stage-summary.success-artifact-url` — `required: false`, NO default, and validated at runtime to be non-empty when `stage-result == 'success'`. The cross-input validation is fine, but declare `default: ''` explicitly for clarity.
  - `compose-system-stage-summary.success-version`, `.success-artifact-ids`, `.skipped-reason`, `.latest-artifact-ids`, `.latest-updated-at`, `.last-run-at` — all `required: false`, NO default. Add `default: ''` to each to make the empty-string semantics explicit.
  - `publish-tag.token`, `create-component-tags.token` — `required: false`, `default: ''`. This is a deliberate "if empty, use ambient `origin` remote" pattern. Works but the empty-string semantics should be documented in the description (they are — skip).
  - Affects: `compose-commit-stage-summary`, `compose-system-stage-summary`
  - Consumers to update: none (adding `default: ''` is a no-op for callers — purely additive clarity)
  - Category: parameter-default

- [ ] **Clarify `build-docker-image.image-name` vs. `tag-docker-image.image-name` shape** — both inputs use the same name, but `build-docker-image.image-name` is the *local* image base (no registry prefix) while `tag-docker-image.image-name` is the image-name segment embedded in a registry-qualified URL. A reader inspecting one can't tell from the name alone whether it's local or registry-qualified. Update the descriptions of both to spell out the shape explicitly. No rename needed — the shared naming is fine if the descriptions disambiguate.
  - Affects: `build-docker-image`, `tag-docker-image`
  - Consumers to update: none (description-only change)
  - Category: parameter-description

- [ ] **Rename output `resolve-docker-image-digests.image-digest-urls` description to specify shape, and rename output `latest-image-timestamp` to `latest-updated-at`** — `latest-image-timestamp` is a timestamp output; the repo's other timestamp outputs use `updated-at` / `last-run-at` / `verified-at` / `last-updated-at` (noun `last-updated-at` on `compose-system-stage-summary`, `verified-at` on `check-unverified-commit-status`, `timestamp` on `resolve-commit`, `last-run-at` on `check-update-since-last-github-workflow-run`). "Timestamp" is not itself wrong, but `latest-image-timestamp` is inconsistent — the downstream consumer of this output (`compose-system-stage-summary.latest-updated-at`) has already renamed it to `latest-updated-at`. Align the producer to the consumer.
  - Affects: `resolve-docker-image-digests`
  - Consumers to update (13 in shop): every `*-acceptance-stage*.yml` that reads `steps.<id>.outputs.latest-image-timestamp`:
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
  - Category: parameter-naming

- [ ] **Align `cleanup-github-prereleases.container-packages` description shape** — description says "Comma-separated list of container package names". The input IS comma-separated, but sibling `cleanup-github-deployments.protected-environments` and `ensure-env-vars-defined.names` also take comma-separated/newline-separated lists with inconsistent separators across the repo (see below). Non-blocking, but confirm/document. No rename proposed unless repo-wide list-separator normalization is taken.
  - Affects: `cleanup-github-prereleases`, `cleanup-github-deployments`, `ensure-env-vars-defined`, `deploy-to-cloud-run` (both `env-vars` and `secrets` use newline-separated)
  - Consumers to update: none (description-only, or a repo-wide separator normalization pass — out of scope for a naming audit)
  - Category: parameter-description

- [ ] **Add `default: ${{ github.repository }}` to `resolve-latest-tag-from-sha.repo`** — input is `required: true`, but every other `repository` input in this repo defaults to `${{ github.repository }}` when `required: false`. If the rename to `repository` is taken (see item above), also flip to `required: false, default: ${{ github.repository }}` to match sibling actions. Do not add a default without the rename — the input-name change and the default change should happen together.
  - Affects: `resolve-latest-tag-from-sha`
  - Consumers to update: same as the `repo` → `repository` rename above (1 in shop)
  - Category: parameter-default
  - **Note:** subsumed by the `repo` → `repository` rename item if executed together.

- [ ] **Document output shape in `compose-release-notes.notes-file`** — the output is a path to a tempfile (`$(mktemp)`). The description says so, but the consumer needs to know the file is (a) on the runner's disk, (b) not persisted beyond the workflow run, (c) plain markdown. Update the description to spell out: "Absolute path to a temp file on the runner filesystem, valid only for the duration of the current job. Contents: plain markdown (UTF-8)."
  - Affects: `compose-release-notes`
  - Consumers to update: none (description-only)
  - Category: parameter-description
