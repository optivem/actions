# Actions audit — Internal — 2026-04-23 17:08 UTC

**Report:** `.reports/20260423-170825-audit-actions-internal.md` — full internal-scope findings and per-item reasoning.

Generated by `actions-auditor`. `backwards_compatible = false`. `scope = all`. `mode = both`. Publication intent: internal-only.

## Plan — Internal items

Items with no consumer-visible surface change. Safe to land independently of consumer updates. Ordered by impact within internal (call-site count descending is still the secondary signal — high-traffic actions still rank higher even when the change is internal).

- [ ] **1. Extract shared git-remote + credential-clearing helpers into `shared/`** — add a `shared/remote-url.sh` (or similarly-named helper) that composes the `https://x-access-token:<TOKEN>@<HOST>/<REPO>.git` URL once, and a companion helper for the `http.<host>.extraheader` / `includeIf.gitdir:` credential-clearing block that `publish-tag` and `create-component-tags` currently duplicate. Replace the inline bash in every affected action with `source "$GITHUB_ACTION_PATH/../shared/<helper>.sh"` + one helper call. No consumer surface change.
  - Affects: `bump-patch-versions`, `check-tag-pattern-exists`, `create-component-tags`, `publish-tag`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`, `validate-tag-exists` (URL composition — 8 actions); `create-component-tags`, `publish-tag` (credentials-clearing — 2 actions, bundled)
  - Consumers to update: none (pure internal refactor — no `action.yml` interface change)
  - Category: devops-alignment
APPROVED


- [ ] **2. Tighten `deploy-docker-compose` display-only input descriptions** — `environment` and `version` are both `required: true` but documented as "display only". Clarify in the description that the value is logged only — e.g. `environment`: "Display-only label shown in step logs (e.g., `acceptance`, `qa`, `production`). Required — every run must announce its target. The action does not route based on this value." — and similar for `version`. No input renaming, no `required:` change, no default added.
  - Affects: `deploy-docker-compose`
  - Consumers to update: none (description-only; 36 current callers unchanged)
  - Category: parameter-description
APPROVED

