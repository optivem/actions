# Actions audit plan — 2026-04-20 14:50 UTC (last updated 2026-04-20)

Generated by `actions-auditor` agent. `backwards_compatible = false`.

Scope of today's already-applied work (for reference, not in this plan):
- Added `ensure-tag-exists` (tool-agnostic, git ls-remote) and replaced all 43 `ensure-release-exists` call sites; archived the old action.
- Renamed 10 GitHub-API-specific actions to carry the `github` segment (`trigger-and-wait-for-github-workflow`, `cleanup-github-deployments`, `create-github-commit-status`, `get-github-commit-status`, `resolve-github-prerelease-tag`, `wait-for-github-commit-run`, `check-github-container-packages-exist`, `has-update-since-last-github-workflow-run`, `has-unverified-github-commit-status`, `resolve-github-commit`).
- Renamed `validate-config` → `ensure-env-vars-defined`, `find-latest-docker-images` → `resolve-docker-image-digests`.
- Reverted `resolve-github-tag-from-sha` → `resolve-tag-from-sha` and rewrote implementation with `git ls-remote --tags` (Tier 2, tool-agnostic); TODO comment in the action about the scale tradeoff.
- Fixed broken `check-target-version-exists` reference in eshop (replaced with `check-version-unreleased`).
- Updated the `actions-auditor` agent definition with the 3-tier naming model (Tier 1 generic, Tier 2 git-native, Tier 3 GitHub-platform-specific) and the "prefer git over `gh api` wherever both work" implementation rule.

## Items

### Deferred to the separate-tags-from-releases plan

See `.plans/20260420-133247-separate-tags-from-releases.md` for the detailed phases. The following audit-plan candidates are intentionally NOT in this file because the separate-tags refactor reshapes them:

- `cleanup-prereleases` → will be renamed / retooled as `cleanup-rc-tags` (Phase 5 there).
- `find-release-by-run` → will be replaced with a tag-based lookup (Phase 3 there).
- `create-release` / `create-prerelease` / `promote-prerelease` → the split-tag-vs-release restructure changes their shape entirely (Phases 1, 2, 4 there). Renames and release-url output additions follow from that restructure, not from this audit plan.
- `push-docker-image` `:latest` push — decided today: `:latest` is load-bearing for acceptance stage (pulls newest post-commit image to run tests). Keep as-is; documented in the agent definition.
