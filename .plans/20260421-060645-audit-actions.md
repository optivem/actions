# Actions audit plan — 2026-04-21 06:06 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false`.

See report section in workflow output for full context.

## Items

- [ ] **Delete `find-release-by-run` action** — zero call sites across all consumers; only stale plan files reference it. Remove the directory.
  - Affects: `find-release-by-run`
  - Consumers to update (0): none
  - Category: dead-code
VJ: APPROVED

- [ ] **Rename `resolve-docker-image-digests` → `resolve-docker-image-digests` stays, but evaluate concern-mixing** — the action emits both per-image `image-digest-urls` (primary return) and `latest-image-timestamp` (cross-image observability). Classification: both outputs are actively read by 10+ consumer workflows, both are legitimate (one digests, the other is the max timestamp used for `has-update-since-last-github-workflow-run`). No action required beyond acknowledgment — retained as-is.
  - Affects: `resolve-docker-image-digests`
  - Consumers to update: none
  - Category: consolidation
VJ: OK so this tays as is


- [ ] **Merge `generate-release-version` and `generate-status-version` into a single action `compose-release-version`** — both are pure string-manipulation versioning primitives that produce a final release tag from a base version. `generate-release-version` strips prerelease suffixes (`v1.0.0-rc.1` → `v1.0.0`); `generate-status-version` appends `-{environment}-{status}` (`v1.0.0-rc.1` → `v1.0.0-rc.1-qa-deployed`). Both are called together in QA/prod stages. Consolidate as `compose-release-version` with a `mode: strip-prerelease|append-status` input (or split into two explicit pure-function primitives with clearer names like `strip-prerelease-suffix` and `append-environment-status`). Recommended: two narrower primitives with distinct verbs; current names both begin `generate-` which obscures that one removes and one adds.
  - Affects: `generate-release-version`, `generate-status-version`
  - Consumers to update (25 unique files):
    - `gh-optivem/.github/workflows/gh-release-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-signoff.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-signoff.yml`
    - `shop/.github/workflows/monolith-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-signoff.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-signoff.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-qa-signoff.yml`
    - `shop/.github/workflows/multitier-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-qa-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-qa-signoff.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage.yml`
  - Category: consolidation
VJ: I'm worried because stauts isn't like a real version, lets discuss this

- [ ] **Rename `deploy-to-cloud-run` → `deploy-google-cloud-run`** (or retain). Current name is fine per Marketplace conventions (the noun unambiguously names Google's product), but flag if the repo prefers brand-prefixed cloud names for symmetry with future `deploy-aws-ecs`, `deploy-azure-app-service` primitives. Recommend retaining the current name — "Cloud Run" is a proper noun product and adding `google-` is redundant. No code change.
  - Affects: `deploy-to-cloud-run`
  - Consumers to update: none
  - Category: naming
VJ: Approve

- [ ] **Rename `run-docker-compose` → split into `start-docker-compose` + `wait-for-urls` (or keep combined but rename)** — current action violates one-concern-per-action. It (a) starts the Compose stack and (b) polls HTTP health URLs. Concerns are independent — some callers may want to start without waiting (debug), or wait against an already-running stack. Split into `start-docker-compose` (start + working-directory + compose-file) and reuse existing `wait-for-url` for each system URL. Alternatively, extract an optional sibling `wait-for-urls` (plural) that accepts a JSON list and keep `run-docker-compose` as a thin composite that chains them.
  - Affects: `run-docker-compose`, `wait-for-url`
  - Consumers to update (24 files):
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-qa-stage.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage.yml`
  - Category: consolidation
VJ: Approived. Yes do it too - `wait-for-urls`

- [ ] **Drop `system-url` input from `run-docker-compose`** — never passed by any consumer; all 24 callers use the `systems` JSON array input instead. `system-url` is speculative flexibility (YAGNI). After the split above, this goes away naturally; if the split is deferred, drop `system-url` as part of a standalone cleanup.
  - Affects: `run-docker-compose`
  - Consumers to update: none (input is never passed)
  - Category: dead-input
VJ: Approved

- [ ] **Drop redundant `registry: ghcr.io` pass-through at `build-docker-image` callers** — all 7 callers pass `registry: ghcr.io`, which is already the action's default. Keep the input on the action (standard config axis for auth targeting — Twelve-Factor Factor III), but simplify the callers by removing the redundant `registry: ghcr.io` line from each caller's `with:` block.
  - Affects: `build-docker-image`
  - Consumers to update (7 files):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-dotnet-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-java-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: dead-input
 APPROVED 

- [ ] **Rename `generate-prerelease-version` verb inconsistency** — the action is a version *composer* (it calls `compose-prerelease-version` after guarding the final tag). The `generate-` vs `compose-` verb pair is confusing because both exist (`compose-prerelease-version` is a primitive, `generate-prerelease-version` is its composite wrapper). Rename to `generate-prerelease-version` → `compose-prerelease-version-guarded` OR rename the primitive to `build-prerelease-version-string`. Recommend: keep the primitive named `compose-prerelease-version` and rename the composite to `generate-rc-version` (RC-specific because it hardcodes `suffix: rc` semantics in its description; adopt the tighter name that matches its role in the pipeline). This also frees `generate-` for the sibling action and makes the concern explicit.
  - Affects: `generate-prerelease-version`
  - Consumers to update (13 files):
    - `gh-optivem/.github/workflows/gh-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage.yml`
  - Category: naming
VJ: Actually why do we even have both?

- [ ] **Drop `suffix` input from `generate-prerelease-version`** — declared with default `rc` and never overridden by any of the 13 callers. This is speculative flexibility (no use case documented for non-RC suffixes at this layer; the primitive `compose-prerelease-version` remains fully parameterized for callers that want a non-RC suffix). YAGNI. If the hardcoded `rc` needs to be swappable in the future, re-add it then. After drop, the action always composes an RC tag, matching its actual role in the pipeline.
  - Affects: `generate-prerelease-version`
  - Consumers to update: none (input is never passed)
  - Category: dead-input
VJ: Askign quesiton, see above

- [ ] **Split `deploy-to-cloud-run` concerns: extract verify/wait** — the action deploys to Cloud Run *and* waits for readiness via `wait-for-url`. `deploy` and `wait-for-service-ready` are separate concerns (Farley: deployment publishes; readiness is a separate post-deploy smoke). Refactor to make readiness opt-in via a `wait: true|false` input, or extract the `wait-for-url` call out of the composite so callers decide when to verify. Low priority — the current shape is a thin composite acceptable by rule §"Composites allowed when they wrap 2–4 primitives in a fixed, well-known order" — but flag so maintainers revisit if deploy targets grow.
  - Affects: `deploy-to-cloud-run`
  - Consumers to update: none (behavior preserved by default)
  - Category: consolidation
APPROVED

- [ ] **Reduce `deploy-to-cloud-run` speculative input surface** — audit the 10 optional inputs (`region`, `port`, `env-vars`, `secrets`, `memory`, `cpu`, `min-instances`, `max-instances`, `allow-unauthenticated`). Per spec: `region`, `port`, `env-vars`, `secrets`, `memory`, `cpu`, `min-instances`, `max-instances`, `allow-unauthenticated` are all **standard Cloud Run sizing / network / auth axes** — KEEP, even though no current caller overrides them. These represent the Cloud Run service contract and would be needed by the moment a course adds a canary with tighter limits. Classification: KEEP all.
  - Affects: `deploy-to-cloud-run`
  - Consumers to update: none
  - Category: dead-input
APRPOVED

- [ ] **Drop dead content-slot inputs from `summarize-stage`** — `stage-failure-content` and `stage-cancelled-content` are declared but never passed by any caller (external or internal). `stage-content` is passed by `summarize-system-stage` only. `stage-skipped-content` is passed by `summarize-system-stage` only. `stage-success-content` is passed by both internal composites. Classification per spec: the failure/cancelled slots are speculative flexibility — no caller has ever wanted custom failure/cancelled content. Drop `stage-failure-content` and `stage-cancelled-content`. Keep the others (success/skipped actively used; stage-content used by one caller).
  - Affects: `summarize-stage`
  - Consumers to update: none (inputs never passed)
  - Category: dead-input
APPROVED

- [ ] **Verify `registry-username` input on `push-docker-image`** — declared with default `${{ github.actor }}` and never overridden by any caller. Classification: KEEP — registry credentials / username is a **standard auth axis** (Marketplace convention; registries other than GHCR expect an explicit username). No action.
  - Affects: `push-docker-image`
  - Consumers to update: none
  - Category: dead-input
VJ: Ok so no action?

- [ ] **Clarify `find-release-by-run` descriptor before removal** — The action's behavior ("find a GitHub Release whose body contains a given run ID") is a search the platform supports without the Releases API (it could be replaced by a tag-scan primitive). Already flagged for removal in an older plan file. Proceed with the delete in the first plan item.
  - Affects: `find-release-by-run`
  - Consumers to update: none
  - Category: dead-code
APPROVED

- [ ] **Rename `map-signoff-result` if unclear** — pure function mapping `approved|other` → `success|failure`. Name is accurate (`map-` verb, result-shape transform). No change. (Listed only to confirm audit pass — not actionable.)
  - Affects: `map-signoff-result`
  - Consumers to update: none
  - Category: naming
VJ: what are alternatives?

- [ ] **Split `build-docker-image` concerns** — the action currently (a) builds, (b) composes a dev prerelease version (via `compose-prerelease-version`) when `component-version` is passed, (c) tags three ways (SHA / latest / version). Concerns (b) and (c) belong to the tagging primitive family, not to build. The action mixes: version source (`compose-prerelease-version`), artifact-type-specific tagging (`docker tag`), and build. Per the concerns table, split into `build-docker-image` (build only, emits a local image) and `tag-docker-image-with-commit-sha` / `tag-docker-image-as-latest` / `tag-docker-image-with-version` primitives — or a single flexible `tag-docker-images` (already exists!) that accepts a list of tags. Recommended: `build-docker-image` emits a single local image keyed by SHA; callers chain `tag-docker-images` for the post-build tagging. This unwinds the `compose-prerelease-version` dependency inside `build-docker-image`, restoring separation of concerns.
  - Affects: `build-docker-image`, `tag-docker-images`, `compose-prerelease-version`
  - Consumers to update (7 files):
    - `shop/.github/workflows/monolith-dotnet-commit-stage.yml`
    - `shop/.github/workflows/monolith-java-commit-stage.yml`
    - `shop/.github/workflows/monolith-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-dotnet-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-java-commit-stage.yml`
    - `shop/.github/workflows/multitier-backend-typescript-commit-stage.yml`
    - `shop/.github/workflows/multitier-frontend-react-commit-stage.yml`
  - Category: consolidation
VJ: Porpose to me how it would look like before doing

- [ ] **Split `summarize-system-stage` presentation from data** — the action (a) validates inputs, (b) processes two artifact-ID arrays via PowerShell, (c) formats markdown, (d) calls `summarize-stage` for final rendering, and (e) emits a notice annotation on skipped. Concerns: data-validation, data-formatting, rendering, notification. Consolidation opportunity: factor the artifact-ID formatter into a separate `format-artifact-list` primitive that both commit-stage and system-stage summaries can reuse, then have the two `summarize-*-stage` composites compose existing primitives (`format-artifact-list` + `summarize-stage`). Low priority — works today.
  - Affects: `summarize-system-stage`, `summarize-commit-stage`, `summarize-stage`
  - Consumers to update: none (behavior preserved)
  - Category: consolidation
VJ: Give me example of what it would look like

- [ ] **Document `summarize-stage` as internal primitive** — zero external call sites; only used by `summarize-commit-stage` and `summarize-system-stage`. Not a deletion candidate (it's the underlying renderer). Add a sentence to its description noting it is a primitive intended for use via the stage-specific composites, OR promote to a callable from consumer workflows (it's already generic enough).
  - Affects: `summarize-stage`
  - Consumers to update: none
  - Category: dead-code
VJ: OK

- [ ] **Document `wait-for-url` as internal primitive** — zero external call sites; only used by `deploy-to-cloud-run`. Per the composite-opacity rule, since `wait-for-url` is a legitimate primitive, keep it and expose it for direct use (it's already callable). Update README to list it alongside the others.
  - Affects: `wait-for-url`
  - Consumers to update: none
  - Category: dead-code
VJ: I though it will be re-usable?