# Actions audit plan — 2026-04-22 07:48 UTC

Generated by `actions-auditor` agent. `backwards_compatible = false` (breaking changes are in scope). `scope = all`. Publication intent: internal-only.

Coexisting with three in-progress plans in `.plans/`:
- `20260422-045423-audit-actions.md` (2 open follow-up items about stale eshop contracts and stale course teaching material).
- `20260422-055006-audit-actions.md` (10 open items covering `exist` → `exists` rename, `check-unverified-commit-status` output polarity, etc.).
- `20260422-073636-consolidate-mainstream-actions.md` (8 open items covering migration of `setup-dotnet`, `setup-java-gradle`, `setup-node`, `create-github-release`, `deploy-to-cloud-run` to Marketplace composites + tag cut).

De-duplication applied: items that overlap with the existing plans (e.g. `setup-*` deletion, `create-github-release` → `softprops/action-gh-release`, `deploy-to-cloud-run` → `google-github-actions/deploy-cloudrun`, `check-ghcr-packages-exist` output rename, `compose-prerelease-version` suffix description) are omitted from this queue and discussed in the report's **Challenges to existing plan** subsection instead.

See report in workflow output for full context.

## Items

> ⏳ **All remaining items are Deferred** — awaiting VJ's responses to the inline `VJ:` questions on each item. The executed items (APPROVED) and dropped items (no-action / out-of-scope) have already been removed from this plan.

- [ ] **Replace hand-rolled `promote-docker-images` with `docker/buildx-imagetools create`** — the action uses hand-rolled `docker pull` + `docker tag` + `docker push` on every source image. Per rubric §1.6 (Container image build/tag/push — use mainstream composite actions) this is the three-primitive split that the rubric explicitly rejects for Docker: it forces every image through a local-daemon round trip, skips BuildKit's remote cache and multi-arch handling, and emits no attestation. Mainstream alternatives: (a) `docker/buildx-imagetools create --tag <new> <source>` — server-side retag, no image data crosses the runner, supports manifest lists natively; (b) `oras tag` for OCI-generic retagging. Recommended: replace the `docker pull` → `docker tag` → `docker push` loop inside `promote-docker-images.sh` with `docker buildx imagetools create`. This keeps the action surface unchanged (same inputs, same `image-urls` output) and removes the three-primitive violation.
  - Affects: `promote-docker-images`
  - Consumers to update (24 in shop):
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-qa-stage.yml`
    - `shop/.github/workflows/multitier-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-prod-stage.yml`
    - `shop/.github/workflows/multitier-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage.yml`
    - `shop/.github/workflows/multitier-typescript-prod-stage-cloud.yml`
  - Category: devops-alignment
VJ: Are you sure same behavior?


- [ ] **`trigger-and-wait-for-github-workflow` hardcodes a flat 10-second `sleep` + `--limit 1` run-list lookup (§1.3 Fail-fast / bounded polling)** — a flat 10s sleep followed by a naive "most recent run matches" lookup races whenever the dispatch takes longer than 10s OR when two dispatches land on the same ref close together (both have the same `headSha`, the lookup returns the wrong one). Replace with bounded polling that matches on `createdAt >= dispatch_time`, with max attempts capped via an existing input. This is agent-flagged in the coexisting `20260422-055006-audit-actions.md` plan; retained here only to make the rubric citation explicit.
  - Affects: `trigger-and-wait-for-github-workflow`
  - Consumers to update: none (internal behavior change)
  - Category: devops-alignment (dedup with existing plan — omit if executing that plan first)
VJL What is insytry best practice regarding waiting with polling?

- [ ] **`compose-prerelease-version` rejects `prefix` values containing a hyphen but doesn't document the constraint (§1.5 SemVer validation)** — the composer output shape is `{prefix}-v{base}-{suffix}.{build-number}`. A prefix like `monolith-java` (three hyphens including the join) still produces a valid SemVer pre-release (`monolith-java-v1.0.0-rc.7` — the pre-release segment is `rc.7`), but a consumer inspecting the string by splitting on `-` would trip. Flag as `type-shape-ambiguous`: add to the `prefix` description a worked example showing the emitted tag shape and a reminder that the caller's downstream parsers must split on `-v` / `-` with awareness, not blind split on first hyphen.
  - Affects: `compose-prerelease-version`
  - Consumers to update: none (description-only)
  - Category: parameter-description
VJ: why it rejects whne I have hyphen ijn my names?

- [ ] **`compose-release-version` strip regex is case-insensitive and locked to `rc|alpha|beta|preview` — hardcoded set without matching `compose-prerelease-version` contract** — the `sed -E` regex `s/-(rc|alpha|beta|preview)\.[0-9]+$//I` silently strips only these four suffixes. Any other SemVer-§9-valid suffix (e.g. `dev`, `nightly`, `hotfix`) passes through unchanged, meaning the "release version" still carries a prerelease segment. This is a correctness gap between the two composer actions. Recommended: (1) narrow the documented `suffix` set in `compose-prerelease-version` to match this regex exactly, AND (2) fail-fast in `compose-release-version` when the stripped output still matches `-[A-Za-z0-9-]+\.[0-9]+$`. Better than silent pass-through.
  - Affects: `compose-release-version`, `compose-prerelease-version`
  - Consumers to update: none (tightens validation; all current consumers use `rc` suffix)
  - Category: devops-alignment
  VJ: actually can we give freedom for the value of suffix, to be anything?

- [ ] **`check-timestamp-newer` relies on `string > string` bash lexicographic comparison on ISO 8601 timestamps (§1.3 fail-fast correctness)** — `if [[ "$SUBJECT" > "$BASELINE" ]]`. This works ONLY when both strings are UTC Z-suffixed same-format. If a caller passes a timezone-aware timestamp (e.g. `2026-04-22T10:15:00+00:00`) or one with different fractional-second precision, the comparison silently mis-orders and the workflow drops runs it should execute (or runs it should skip). Replace the bash comparison with explicit `date -d` normalisation before comparing, OR fail-fast if either input doesn't match the documented `YYYY-MM-DDTHH:MM:SSZ` shape.
  - Affects: `check-timestamp-newer`
  - Consumers to update: none (internal correctness fix)
  - Category: devops-alignment

- [ ] **`check-tag-pattern-exists` and `resolve-commit` accept `token` input but use it via direct URL-embedding (§1.4 Secrets / auth)** — both actions build `https://x-access-token:${TOKEN}@${GIT_HOST}/${REPO}.git` and hand that to `git ls-remote` / `git fetch`. The TOKEN ends up rendered into the literal command line, which violates the §1.4 guidance "Never interpolate secrets into a rendered command". Correct pattern: use `git -c http.extraheader="Authorization: bearer $TOKEN" fetch ...` with `TOKEN` set via step-level `env:` only. Rubric §3.3 specifically carves out the URL-embedding pattern as acceptable for Tier 2 portability today, but the §1.4 secret-masking concern still applies. Recommended: migrate to the `http.extraheader` pattern while keeping the `token` input shape unchanged.
  - Affects: `check-tag-pattern-exists`, `ensure-tag-exists`, `publish-tag`, `create-component-tags`, `resolve-commit`, `resolve-latest-prerelease-tag`, `resolve-latest-tag-from-sha`
  - Consumers to update: none (internal)
  - Category: devops-alignment

- [ ] **Descriptions containing emoji in `$GITHUB_STEP_SUMMARY` must round-trip as UTF-8 — verify on Windows runners** — not an immediate fix, but `render-stage-summary`, `format-artifact-list`, `deploy-to-cloud-run`, and ~6 other actions emit emoji characters (`✅` `🚀` `📦`) via `echo`/`printf` to `$GITHUB_STEP_SUMMARY`. GitHub runners are UTF-8 by default; if any Windows consumer switches shell to `cmd.exe` (not expected but not impossible) the output corrupts. Low-risk: leave as-is, but add a one-line "UTF-8 shell required" note to `README.md`'s shell policy section so the implicit dependency is visible.
  - Affects: `README.md`
  - Consumers to update: none
  - Category: devops-alignment
Or should be remove emojis?

- [ ] **`compose-prerelease-status` output `tag` should be named `status-tag`** — the action composes a *status marker* tag like `v1.0.0-rc.1-qa-deployed`, distinct from a standard git tag. The current output name `tag` is too generic — a consumer reading `steps.<id>.outputs.tag` cannot tell at a glance whether it is a release tag, a prerelease tag, or a status tag. Rename to `status-tag` to match the action's description ("Output is a status-marker tag, not a SemVer version.") and align with sibling `create-commit-status` which qualifies its outputs. Rubric §4 — the name must honestly describe what the value is.
  - Affects: `compose-prerelease-status`
  - Consumers to update (18 in shop):
    - `shop/.github/workflows/monolith-dotnet-qa-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-qa-signoff.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-stage.yml`
    - `shop/.github/workflows/monolith-java-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-qa-signoff.yml`
    - `shop/.github/workflows/monolith-java-prod-stage.yml`
    - `shop/.github/workflows/monolith-java-prod-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage.yml`
    - `shop/.github/workflows/monolith-typescript-qa-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-qa-signoff.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage.yml`
    - `shop/.github/workflows/monolith-typescript-prod-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-qa-stage.yml`
    - `shop/.github/workflows/multitier-java-qa-stage.yml`
    - `shop/.github/workflows/multitier-typescript-qa-stage.yml`
  - Category: parameter-naming

- [ ] **`compose-prerelease-status` input `status` has no documented enum but the action passes it through unchanged** — the description says "Status marker (e.g., deployed, passed, failed, approved)" but the runs block doesn't validate. Callers in `shop/.github/workflows/*-qa-signoff.yml` pass `approved` / `rejected`; callers in `*-qa-stage.yml` pass `deployed`; callers in `*-prod-stage.yml` pass `deployed`. This is a `type-shape-ambiguous` description — pin to an explicit enum so the action fails-fast on unknown values AND the description maps cleanly to `compose-release-notes`'s status icon mapping.
  - Affects: `compose-prerelease-status`
  - Consumers to update: none (description + fail-fast validation)
  - Category: parameter-description

- [ ] **Rename `check-update-since-last-github-workflow-run` → `check-update-since-last-workflow-run`** — current name carries the `github-workflow-run` compound noun. Rubric §3.1 says `github` prefix applies when the bare core noun is ambiguous; the CORE noun being checked is `update`, a generic concept. The `github-workflow-run` is an implementation detail — the action receives a workflow-run timestamp via `gh api run list`, so its Tier is 3 for the underlying API call. BUT the action's PURPOSE is tier-agnostic: it's a "has the subject moved since we last looked" check. A parallel implementation for GitLab would compare against GitLab pipeline-run timestamps. Recommended: split into a tier-3 helper (`get-last-successful-workflow-run-timestamp`) that returns just the timestamp, and a tier-1 comparator (`check-timestamp-newer`) that does the comparison. This isolates the platform-API surface behind one primitive and makes the comparator reusable. (§5 primitives-first, §6 one-concern-per-action.)
  - Affects: `check-update-since-last-github-workflow-run`
  - Consumers to update (18 in shop):
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/monolith-typescript-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-dotnet-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-java-acceptance-stage-legacy.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-cloud.yml`
    - `shop/.github/workflows/multitier-typescript-acceptance-stage-legacy.yml`
  - Category: consolidation
VJ: last time you adivsed me tbhat for workflow, to have the github prefix

- [ ] **`render-stage-summary` hardcodes stage-name + stage-result but `render-system-stage-summary` builds richer content on top — composite/primitive seam is thin but `stage-success-content`, `stage-content`, and `stage-skipped-content` all carry interleaved templating** — this isn't a violation of §5/§6 on its own, but the three "content" inputs mutate the rendered output in subtle ways (fallback text on empty; markdown embedded in a Bash `case`). Consider whether the primitive should accept a single `content:` JSON input keyed by stage result (`{success: "...", failure: "...", skipped: "..."}`) — this collapses the three inputs into one and makes the contract easier to see at the call site. Low-priority refactor; flagged as a candidate for a future consolidation pass, not an immediate fix.
  - Affects: `render-stage-summary`
  - Consumers to update: none (flagged for future consideration)
  - Category: (future-consideration — do not execute immediately)
VJ: what's the proposal?

Per project convention, items are removed from this file as they are executed, the file is deleted when empty, and the `.plans/` directory is deleted when it contains no files.
